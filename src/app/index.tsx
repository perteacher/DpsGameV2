import { useEffect, useRef, useState } from 'react'
import { Dimensions, Image, ImageBackground, Platform, ScrollView, StyleSheet, Text, TouchableOpacity, useWindowDimensions, Vibration, View } from 'react-native'
import { SafeAreaProvider, SafeAreaView } from 'react-native-safe-area-context'
import AsyncStorage from '@react-native-async-storage/async-storage'

const BG_LOBBY = require('../../assets/images/bg_lobby.png')
const BG_HUNTING = require('../../assets/images/bg_hunting.png')
const BG_BOSS = require('../../assets/images/bg_boss.png')
const BEACON_강화 = require('../../assets/images/beacon_enhance.png')
const BEACON_보스 = require('../../assets/images/beacon_boss.png')
const BEACON_사냥 = require('../../assets/images/beacon_hunt.png')
const BEACON_판매 = require('../../assets/images/beacon_sell.png')

// 웹: Dimensions.get가 0 리턴 가능 → window.innerWidth/Height 우선 사용
const _dim = Dimensions.get('window')
let _w = _dim.width
let _h = _dim.height
if (Platform.OS === 'web' && typeof window !== 'undefined') {
  if (window.innerWidth > 0) _w = window.innerWidth
  if (window.innerHeight > 0) _h = window.innerHeight
}
const 화면W = _w > 0 ? _w : 400
const 화면H = _h > 0 ? _h : 800

const 저장키 = 'dps_game_save_v4'  // v4: 보주/크리스탈 시스템

// ============================================
// 유틸 함수
// ============================================

const 단위목록 = ['', '만', '억', '조', '경', '해', '자', '양', '구', '간', '정', '재', '극', '항', '아', '나', '불', '무']

function 숫자포맷(n: number): string {
  if (n < 10000) return Math.floor(n).toString()
  let i = 0
  let v = n
  while (v >= 10000 && i < 단위목록.length - 1) {
    v = v / 10000
    i++
  }
  return (v >= 1000 ? Math.floor(v).toString() : v.toFixed(1)) + 단위목록[i]
}

// 강화 기준확률 룩업테이블 (레벨별 원본 맵 실측값)
// 인덱스 = 현재 강 단계 (1~50)
const 강화확률표: number[] = [
  0,                                          // 0 (unused)
  0.600, 0.600, 0.575, 0.543,                // 1~4
  0.500, 0.500, 0.500, 0.500, 0.470,         // 5~9
  0.465, 0.463, 0.452, 0.450, 0.450,         // 10~14
  0.440, 0.440, 0.440, 0.430, 0.420,         // 15~19
  0.380, 0.380, 0.360, 0.360, 0.350,         // 20~24
  0.300, 0.250, 0.250, 0.250, 0.250,         // 25~29
  0.250, 0.250, 0.200, 0.200, 0.200,         // 30~34
  0.200, 0.160, 0.120, 0.100, 0.050,         // 35~39
  0, 0, 0, 0,                                // 40~43 (특수 구간 — 기본 0%)
  0.100, 0.070, 0.040, 0.010, 0.000,         // 44~48 (개별확률)
  0.005,                                      // 49 (고정)
  0,                                          // 50 (초월 시스템)
]

// 밸런스: 강화 난이도 밴드별 배율 (확률표에 곱). 낮출수록 그 구간 강화 느려짐 → 타임라인 튜닝 노브.
// 목표 타임라인(컨트롤+방치): 26강 ~1일 / 26→41 ~1주 / 41→51 ~1주 / 51→60 ~1달
const 강화배율_초반 = 0.73   // 1~25강 base-only (DP역산: 26강 ≈ 1일)
const 강화배율_중반 = 0.90   // 26~39강 base-only (DP역산: 26→40 ≈ 5.5일, +40강관문 ≈ 1주)
function 강화기본확률(단계: number): number {
  const 배율 = 단계 <= 25 ? 강화배율_초반 : (단계 <= 39 ? 강화배율_중반 : 1)
  return (강화확률표[단계] ?? 0) * 배율
}

type 강화스텟 = {
  가산1강: number; 가산2강: number; 가산3강: number
  가산1강2: number; 가산2강2: number; 가산3강2: number
  특수강화: number; 특수강화2: number
  특수파괴방지: number; 특수파괴방지2: number
  가산44강: number; 가산45강: number; 가산46강: number; 가산47강: number; 가산48강: number
  돈수급량: number; 유닛공업: number
  촉진: number
}

// 일반스텟 메타 (label, max, cost per +1, 효과텍스트 계산)
type 일반스텟메타 = { key: keyof 강화스텟; label: string; max: number; cost: number; 효과: (v: number) => string }
const 일반스텟표: 일반스텟메타[] = [
  { key: '돈수급량',      label: '💰 돈 수급량',           max: 100, cost: 100,   효과: v => `자원 +${v * 3}%` },
  { key: '유닛공업',      label: '⚔️ 보유 유닛 공격력업', max: 50,  cost: 20,    효과: v => `강도별 upg ×(1+${v})` },
  { key: '가산1강',       label: '🔨 +1강 확률',           max: 100, cost: 10,    효과: v => `+${(v * 0.1).toFixed(1)}%` },
  { key: '가산2강',       label: '🔨 +2강 확률',           max: 50,  cost: 200,   효과: v => `+${(v * 0.1).toFixed(1)}%` },
  { key: '가산3강',       label: '🔨 +3강 확률',           max: 30,  cost: 1000,  효과: v => `+${(v * 0.1).toFixed(1)}%` },
  { key: '특수강화',      label: '⚡ 특수 강화 확률',      max: 100, cost: 500,   효과: v => `+${(v * 0.1).toFixed(1)}%` },
  { key: '가산1강2',      label: '🔩 +1강 확률 (2)',       max: 50,  cost: 1000,  효과: v => `+${(v * 0.1).toFixed(1)}%` },
  { key: '가산2강2',      label: '🔩 +2강 확률 (2)',       max: 20,  cost: 2500,  효과: v => `+${(v * 0.1).toFixed(1)}%` },
  { key: '가산3강2',      label: '🔩 +3강 확률 (2)',       max: 10,  cost: 5000,  효과: v => `+${(v * 0.1).toFixed(1)}%` },
  { key: '특수강화2',     label: '⚡ 특수강화 (2)',        max: 50,  cost: 1000,  효과: v => `+${(v * 0.1).toFixed(1)}%` },
  { key: '특수파괴방지',  label: '🛡️ 파괴방지',           max: 300, cost: 250,   효과: v => `+${(v * 0.1).toFixed(1)}%` },
  { key: '특수파괴방지2', label: '🛡️ 파괴방지 (2)',       max: 100, cost: 1000,  효과: v => `+${(v * 0.1).toFixed(1)}%` },
  { key: '가산44강',      label: '🌟 44강 확률',           max: 100, cost: 1000,  효과: v => `+${(v * 0.1).toFixed(1)}%` },
  { key: '가산45강',      label: '🌟 45강 확률',           max: 100, cost: 2000,  효과: v => `+${(v * 0.1).toFixed(1)}%` },
  { key: '가산46강',      label: '🌟 46강 확률',           max: 100, cost: 3000,  효과: v => `+${(v * 0.1).toFixed(1)}%` },
  { key: '가산47강',      label: '🌟 47강 확률',           max: 100, cost: 4000,  효과: v => `+${(v * 0.1).toFixed(1)}%` },
  { key: '가산48강',      label: '💎 48강 확률',           max: 200, cost: 10000, 효과: v => `+${(v * 0.05).toFixed(2)}%` },
  { key: '촉진',          label: '🚀 촉진(자동생산)',      max: 100, cost: 3000,  효과: v => `자동생산 +${v}/tick` },
]

// 초월스텟 — 17종 신규 구조
type 초월스텟타입 = {
  추가초월확률: number
  추가51강: number; 추가52강: number; 추가53강: number; 추가54강: number; 추가55강: number
  공업51: number; 공업52: number; 공업53: number; 공업54: number; 공업55: number; 공업56: number
  추가54강2: number; 추가55강2: number
  추가융합: number
  추가57강: number; 추가58강: number
  촉진: number
}
const 초기초월스텟: 초월스텟타입 = {
  추가초월확률: 0,
  추가51강: 0, 추가52강: 0, 추가53강: 0, 추가54강: 0, 추가55강: 0,
  공업51: 0, 공업52: 0, 공업53: 0, 공업54: 0, 공업55: 0, 공업56: 0,
  추가54강2: 0, 추가55강2: 0,
  추가융합: 0,
  추가57강: 0, 추가58강: 0,
  촉진: 0,
}
type 초월스텟메타 = { key: keyof 초월스텟타입; label: string; max: number; cost: number; 효과: (v: number) => string }
const 초월스텟표: 초월스텟메타[] = [
  { key: '추가초월확률', label: '🌀 추가 초월확률',  max: 100, cost: 1,    효과: v => `+${(v * 0.001).toFixed(3)}%` },
  { key: '추가51강',     label: '⚡ 추가 51강 확률', max: 200, cost: 1,    효과: v => `+${(v * 0.1).toFixed(1)}%` },
  { key: '추가52강',     label: '⚡ 추가 52강 확률', max: 200, cost: 2,    효과: v => `+${(v * 0.1).toFixed(1)}%` },
  { key: '추가53강',     label: '⚡ 추가 53강 확률', max: 200, cost: 4,    효과: v => `+${(v * 0.1).toFixed(1)}%` },
  { key: '추가54강',     label: '⚡ 추가 54강 확률', max: 200, cost: 6,    효과: v => `+${(v * 0.01).toFixed(2)}%` },
  { key: '추가55강',     label: '⚡ 추가 55강 확률', max: 200, cost: 8,    효과: v => `+${(v * 0.01).toFixed(2)}%` },
  { key: '공업51',       label: '⚔️ 51강 유닛 공업', max: 50,  cost: 1,    효과: v => `+${v}` },
  { key: '공업52',       label: '⚔️ 52강 유닛 공업', max: 50,  cost: 2,    효과: v => `+${v}` },
  { key: '공업53',       label: '⚔️ 53강 유닛 공업', max: 50,  cost: 4,    효과: v => `+${v}` },
  { key: '공업54',       label: '⚔️ 54강 유닛 공업', max: 50,  cost: 6,    효과: v => `+${v}` },
  { key: '공업55',       label: '⚔️ 55강 유닛 공업', max: 30,  cost: 8,    효과: v => `+${v}` },
  { key: '공업56',       label: '⚔️ 56강 유닛 공업', max: 30,  cost: 10,   효과: v => `+${v}` },
  { key: '추가54강2',    label: '⚡ 추가 54강 확률 (2)', max: 160, cost: 200, 효과: v => `+${(v * 0.05).toFixed(2)}%` },
  { key: '추가55강2',    label: '⚡ 추가 55강 확률 (2)', max: 300, cost: 200, 효과: v => `+${(v * 0.02).toFixed(2)}%` },
  { key: '추가융합',     label: '🔮 추가 융합 확률', max: 100, cost: 250,  효과: v => `+${(v * 0.1).toFixed(1)}%p` },
  { key: '추가57강',     label: '💥 추가 57강 확률', max: 50,  cost: 1000, 효과: v => `+${(v * 0.1).toFixed(1)}%` },
  { key: '추가58강',     label: '💥 추가 58강 확률', max: 50,  cost: 1500, 효과: v => `+${(v * 0.1).toFixed(1)}%` },
  { key: '촉진',         label: '🚀 촉진(자동생산)', max: 200, cost: 100,  효과: v => `자동생산 +${v}/tick` },
]

// 강화 시도 — 스텟 적용 버전
// 반환: 0=실패, 1=+1, 2=+2, 3=+3
// 외부p1: 보주/크리스탈/영구강화에서 오는 추가 성공확률 (0~1 소수)
// 보석p: 레벨별 보석 추가 확률
type 보석p타입 = { add44: number; add45: number; add46: number; add47: number; add48: number }
const 빈보석p: 보석p타입 = { add44: 0, add45: 0, add46: 0, add47: 0, add48: 0 }
// 일반p1: 일반확률(1~39강 +1)에만 더해지는 보너스 (고유유닛 추가1강 등). 개별/특수/고정/초월엔 미적용.
function 강화시도(단계: number, 스텟: 강화스텟, 외부p1: number = 0, 보석p: 보석p타입 = 빈보석p, 초월s?: 초월스텟타입, 일반p1: number = 0): number {
  if (단계 >= 60) return 0  // 60강 = MAX
  if (단계 === 50) return 0  // 50→51은 초월 시스템 별도
  const r = Math.random()
  // 51~59강: 외부보너스 + 신규 초월스텟 단계별 추가확률
  if (단계 >= 51) {
    let 초월보너스 = 0
    if (초월s) {
      if (단계 === 51) 초월보너스 = 초월s.추가51강 * 0.001
      else if (단계 === 52) 초월보너스 = 초월s.추가52강 * 0.001
      else if (단계 === 53) 초월보너스 = 초월s.추가53강 * 0.001
      else if (단계 === 54) 초월보너스 = 초월s.추가54강 * 0.0001 + 초월s.추가54강2 * 0.0005
      else if (단계 === 55) 초월보너스 = 초월s.추가55강 * 0.0001 + 초월s.추가55강2 * 0.0002
      else if (단계 === 56) 초월보너스 = 초월s.추가융합 * 0.001
      else if (단계 === 57) 초월보너스 = 초월s.추가57강 * 0.001
      else if (단계 === 58) 초월보너스 = 초월s.추가58강 * 0.001
    }
    const p = Math.min(0.95, 외부p1 + 초월보너스)
    return r < p ? 1 : 0
  }
  const base = 강화기본확률(단계)

  // 49강: 고정 0.5% (외부보너스 미적용)
  if (단계 === 49) {
    return r < 0.005 ? 1 : 0
  }
  // 48강: 개별확률 0% → 스텟 보너스만
  if (단계 === 48) {
    const bonus = 스텟.가산48강 * 0.0005 + 외부p1 + 보석p.add48
    return r < Math.min(0.95, bonus) ? 1 : 0
  }
  // 44~47강: 개별확률 + 스텟 + 외부보너스 + 보석
  if (단계 >= 44 && 단계 <= 47) {
    const lvBonus = [스텟.가산44강, 스텟.가산45강, 스텟.가산46강, 스텟.가산47강][단계 - 44] * 0.001
    const 보석추가 = [보석p.add44, 보석p.add45, 보석p.add46, 보석p.add47][단계 - 44]
    return r < Math.min(0.95, base + lvBonus + 외부p1 + 보석추가) ? 1 : 0
  }
  // 40~43강: 특수 확률 + 외부보너스 (궁극 등)
  if (단계 >= 40 && 단계 <= 43) {
    const bonus = (스텟.특수강화 + 스텟.특수강화2) * 0.001 + 외부p1
    return r < Math.min(0.95, bonus) ? 1 : 0
  }
  // 1~39강: base 확률만 (스텟/보석/보주/보스 보너스 전부 미적용) → 밴드 배율(초반/중반)이 시간 직접 제어
  // 38~39강은 +2/+3 없이 +1만(base×1.11). 그 외 1~37강은 +1/+2/+3 분리.
  // (스텟/보석은 40강+ 부터 효과 — 위 분기에서 처리)
  const s1 = Math.min(0.95, 단계 >= 38 ? base * 1.11 : base)
  const s2 = 단계 >= 38 ? 0 : Math.min(0.95, base / 10)
  const s3 = 단계 >= 38 ? 0 : Math.min(0.95, base / 100)
  if (r < s3) return 3
  if (r < s3 + s2) return 2
  if (r < s3 + s2 + s1) return 1
  return 0
}

function 강화비용(단계: number) {
  return 30 + 단계 * 20
}

// 강화 실패 페널티: 항상 파괴 (40~49강만 파괴방지 적용 — 신규 사양)
// 파괴방지: 포인트당 0.1% 파괴방지율, 최대 95%
function 강화실패결과(lv: number, 파괴방지: number = 0): { 감소: number; 파괴: boolean } {
  if (lv <= 5) return { 감소: 0, 파괴: false }  // 5강 이하 안전
  // 파괴방지는 40~49강 강화 실패에만 적용
  if (lv >= 40 && lv <= 49) {
    const 방지율 = Math.min(0.95, 파괴방지 * 0.001)
    if (Math.random() < 방지율) return { 감소: 0, 파괴: false }
  }
  return { 감소: 0, 파괴: true }  // 파괴
}

// 공격력/공속 — 유저 제공 실측 테이블 (유닛 기본 수치.xlsx)
// [강도] = { base: 기본공격력, upg: 1유닛공업당 추가, 속도: 단계명, 연타: 1회공격당타수 }
// 60강 — 미정 (0)
type 강도스펙 = { base: number; upg: number; 속도: '매우느림'|'느림'|'보통'|'빠름'|'극한'|'초월'; 연타: number }
const 강도표: Record<number, 강도스펙> = {
  1:  { base: 1,         upg: 0,       속도: '보통',    연타: 1 },
  2:  { base: 10,        upg: 1,       속도: '매우느림', 연타: 1 },
  3:  { base: 20,        upg: 2,       속도: '매우느림', 연타: 1 },
  4:  { base: 20,        upg: 2,       속도: '느림',    연타: 1 },
  5:  { base: 30,        upg: 3,       속도: '느림',    연타: 1 },
  6:  { base: 50,        upg: 5,       속도: '느림',    연타: 1 },
  7:  { base: 80,        upg: 8,       속도: '느림',    연타: 1 },
  8:  { base: 120,       upg: 12,      속도: '느림',    연타: 3 },
  9:  { base: 150,       upg: 15,      속도: '느림',    연타: 2 },
  10: { base: 250,       upg: 25,      속도: '느림',    연타: 1 },
  11: { base: 500,       upg: 50,      속도: '매우느림', 연타: 2 },
  12: { base: 400,       upg: 40,      속도: '느림',    연타: 1 },
  13: { base: 550,       upg: 55,      속도: '느림',    연타: 1 },
  14: { base: 1000,      upg: 100,     속도: '매우느림', 연타: 1 },
  15: { base: 1300,      upg: 130,     속도: '매우느림', 연타: 1 },
  16: { base: 1000,      upg: 100,     속도: '느림',    연타: 1 },
  17: { base: 1300,      upg: 130,     속도: '느림',    연타: 1 },
  18: { base: 1800,      upg: 180,     속도: '느림',    연타: 1 },
  19: { base: 600,       upg: 60,      속도: '빠름',    연타: 1 },
  20: { base: 2000,      upg: 200,     속도: '보통',    연타: 1 },
  21: { base: 2700,      upg: 270,     속도: '보통',    연타: 1 },
  22: { base: 3500,      upg: 350,     속도: '보통',    연타: 1 },
  23: { base: 5000,      upg: 500,     속도: '보통',    연타: 1 },
  24: { base: 6500,      upg: 650,     속도: '보통',    연타: 1 },
  25: { base: 5000,      upg: 500,     속도: '빠름',    연타: 1 },
  26: { base: 10,        upg: 1,       속도: '보통',    연타: 1 },
  27: { base: 30,        upg: 3,       속도: '느림',    연타: 1 },
  28: { base: 20,        upg: 2,       속도: '보통',    연타: 1 },
  29: { base: 30,        upg: 3,       속도: '보통',    연타: 1 },
  30: { base: 100,       upg: 10,      속도: '느림',    연타: 1 },
  31: { base: 80,        upg: 8,       속도: '보통',    연타: 1 },
  32: { base: 150,       upg: 15,      속도: '보통',    연타: 1 },
  33: { base: 700,       upg: 70,      속도: '매우느림', 연타: 1 },
  34: { base: 350,       upg: 35,      속도: '보통',    연타: 1 },
  35: { base: 1200,      upg: 120,     속도: '느림',    연타: 1 },
  36: { base: 1500,      upg: 150,     속도: '느림',    연타: 3 },
  37: { base: 3500,      upg: 350,     속도: '보통',    연타: 1 },  // 36강(연타3) DPS 역전 해소
  38: { base: 4000,      upg: 400,     속도: '보통',    연타: 1 },
  39: { base: 3500,      upg: 350,     속도: '빠름',    연타: 1 },
  40: { base: 4000,      upg: 400,     속도: '극한',    연타: 1 },  // 매우빠름→극한 매핑
  41: { base: 40,        upg: 4,       속도: '매우느림', 연타: 1 },
  42: { base: 70,        upg: 7,       속도: '느림',    연타: 1 },
  43: { base: 120,       upg: 12,      속도: '보통',    연타: 1 },
  44: { base: 400,       upg: 40,      속도: '빠름',    연타: 1 },
  45: { base: 2500,      upg: 250,     속도: '느림',    연타: 1 },
  46: { base: 2800,      upg: 280,     속도: '보통',    연타: 1 },
  47: { base: 3400,      upg: 340,     속도: '빠름',    연타: 1 },
  48: { base: 6550,      upg: 655,     속도: '극한',    연타: 1 },
  49: { base: 6550,      upg: 655,     속도: '초월',    연타: 1 },
  50: { base: 65500,     upg: 728,     속도: '초월',    연타: 1 },
  51: { base: 30,        upg: 3,       속도: '보통',    연타: 1 },
  52: { base: 200,       upg: 20,      속도: '느림',    연타: 1 },
  53: { base: 350,       upg: 35,      속도: '보통',    연타: 1 },
  54: { base: 700,       upg: 70,      속도: '빠름',    연타: 1 },
  55: { base: 14400,     upg: 1440,    속도: '느림',    연타: 1 },
  56: { base: 30000,     upg: 3000,    속도: '보통',    연타: 1 },
  57: { base: 1500000,   upg: 150000,  속도: '보통',    연타: 1 },
  58: { base: 15000000,  upg: 1500000, 속도: '보통',    연타: 1 },
  59: { base: 37373737,  upg: 3737373, 속도: '빠름',    연타: 1 },
  60: { base: 0,         upg: 0,       속도: '보통',    연타: 1 },  // 미정
}

// 공속 단계 → atk/s (cooldown 역수, 전체 2배 빠르게 적용)
const _공속맵: Record<string, number> = {
  '매우느림': 1 / 1.25,   // 0.80
  '느림':     1 / 0.9,    // 1.11
  '보통':     1 / 0.6,    // 1.67
  '빠름':     1 / 0.4,    // 2.50
  '극한':     1 / 0.25,   // 4.00
  '초월':     1 / 0.15,   // 6.67
}

// 새 공격력 — 신규 초월스텟 구조 적용
// - 일반.유닛공업: 모든 강도의 upg에 ×(1+v) (max 50)
// - 초월.공업51~56: 해당 강도의 upg에 추가 (1pt = +1 유닛공업 등가)
function 공격력(단계: number, 초월s?: 초월스텟타입, 일반공업: number = 0) {
  const t = 강도표[단계] ?? 강도표[1]
  let 공업합 = 일반공업
  if (초월s && 단계 >= 51 && 단계 <= 56) {
    const 공업키 = (['공업51','공업52','공업53','공업54','공업55','공업56'] as const)[단계 - 51]
    공업합 += 초월s[공업키] || 0
  }
  return t.base + t.upg * (1 + 공업합)
}

function 공격속도(단계: number) {
  const t = 강도표[단계] ?? 강도표[1]
  return _공속맵[t.속도] ?? 1.0
}

function 연타수(단계: number) {
  return (강도표[단계] ?? 강도표[1]).연타
}

// DPS = 공격력 × 공속 × 연타
function 유닛DPS(단계: number) {
  return 공격력(단계) * 공격속도(단계) * 연타수(단계)
}

// 51강+ 소득 배수: 51강 리셋된 낮은 공격력 보정 + 단계별 성장 → 50강보다 항상 더 잘 벌도록
// (50강 소득 ∝ 공격력×공속 ≈ 65500×6.67. 51강 base=30×1.67이므로 ×50000부터 시작)
function 소득배수(단계: number): number {
  if (단계 < 51) return 1
  return 50000 * Math.pow(3, 단계 - 51)
}

// DPS 단계별 자원 배수
function 자원배수(총DPS: number): number {
  if (총DPS < 50) return 1
  if (총DPS < 500) return 2
  if (총DPS < 5000) return 4
  if (총DPS < 50000) return 8
  if (총DPS < 500000) return 16
  if (총DPS < 5000000) return 32
  if (총DPS < 50000000) return 64
  return 128
}
// 밸런스: 전체 자원(미네랄) 수급 배율. 낮출수록 초반 경제·구입·진행 전반이 느려짐.
const 자원전역배율 = 0.12

// 캐릭터 레벨 — 다음 레벨 필요 경험치. 30만 레벨까지.
// 계산기(뉴비용.xlsx DB1!A열=레벨별 누적 필요경험치)와 완전 일치: lv→lv+1 = 3*lv*(lv-1)+10
// (검증: lv1→2=10, 2→3=16, 4→5=46, 99→100=29116, 1000→1001=2997010)
const 캐릭레벨최대 = 300000
function 다음경험치(lv: number): number {
  if (lv >= 캐릭레벨최대) return Infinity
  return 3 * lv * (lv - 1) + 10
}
// 초월경험치 → 초월레벨업 필요량 (30만 레벨 도달 후). 초월lv 1당 100 증가
function 다음초월경험치(초월lv: number): number {
  return 100 * Math.max(1, 초월lv)
}
// 51강 강화 1회 시도당 초월경험치
const 강화51초월경험 = 1
// 52강+ 유닛 판매 시 초월경험치 (강도별, 밸런스값 — 조정 가능)
const 판매초월경험표: Record<number, number> = {
  52: 20, 53: 350, 54: 10000, 55: 160000, 56: 3000000,
  57: 50000000, 58: 1000000000, 59: 30000000000, 60: 1000000000000,
}

// 1~56강 전부 구입 가능
const 생산강도목록 = Array.from({ length: 50 }, (_, i) => i + 1)  // 구입은 50강까지만 (51강+ 차단)
// 앵커 비용 (기존 데이터). 빠진 단계는 기하보간/외삽으로 채움.
// 41강+ 비용 대폭 상승 (가파른 램프)
const 생산비용표: Record<number, number> = {
  1: 1500, 7: 40000, 11: 700000, 15: 8000000,
  18: 100000000, 20: 600000000, 22: 3500000000, 24: 20000000000,
  26: 100000000000, 28: 600000000000, 30: 3500000000000, 32: 20000000000000,
  34: 1.2e14, 36: 8e14, 37: 6e15, 38: 5e16, 39: 8e17, 40: 1e19,
  41: 8e19, 42: 6e20, 43: 5e21, 44: 4e22, 45: 3e23, 46: 2e24,
  47: 1.5e25, 48: 1e26, 49: 8e26, 50: 6e27, 51: 5e28, 52: 4e29, 53: 3e30, 54: 2e31,
}
const _생산앵커 = Object.keys(생산비용표).map(Number).sort((a, b) => a - b)
function 생산비용(강도: number): number {
  if (생산비용표[강도] != null) return 생산비용표[강도]
  if (강도 <= _생산앵커[0]) return 생산비용표[_생산앵커[0]]
  const 최대앵커 = _생산앵커[_생산앵커.length - 1]
  if (강도 > 최대앵커) {
    // 외삽: 마지막 두 앵커의 단계당 배율로 연장
    const a = _생산앵커[_생산앵커.length - 2], b = 최대앵커
    const perLv = Math.pow(생산비용표[b] / 생산비용표[a], 1 / (b - a))
    return Math.round(생산비용표[b] * Math.pow(perLv, 강도 - b))
  }
  // 앵커 사이 기하보간
  let lo = _생산앵커[0], hi = 최대앵커
  for (const k of _생산앵커) { if (k <= 강도) lo = k; if (k >= 강도) { hi = k; break } }
  if (lo === hi) return 생산비용표[lo]
  const r = (강도 - lo) / (hi - lo)
  return Math.round(생산비용표[lo] * Math.pow(생산비용표[hi] / 생산비용표[lo], r))
}

// 마린 판매 시 드는 크레딧 (강도별). 51강·40강 이하는 0(무료).
function 판매크레딧비용(lv: number): number {
  if (lv >= 41 && lv <= 44) return 100
  if (lv >= 45 && lv <= 48) return 1e6
  if (lv === 49) return 1e7
  if (lv === 50) return 1e8
  if (lv === 52) return 1e12
  if (lv === 53) return 1e13
  if (lv === 54) return 1e14
  if (lv === 55) return 1e15
  if (lv === 56) return 1e16
  if (lv === 57) return 1e19
  if (lv === 58) return 1e20
  if (lv === 59) return 1e21
  if (lv === 60) return 1e28
  return 0
}

// 판매 보상 (원본 맵 45~60강 뽑기 확률표 기반)
// 기본 지급 + 각성보석/크리스탈조각 확률 드랍
// 출처: SCX 트리거 string 292~295, 802~803, 904~908, 126~147, 1232
// 크리스탈박스 등급: 노말/레어/유니크/갤럭시/퀘이사/오리진 — 해당 등급 중 1종 랜덤 +1개
type 크리스탈박스등급 = '노말' | '레어' | '유니크' | '갤럭시' | '퀘이사' | '오리진'
type 판매보상결과 = {
  무색조각: number; 응무조: number; 크리스탈조각: number
  ExP: number; 각성보석: number
  은하조각: number; 자각보주: number
  박스: 크리스탈박스등급[]
  // 신규 (xlsx 기반)
  일반XP: number   // 15~44강 — 캐릭레벨업
  크레딧: number   // 45~50강 확률 드랍
  판매불가: boolean // 51강
}
// xlsx '유닛 기본 수치' 시트 기반 — 신규 판매 보상 시스템
// 15~44: 일반XP만 / 45~50: 무색+크레딧/각성 확률 / 51:불가 / 52~56: 초월XP+크리조각+박스 / 57~59: 응무조+퀘이사 / 60: 응무조1억+4종25%
const 판매일반XP표: Record<number, number> = {
  15: 2, 16: 4, 17: 10, 18: 20, 19: 30, 20: 54,
  21: 90, 22: 160, 23: 320, 24: 670, 25: 1500,
  26: 15000, 27: 21000, 28: 37500, 29: 60000, 30: 102000,
  31: 188000, 32: 300000, 33: 500000, 34: 1000000, 35: 1600000,
  36: 5000000, 37: 8000000, 38: 12000000, 39: 20000000, 40: 50000000,
  41: 120000000, 42: 300000000, 43: 1600000000, 44: 8000000000,
}
function 판매보상(lv: number): 판매보상결과 {
  const base: 판매보상결과 = { 무색조각: 0, 응무조: 0, 크리스탈조각: 0, ExP: 0, 각성보석: 0, 은하조각: 0, 자각보주: 0, 박스: [], 일반XP: 0, 크레딧: 0, 판매불가: false }
  if (lv < 15) return base  // 14강 이하 보상 없음
  if (lv === 51) return { ...base, 판매불가: true }  // 51강 판매 불가 (엑셀 기준)
  // 15~44: 일반XP만
  if (lv <= 44) return { ...base, 일반XP: 판매일반XP표[lv] ?? 0 }
  // 45~50: 무색조각 기본 + 확률 (크레딧/각성/무색)
  if (lv === 45) return { ...base, 무색조각: 1
    + (Math.random() < 0.015 ? 10000 : 0)
    + (Math.random() < 0.05  ? 2000000 : 0),  // 크레딧 200만 5%
    각성보석: Math.random() < 0.0005 ? 1 : 0,
    크레딧: (Math.random() < 0.00002 ? 3222222222 : 0) + (Math.random() < 0.05 ? 2000000 : 0) }
  if (lv === 46) return { ...base, 무색조각: 4 + (Math.random() < 0.0055 ? 40000 : 0),
    각성보석: Math.random() < 0.00075 ? 1 : 0,
    크레딧: (Math.random() < 0.00002 ? 3222222222 : 0) + (Math.random() < 0.05 ? 3000000 : 0) }
  if (lv === 47) return { ...base, 무색조각: 7 + (Math.random() < 0.0047 ? 70000 : 0),
    각성보석: Math.random() < 0.0016 ? 1 : 0,
    크레딧: (Math.random() < 0.00002 ? 3222222222 : 0) + (Math.random() < 0.05 ? 4000000 : 0) }
  if (lv === 48) return { ...base, 무색조각: 10 + (Math.random() < 0.0099 ? 100000 : 0),
    각성보석: Math.random() < 0.005 ? 1 : 0,
    크레딧: (Math.random() < 0.00002 ? 3222222222 : 0) + (Math.random() < 0.05 ? 5000000 : 0) }
  if (lv === 49) return { ...base, 무색조각: 322 + (Math.random() < 0.015 ? 1000000 : 0) + (Math.random() < 0.15 ? 100000 : 0),
    각성보석: Math.random() < 0.01 ? 7 : 0,
    크레딧: (Math.random() < 0.00022 ? 3222222222 : 0) + (Math.random() < 0.30 ? 10000000 : 0) }
  if (lv === 50) return { ...base, 무색조각: 3222 + (Math.random() < 0.15 ? 4000000 : 0),
    각성보석: Math.random() < 0.05 ? 75 : 0,
    크레딧: (Math.random() < 0.0005 ? 32222222222 : 0) + (Math.random() < 0.35 ? 100000000 : 0) }
  // 52~56: 초월XP + 크리스탈조각 + 크리스탈 박스 확률
  if (lv === 52) {
    const 박스: 크리스탈박스등급[] = []
    if (Math.random() < 0.03) 박스.push('노말')
    return { ...base, 크리스탈조각: 1, ExP: 500, 박스 }
  }
  if (lv === 53) {
    const 박스: 크리스탈박스등급[] = []
    if (Math.random() < 0.01) 박스.push('레어')
    if (Math.random() < 0.07) 박스.push('노말')
    return { ...base, 크리스탈조각: 12, ExP: 8000, 박스 }
  }
  if (lv === 54) {
    const 박스: 크리스탈박스등급[] = []
    if (Math.random() < 0.00002) 박스.push('갤럭시')
    if (Math.random() < 0.001)   박스.push('유니크')
    if (Math.random() < 0.04)    박스.push('레어')
    if (Math.random() < 0.18)    박스.push('노말')
    return { ...base, 크리스탈조각: 300, ExP: 100000, 박스 }
  }
  if (lv === 55) {
    const 박스: 크리스탈박스등급[] = []
    if (Math.random() < 0.0001) 박스.push('갤럭시')
    if (Math.random() < 0.01)   박스.push('유니크')
    if (Math.random() < 0.30)   박스.push('레어')
    if (Math.random() < 0.50)   박스.push('노말')
    return { ...base, 크리스탈조각: 2000, ExP: 1400000, 박스 }
  }
  if (lv === 56) {
    const 박스: 크리스탈박스등급[] = []
    if (Math.random() < 0.001) 박스.push('갤럭시')
    if (Math.random() < 0.05)  박스.push('유니크')
    if (Math.random() < 0.70)  박스.push('레어')
    return { ...base, 크리스탈조각: 10000, ExP: 25000000, 박스 }
  }
  // 57~59: 응무조 + 퀘이사 박스
  if (lv === 57) {
    const 박스: 크리스탈박스등급[] = []
    if (Math.random() < 0.00002) 박스.push('퀘이사')
    if (Math.random() < 0.02) for (let i = 0; i < 50; i++)  박스.push('갤럭시')
    if (Math.random() < 0.10) for (let i = 0; i < 100; i++) 박스.push('유니크')
    return { ...base, 응무조: 1 + (Math.random() < 0.30 ? 10 : 0), 박스 }
  }
  if (lv === 58) {
    const 박스: 크리스탈박스등급[] = []
    if (Math.random() < 0.00002) for (let i = 0; i < 10; i++) 박스.push('퀘이사')
    if (Math.random() < 0.001) 박스.push('퀘이사')
    if (Math.random() < 0.05) for (let i = 0; i < 1000; i++) 박스.push('갤럭시')
    if (Math.random() < 0.10) for (let i = 0; i < 2000; i++) 박스.push('유니크')
    return { ...base, 응무조: 15 + (Math.random() < 0.30 ? 200 : 0), 박스 }
  }
  if (lv === 59) {
    const 박스: 크리스탈박스등급[] = []
    if (Math.random() < 0.0005) for (let i = 0; i < 50; i++) 박스.push('퀘이사')
    if (Math.random() < 0.01)   for (let i = 0; i < 3; i++)  박스.push('퀘이사')
    return { ...base, 응무조: 300 + (Math.random() < 0.50 ? 1000 : 0), 박스 }
  }
  // 60: 응무조 1억 + 창조/파멸/자각/은하 25%씩
  {
    const r = Math.random()
    const 결과: 판매보상결과 = { ...base, 응무조: 100000000 }
    if (r < 0.50) 결과.박스 = ['오리진']  // 창조 또는 파멸
    else if (r < 0.75) 결과.자각보주 = 1
    else 결과.은하조각 = 1
    return 결과
  }
}

// 크리스탈박스 → 해당 등급 크리스탈 중 1종 랜덤 픽
const 크리스탈박스내용물: Record<크리스탈박스등급, (keyof 명칭크리스탈목록)[]> = {
  노말: ['방어', '행운', '경험', '무력', '절약', '총명', '보호', '각성'],
  레어: ['홍색', '주황', '노랑', '초록', '파랑', '남색', '보라', '하늘색', '무색명칭'],
  유니크: ['흑색', '백색명칭'],
  갤럭시: ['우주'],
  퀘이사: ['길운Q', '무구Q', '집중Q', '절제Q', '탐욕Q', '증식Q', '미래Q', '돌파Q'],
  오리진: ['창조O', '파멸O'],
}
function 박스개봉(등급: 크리스탈박스등급): keyof 명칭크리스탈목록 {
  const 후보 = 크리스탈박스내용물[등급]
  return 후보[Math.floor(Math.random() * 후보.length)]
}

// 사냥터 슬롯 (총 공격수 마일스톤)
const 슬롯마일스톤 = [
  { 필요공격수: 0, 슬롯수: 8 },
  { 필요공격수: 1000, 슬롯수: 12 },
  { 필요공격수: 10000, 슬롯수: 16 },
  { 필요공격수: 100000, 슬롯수: 20 },
  { 필요공격수: 1000000, 슬롯수: 24 },
  { 필요공격수: 10000000, 슬롯수: 32 },
  { 필요공격수: 100000000, 슬롯수: 40 },
  { 필요공격수: 1000000000, 슬롯수: 48 },
]

function 현재슬롯수(공격수: number) {
  let 슬롯 = 8
  for (const m of 슬롯마일스톤) if (공격수 >= m.필요공격수) 슬롯 = m.슬롯수
  return 슬롯
}

// 타격수 보상 마일스톤 (원본 맵 string 1064~1184, 1220~1255)
// 한국 단위: 만(1e4), 억(1e8), 조(1e12), 경(1e16), 해(1e20), 자(1e24)
type 타격보상 = { ExP: number; 조각: number; 크리스탈박스: 크리스탈박스등급[]; 은하조각: number; 크레딧: number; 상급조각: number }
const _b = (ExP: number, 조각: number, 크리스탈박스: 크리스탈박스등급[] = [], 은하조각 = 0, 크레딧 = 0, 상급조각 = 0): 타격보상 => ({ ExP, 조각, 크리스탈박스, 은하조각, 크레딧, 상급조각 })
const 타격마일스톤표: { 임계값: number; 라벨: string; 보상: 타격보상 }[] = [
  { 임계값: 15000,  라벨: '15000',  보상: _b(50, 0) },
  { 임계값: 300000, 라벨: '30만',   보상: _b(250, 0, [], 0, 2000000) },
  { 임계값: 1e6,    라벨: '100만',  보상: _b(1000, 0) },
  { 임계값: 1e7,    라벨: '1000만', 보상: _b(200, 1) },
  { 임계값: 5e7,    라벨: '5000만', 보상: _b(200, 3) },
  { 임계값: 2e8,    라벨: '2억',    보상: _b(1000, 10, ['노말']) },
  { 임계값: 5e8,    라벨: '5억',    보상: _b(2000, 30, ['노말','노말','노말']) },
  { 임계값: 4e9,    라벨: '40억',   보상: _b(5000, 100, ['레어']) },
  { 임계값: 8.6e9,  라벨: '86억',   보상: _b(10000, 200, ['레어','레어']) },
  { 임계값: 2.14e10,라벨: '214억',  보상: _b(30000, 500, ['레어','레어','레어','레어']) },
  { 임계값: 4.3e10, 라벨: '430억',  보상: _b(50000, 800, ['레어','레어','레어','레어','레어','레어']) },
  { 임계값: 1e11,   라벨: '1000억', 보상: _b(100000, 1500, ['레어','레어','레어','레어','레어','레어','레어','레어','레어','레어']) },
  { 임계값: 1e12,   라벨: '1조',    보상: _b(200000, 0, ['유니크']) },
  { 임계값: 1e13,   라벨: '10조',   보상: _b(300000, 0, ['유니크','유니크','유니크','유니크','유니크','유니크','유니크']) },
  { 임계값: 1e14,   라벨: '100조',  보상: _b(400000, 0, [], 0, 0, 1) },
  { 임계값: 1e15,   라벨: '1000조', 보상: _b(500000, 0, [], 0, 0, 3) },
  { 임계값: 1e16,   라벨: '1경',    보상: _b(1000000, 0, ['퀘이사']) },
  { 임계값: 5e16,   라벨: '5경',    보상: _b(1000000, 0, ['퀘이사','퀘이사','퀘이사']) },
  { 임계값: 1e17,   라벨: '10경',   보상: _b(1000000, 0, ['퀘이사','퀘이사','퀘이사','퀘이사','퀘이사','퀘이사','퀘이사','퀘이사','퀘이사','퀘이사']) },
  { 임계값: 5e17,   라벨: '50경',   보상: _b(1000000, 0, [], 0, 0, 100) },
  { 임계값: 1e18,   라벨: '100경',  보상: _b(1000000, 0, [], 1) },
  { 임계값: 5e18,   라벨: '500경',  보상: _b(1000000, 0, [], 3) },
  { 임계값: 1e19,   라벨: '1000경', 보상: _b(1000000, 0, [], 5) },
  { 임계값: 5.55e19,라벨: '5550경', 보상: _b(2000000, 0) },
  { 임계값: 1.15e20,라벨: '1해1500경', 보상: _b(3000000, 0) },
  { 임계값: 3e20,   라벨: '3해',    보상: _b(4000000, 0) },
  { 임계값: 5e20,   라벨: '5해',    보상: _b(5000000, 0) },
  { 임계값: 1e21,   라벨: '10해',   보상: _b(6000000, 0) },
  { 임계값: 2.5e21, 라벨: '25해',   보상: _b(7000000, 0) },
  { 임계값: 6e21,   라벨: '60해',   보상: _b(8000000, 0) },
  { 임계값: 1.5e22, 라벨: '150해',  보상: _b(9000000, 0) },
  { 임계값: 4.5e22, 라벨: '450해',  보상: _b(10000000, 0) },
  { 임계값: 9e22,   라벨: '900해',  보상: _b(11000000, 0) },
  { 임계값: 1.5e23, 라벨: '1500해', 보상: _b(12000000, 0) },
  { 임계값: 3.2e23, 라벨: '3200해', 보상: _b(13000000, 0) },
  { 임계값: 5e23,   라벨: '5000해', 보상: _b(14000000, 0) },
  { 임계값: 7.5e23, 라벨: '7500해', 보상: _b(15000000, 0) },
  { 임계값: 1e24,   라벨: '1자',    보상: _b(16000000, 0) },
]

// ============================================
// 상수
// ============================================

// 웹은 데스크탑 큰 화면 대응, 모바일은 헤더 공간 확보
const _isWeb = Platform.OS === 'web'
// 웹: 필드_W를 고정(460)해 로드 타이밍에 따른 폭 흔들림 제거. 실제 뷰포트엔 화면맞춤배율(zoom)로 맞춤.
// 네이티브: 화면폭에 맞춰 축소(작은 폰 대응).
const 필드_W = _isWeb ? 460 : Math.min(380, 화면W - 16)
const 필드_H = _isWeb
  ? Math.max(380, Math.min(560, 화면H - 220))
  : Math.max(280, Math.min(440, 화면H - 400))
const 마린_크기 = 34
const 적_크기 = 42      // 일반 적 (보스/몹용 별도)
const 몹_크기 = 90       // 사냥터 몹 (크게)
const 보스_크기 = 110    // 보스 (가장 큼)
const 공격사거리 = 180
const 기본이동속도 = 280  // 사용자 요청 — 전반적 속도 상승 (160→280)

// 베이스 화면: 4 꼭지점 배치 (중앙 = 마린 spawn)
const ZONE_W = Math.min(105, (필드_W - 30) / 2)
const ZONE_H = 70
const ZONE_강화 = { x: 10, y: 10, w: ZONE_W, h: ZONE_H, label: '🔨 강화소', color: '#f5a623' }
const ZONE_보스존입구 = { x: 필드_W - ZONE_W - 10, y: 10, w: ZONE_W, h: ZONE_H, label: '⚔️ 보스존', color: '#e94560' }
const ZONE_사냥터입구 = { x: 10, y: 필드_H - ZONE_H - 10, w: ZONE_W, h: ZONE_H, label: '🐺 사냥터', color: '#9b59b6' }
const ZONE_판매소 = { x: 필드_W - ZONE_W - 10, y: 필드_H - ZONE_H - 10, w: ZONE_W, h: ZONE_H, label: '🛒 판매소', color: '#f1c40f' }
// 서브 화면 베이스 복귀 (아래쪽)
const ZONE_베이스입구 = { x: 필드_W / 2 - 50, y: 필드_H - 60, w: 100, h: 50, label: '🏠 베이스', color: '#7ed957' }

const 강화쿨다운 = 300  // 0.3초마다 자동 강화 시도 (800→300, 사용자 요청)

// ============================================
// 타입
// ============================================

type Pos = { x: number; y: number }
type 유닛상태 = 'idle' | 'move' | 'attack-move' | 'hold' | 'attacking'
type 화면 = 'base' | 'hunting1' | 'hunting2' | 'hunting3' | 'boss'
type 유닛위치 = 'base' | 'hunting1' | 'hunting2' | 'hunting3' | 'boss'

// 마린 강도 → 사냥터 화면 매핑
function 사냥화면(lv: number): 'hunting1' | 'hunting2' | 'hunting3' {
  if (lv <= 25) return 'hunting1'
  if (lv <= 40) return 'hunting2'
  return 'hunting3'
}
// 화면이 사냥터인지
function is사냥터(s: 화면): s is 'hunting1' | 'hunting2' | 'hunting3' {
  return s === 'hunting1' || s === 'hunting2' || s === 'hunting3'
}

type 마린 = {
  id: number
  lv: number
  pos: Pos
  dest: Pos | null
  state: 유닛상태
  타겟적id: number | null
  마지막공격시간: number
  공격플래시Until: number
  마지막강화시간: number
  location: 유닛위치
}

type 몹 = {
  id: number
  pos: Pos
  hp: number
  maxHp: number
  flashUntil: number
  deadUntil: number  // > now면 죽은 상태
  티어: 1 | 2 | 3  // 사냥터 1/2/3 (1=1~25강, 2=26~40, 3=41~60 DPS측정기)
}

// 마린 강도 → 사냥터 티어
function 사냥터티어(lv: number): 1 | 2 | 3 {
  if (lv <= 25) return 1
  if (lv <= 40) return 2
  return 3
}

type 적 = {
  id: number
  pos: Pos
  hp: number
  maxHp: number
  flashUntil: number
}

type 구역 = { x: number; y: number; w: number; h: number; label: string; color: string }

// 보주 (24종 신규 — 응무조로 구입). 효과는 추후 사용자 제공, 일단 이름/비용만
type 보주타입 =
  | '하급' | '중급' | '상급' | '특급' | '고급'
  | '경험' | '절약' | '힘' | '지위' | '권력'
  | '세공' | '신위' | '절명' | '풍요' | '폭식'
  | '영겁' | '명운' | '성운' | '천운' | '무위'
  | '풍성' | '부유' | '무엄' | '공명'
type 보주목록 = Record<보주타입, number>
const 보주종류목록: 보주타입[] = [
  '하급', '중급', '상급', '특급', '고급',
  '경험', '절약', '힘', '지위', '권력',
  '세공', '신위', '절명', '풍요', '폭식',
  '영겁', '명운', '성운', '천운', '무위',
  '풍성', '부유', '무엄', '공명',
]
const 초기보주: 보주목록 = {
  하급: 0, 중급: 0, 상급: 0, 특급: 0, 고급: 0,
  경험: 0, 절약: 0, 힘: 0, 지위: 0, 권력: 0,
  세공: 0, 신위: 0, 절명: 0, 풍요: 0, 폭식: 0,
  영겁: 0, 명운: 0, 성운: 0, 천운: 0, 무위: 0,
  풍성: 0, 부유: 0, 무엄: 0, 공명: 0,
}
// 1개 구입 비용 (응무조). xlsx 기반
const 보주구입비용: Record<보주타입, number> = {
  하급: 2, 중급: 4, 상급: 6, 특급: 8, 고급: 11,
  경험: 4, 절약: 12, 힘: 2, 지위: 40, 권력: 4,
  세공: 1e5, 신위: 1e8, 절명: 1, 풍요: 1e8, 폭식: 1e7,
  영겁: 1e6, 명운: 1e7, 성운: 1e8, 천운: 1e9, 무위: 1e16,
  풍성: 1e19, 부유: 1e22, 무엄: 1e22, 공명: 1e20,
}
// Max 보유 — xlsx 기반
const 보주max: Record<보주타입, number> = {
  하급: 100, 중급: 100, 상급: 100, 특급: 500, 고급: 1000,
  경험: 1000, 절약: 1500, 힘: 500, 지위: 500, 권력: 500,
  세공: 50, 신위: 1000, 절명: 500, 풍요: 1000, 폭식: 1000,
  영겁: 1000, 명운: 2000, 성운: 2000, 천운: 2000, 무위: 100,
  풍성: 1000, 부유: 100, 무엄: 100, 공명: 100,
}
// 보주 효과 설명 — xlsx 기반
const 보주효과설명: Record<보주타입, string> = {
  하급: '54강 확률 +0.1%/개',
  중급: '55강 확률 +0.1%/개',
  상급: '56강 융합확률 +0.05%/개',
  특급: '57강 확률 +0.02%/개',
  고급: '58강 확률 +0.01%/개',
  경험: '초월 경험치 획득량 +100%/개',
  절약: '56강 강화 필요 마리수 -10/개',
  힘: '57강 공격력 +10%/개',
  지위: '58강 공격력 +10%/개',
  권력: '59강 공격력 +10%/개',
  세공: '광산 크레딧 +10%/개',
  신위: '55~59강 기본공격력 +100%/개',
  절명: '55~59강 크리 +0.1%/개',
  풍요: '광산 크레딧 +1배/개 (×)',
  폭식: '돈수급량 +10%/개',
  영겁: '54,55강 확률 +0.03%/개',
  명운: '56강 융합확률 +0.02%/개',
  성운: '57강 확률 +0.02%/개',
  천운: '58강 확률 +0.02%/개',
  무위: '59강 +0.001%/개, 채광력 +0.1배/개',
  풍성: '광산 크레딧 +1배/개 (×)',
  부유: '광산 크레딧 +20%/개',
  무엄: '59강 확률 +0.1%/개',
  공명: '레벨업 필요경험치 ×0.9/개 (복리)',
}

// 명칭 크리스탈 (누적 수집형, 패시브 보너스)
type 명칭크리스탈목록 = {
  // 노말 (cost 50 크리스탈조각)
  방어: number; 행운: number; 경험: number; 무력: number
  절약: number; 총명: number; 보호: number; 각성: number
  // 레어 (cost 100)
  홍색: number; 주황: number; 노랑: number; 초록: number
  파랑: number; 남색: number; 보라: number; 하늘색: number; 무색명칭: number
  // 유니크 (cost 500)
  흑색: number; 백색명칭: number
  // 갤럭시 (cost 2000)
  우주: number
  // 퀘이사 (cost 200)
  길운Q: number; 무구Q: number; 집중Q: number; 절제Q: number
  탐욕Q: number; 증식Q: number; 미래Q: number; 돌파Q: number
  // 오리진 (cost 1000)
  창조O: number; 파멸O: number
}
const 초기명칭크리스탈: 명칭크리스탈목록 = {
  방어: 0, 행운: 0, 경험: 0, 무력: 0, 절약: 0, 총명: 0, 보호: 0, 각성: 0,
  홍색: 0, 주황: 0, 노랑: 0, 초록: 0, 파랑: 0, 남색: 0, 보라: 0, 하늘색: 0, 무색명칭: 0,
  흑색: 0, 백색명칭: 0, 우주: 0,
  길운Q: 0, 무구Q: 0, 집중Q: 0, 절제Q: 0, 탐욕Q: 0, 증식Q: 0, 미래Q: 0, 돌파Q: 0,
  창조O: 0, 파멸O: 0,
}

// 보석 시스템 (16종, 무색조각으로 구입)
// 원본 맵 기반: 강화보석 5종(하급~고급), 효과보석 7종(재물/경험/보호/궁극/수호/초월/인내), 신규 4종(강타/자동화/채광/섬세)
type 보석타입 = '하급' | '중급' | '상급' | '특급' | '고급' | '재물' | '경험보석' | '보호보석' | '궁극' | '수호' | '초월보석' | '인내' | '강타' | '자동화' | '채광' | '촉진'
type 보석목록 = { [K in 보석타입]: number }
const 초기보석: 보석목록 = { 하급: 0, 중급: 0, 상급: 0, 특급: 0, 고급: 0, 재물: 0, 경험보석: 0, 보호보석: 0, 궁극: 0, 수호: 0, 초월보석: 0, 인내: 0, 강타: 0, 자동화: 0, 채광: 0, 촉진: 0 }

const 보석max: Record<보석타입, number> = {
  하급: 100, 중급: 100, 상급: 100, 특급: 100, 고급: 1000,
  재물: 200, 경험보석: 1000, 보호보석: 200, 궁극: 100, 수호: 1000, 초월보석: 350, 인내: 1000,
  강타: 8, 자동화: 100, 채광: 100, 촉진: 100,
}
// 동적 비용: DPS계산기(뉴비용) old1 시트 수식 그대로. n = 현재 보유 → 다음 1개 구입 비용 (무색조각)
function 보석현재비용(종류: 보석타입, 보유: number): number {
  const n = 보유
  switch (종류) {
    case '하급': return n * n + n + 1
    case '중급': return 4 * n * n + 4 * n + 1
    case '상급': return 7 * n * n + 7 * n + 1
    case '특급': return 10 * n * n + 10 * n + 1
    case '고급': return 12 * n * n + 12 * n + 1
    case '재물': return 6 * n * n * n + 6 * n * n + 100  // 비용 대폭 상향(강함 보정)
    case '경험보석': return 2 * n + 2
    case '보호보석': return 3 * n * n * n + 6 * n * n + 3 * n + 100
    case '궁극': return 33 * n * n + 33 * n + 100
    case '채광': return 162 * n * n + 162 * n + 10000
    case '수호': { const k = n + 1; return 12 * k * k * k - 12 * k * k + 5000000 }
    case '초월보석': { const k = n + 1; return 50 * k * k * k - 50 * k * k + 4000000 }
    case '인내': return 1000000000000
    case '강타': return Math.floor(10000000000 * Math.pow(1.53629342670681, n) + 0.5)
    case '자동화': return Math.floor(100000000 * Math.pow(1.84567943776957, n) + 0.5)
    // 촉진(계산기 촉진1 식): n<30 ? 2e8+1e8*(n+1) : 4e10+(n-30)*3.6e10
    case '촉진': return n < 30 ? 200000000 + 100000000 * (n + 1) : 40000000000 + (n - 30) * 36000000000
  }
}
// 크리스탈 레벨업 비용 (보유 N개 소모 → 레벨 +1)
// 등급별 max 레벨: 노말/레어=10, 유니크/퀘이사=20, 갤럭시/오리진=50
type 크리스탈등급 = '노말' | '레어' | '유니크' | '갤럭시' | '퀘이사' | '오리진'
const _레벨업표A = [1, 3, 5, 7, 10, 13, 17, 22, 30, 40]  // 1~10: 노말/레어/유니크/갤럭시/퀘이사 공통
const _레벨업표B = [1000, 4000, 10000, 20000, 33000, 50000, 100000, 300000, 500000, 1000000]  // 11~20: 우주/백색/흑색/오리진
const _레벨업표C = [250, 850, 2530, 5690, 10360, 21130, 46500, 100500, 235800, 500000]  // 11~20: 퀘이사
const _레벨업표D = [5, 15, 60, 250, 400, 750, 1250, 2500, 5000, 10000]  // 1~10: 오리진
const _레벨업표E = [7, 20, 60, 350, 650, 1200, 1800, 3000, 5000, 10000]  // 21~30: 우주
const _레벨업표F = [15000, 24500, 45000, 90000, 180000, 350000, 700000, 1500000, 3000000, 6000000,
                  12500000, 15000000, 30000000, 60000000, 1.2e8, 2.5e8, 5e8, 1e9, 2e9, 4e9]  // 31~50: 우주/오리진
const 크리스탈등급max: Record<크리스탈등급, number> = { 노말: 10, 레어: 10, 유니크: 20, 갤럭시: 50, 퀘이사: 20, 오리진: 50 }
// 크리스탈 제작 비용. 퀘이사는 상급크리스탈조각 사용, 그 외는 일반 크리스탈조각
const 크리스탈제작비용: Partial<Record<크리스탈등급, number>> = {
  노말: 500,
  레어: 10000,
  유니크: 1000000,
  갤럭시: 30000000,
  퀘이사: 10,  // 상급조각 10개
}
function 크리스탈레벨업비용(등급: 크리스탈등급, 현재lv: number): number {
  // 현재lv -> lv+1 비용 (현재lv 1~max-1 유효)
  const idx = 현재lv - 1
  if (등급 === '오리진') {
    if (현재lv <= 10) return _레벨업표D[idx]
    if (현재lv <= 20) return _레벨업표B[idx - 10]
    if (현재lv <= 30) return _레벨업표E[idx - 20]
    if (현재lv <= 50) return _레벨업표F[idx - 30]
    return Infinity
  }
  if (현재lv <= 10) return _레벨업표A[idx]
  if (등급 === '퀘이사' && 현재lv <= 20) return _레벨업표C[idx - 10]
  if ((등급 === '유니크' || 등급 === '갤럭시') && 현재lv <= 20) return _레벨업표B[idx - 10]
  if (등급 === '갤럭시' && 현재lv <= 30) return _레벨업표E[idx - 20]
  if (등급 === '갤럭시' && 현재lv <= 50) return _레벨업표F[idx - 30]
  return Infinity
}
// 크리스탈 key → 등급 매핑
const 크리스탈등급맵: Record<keyof 명칭크리스탈목록, 크리스탈등급> = {
  방어: '노말', 행운: '노말', 경험: '노말', 무력: '노말', 절약: '노말', 총명: '노말', 보호: '노말', 각성: '노말',
  홍색: '레어', 주황: '레어', 노랑: '레어', 초록: '레어', 파랑: '레어', 남색: '레어', 보라: '레어', 하늘색: '레어', 무색명칭: '레어',
  흑색: '유니크', 백색명칭: '유니크',
  우주: '갤럭시',
  길운Q: '퀘이사', 무구Q: '퀘이사', 집중Q: '퀘이사', 절제Q: '퀘이사', 탐욕Q: '퀘이사', 증식Q: '퀘이사', 미래Q: '퀘이사', 돌파Q: '퀘이사',
  창조O: '오리진', 파멸O: '오리진',
}

// 명칭 크리스탈 보유 cap (레벨업 재료 — 무제한. 레벨 max는 크리스탈등급max로 별도 제어)
const _무한대 = Number.MAX_SAFE_INTEGER
const 명칭크리스탈max: Partial<Record<keyof 명칭크리스탈목록, number>> = {
  방어: _무한대, 행운: _무한대, 경험: _무한대, 무력: _무한대, 절약: _무한대, 총명: _무한대, 보호: _무한대, 각성: _무한대,
  홍색: _무한대, 주황: _무한대, 노랑: _무한대, 초록: _무한대, 파랑: _무한대, 남색: _무한대, 보라: _무한대, 하늘색: _무한대, 무색명칭: _무한대,
  흑색: _무한대, 백색명칭: _무한대,
  우주: _무한대,
  길운Q: _무한대, 무구Q: _무한대, 집중Q: _무한대, 절제Q: _무한대, 탐욕Q: _무한대, 증식Q: _무한대, 미래Q: _무한대, 돌파Q: _무한대,
  창조O: _무한대, 파멸O: _무한대,
}

function 보석보너스합산(b: 보석목록) {
  // 강화확률 추가 (decimal). 궁극은 1~47강 모두에 적용 → 외부보너스로 이전
  const add44 = b.하급 * 0.001 + b.초월보석 * 0.0001
  const add45 = b.중급 * 0.001 + b.초월보석 * 0.0001
  const add46 = b.상급 * 0.001 + b.초월보석 * 0.0001
  const add47 = b.특급 * 0.001 + b.초월보석 * 0.0001
  const add48 = b.고급 * 0.0005 + b.초월보석 * 0.0001
  const 궁극보너스 = b.궁극 * 0.0001  // 1~47강 모두에 +0.01%/개
  // 파괴방지 (points, 10 points = 1%)
  const 파괴방지 = b.보호보석 * 1 + b.수호 * 0.1
  // 자원/경험/초월 배수 — 복리 (1.1^N)
  // 재물: 보석당 자원 복리 배율. 1.1(10%)→1.05(5%)로 하향. 더 약하게 하려면 더 낮추기.
  const 재물배율 = 1.025
  const 자원배수추가 = (Math.pow(재물배율, b.재물) * Math.pow(1.05, b.채광)) - 1
  const 경험배수 = Math.pow(1.1, b.경험보석)
  const 초월확률추가 = b.인내 * 0.00001
  const 보스수보너스 = b.강타
  // 자동화 효율 (복리, 5%/개) — 캡 ×2 (50개 도달 시)
  const 자동속도배수 = Math.min(2, Math.pow(1.05, b.자동화))
  return { add44, add45, add46, add47, add48, 궁극보너스, 파괴방지, 자원배수추가, 경험배수, 초월확률추가, 보스수보너스, 자동속도배수 }
}

// 고유유닛 업그레이드 스텟
type 고유유닛스텟 = {
  공격력: number; 공속: number; 경험치: number; 추가1강: number; 특수강화: number
  위치: 1 | 2        // 사1 또는 사2 (사3은 진입 X). 26강+ 마린 존재시 자동 사2
  파괴방지: number
  단수: number       // 1+. n→n+1 비용=각성의보석 n개. 단수효과 = 1.1^(n-1) 곱
}
const 초기고유유닛: 고유유닛스텟 = { 공격력: 0, 공속: 0, 경험치: 0, 추가1강: 0, 특수강화: 0, 위치: 1, 파괴방지: 0, 단수: 1 }

function 고유유닛공격력(스텟: 고유유닛스텟) { return 500 + 스텟.공격력 * 500 }
function 고유유닛공속(스텟: 고유유닛스텟): number {
  const tbl = [1.0, 1.5, 2.0, 2.5, 3.0, 3.5, 4.0]
  return tbl[Math.min(스텟.공속, 6)]
}
function 고유유닛DPS(스텟: 고유유닛스텟) { return 고유유닛공격력(스텟) * 고유유닛공속(스텟) }

// ============================================
// 유틸
// ============================================

function 거리(a: Pos, b: Pos) {
  return Math.hypot(a.x - b.x, a.y - b.y)
}

function 이동좌표(현재: Pos, 목적: Pos, 거리량: number): Pos {
  const d = 거리(현재, 목적)
  if (d <= 거리량) return 목적
  const t = 거리량 / d
  return { x: 현재.x + (목적.x - 현재.x) * t, y: 현재.y + (목적.y - 현재.y) * t }
}

function 점이사각형안에(점: Pos, x1: number, y1: number, x2: number, y2: number) {
  return 점.x >= x1 && 점.x <= x2 && 점.y >= y1 && 점.y <= y2
}

function 점이구역안에(점: Pos, z: 구역) {
  return 점.x >= z.x && 점.x <= z.x + z.w && 점.y >= z.y && 점.y <= z.y + z.h
}

let 다음마린ID = 0
function 새마린ID() { return 다음마린ID++ }

function 새마린(lv: number, pos: Pos, location: 유닛위치 = 'base'): 마린 {
  return {
    id: 새마린ID(),
    lv,
    pos,
    dest: null,
    state: 'idle',
    타겟적id: null,
    마지막공격시간: 0,
    공격플래시Until: 0,
    마지막강화시간: 0,
    location,
  }
}

function 베이스시작위치(_i: number): Pos {
  // 생산/강화 완료 마린이 모두 중앙 한 점으로 모이도록 (i 무시)
  return clampPosStatic({ x: 필드_W / 2, y: 필드_H / 2 })
}

// static clamp (필드 경계 내)
function clampPosStatic(p: Pos): Pos {
  const r = 마린_크기 / 2 + 2
  return {
    x: Math.max(r, Math.min(필드_W - r, p.x)),
    y: Math.max(r, Math.min(필드_H - r, p.y)),
  }
}

function 사냥터시작위치(i: number): Pos {
  const cols = 12
  const row = Math.floor(i / cols)
  // 적/보스(위)와 베이스 비콘(아래) 사이 중간 영역
  const yBase = 필드_H - 90
  return { x: 25 + (i % cols) * 30, y: yBase - row * 22 }
}

function 초기마린들(): 마린[] {
  다음마린ID = 0
  return Array.from({ length: 8 }, (_, i) => 새마린(1, 베이스시작위치(i)))
}

function 보스HP(보스번호: number): number {
  return Math.round(300 * Math.pow(2.5, 보스번호 - 1))
}

function 보스DPS게이트(보스번호: number): number {
  // requiredDps 5배 상승: HP/60 * 5 = HP/12
  return Math.round(보스HP(보스번호) / 12)
}


// 마린 색상 (lv tier별)
function 마린색(lv: number): string {
  if (lv <= 10) return '#7ed957'
  if (lv <= 20) return '#f5a623'
  if (lv <= 30) return '#4a90e2'
  if (lv <= 40) return '#9b59b6'
  if (lv <= 50) return '#f1c40f'
  return '#e94560'
}


// 유닛 sprite sheet 좌표 (1~60 lv → 시트 row/col)
// 신규 lv = (타입-1)*6 + 컬러. 타입 1~10, 컬러 1~6
// 시트: row = 컬러(1~6), col = 타입(1~10)
function 유닛스프라이트(lv: number): { row: number; col: number } {
  const type = Math.floor((lv - 1) / 6) + 1  // 1~10 (시트 column)
  const color = ((lv - 1) % 6) + 1            // 1~6 (시트 row)
  return { row: color - 1, col: type - 1 }
}

// 보주 효과표 — 강화시도 단계별 보너스 + 기타. 타입은 강화시도 stage 보너스 적용용
const 보주효과표: Record<보주타입, { 타입: string; 값: number; 설명: string }> = {
  하급: { 타입: '강화54', 값: 0.001, 설명: 보주효과설명.하급 },
  중급: { 타입: '강화55', 값: 0.001, 설명: 보주효과설명.중급 },
  상급: { 타입: '강화56', 값: 0.0005, 설명: 보주효과설명.상급 },
  특급: { 타입: '강화57', 값: 0.0002, 설명: 보주효과설명.특급 },
  고급: { 타입: '강화58', 값: 0.0001, 설명: 보주효과설명.고급 },
  경험: { 타입: '초월경험', 값: 1.0, 설명: 보주효과설명.경험 },
  절약: { 타입: '미정', 값: 0, 설명: 보주효과설명.절약 },
  힘: { 타입: '공격57', 값: 0.10, 설명: 보주효과설명.힘 },
  지위: { 타입: '공격58', 값: 0.10, 설명: 보주효과설명.지위 },
  권력: { 타입: '공격59', 값: 0.10, 설명: 보주효과설명.권력 },
  세공: { 타입: '미정', 값: 0, 설명: 보주효과설명.세공 },
  신위: { 타입: '기본공격55_59', 값: 1.0, 설명: 보주효과설명.신위 },
  절명: { 타입: '크리55_59', 값: 0.001, 설명: 보주효과설명.절명 },
  풍요: { 타입: '미정', 값: 0, 설명: 보주효과설명.풍요 },
  폭식: { 타입: '자원', 값: 0.10, 설명: 보주효과설명.폭식 },
  영겁: { 타입: '강화54_55', 값: 0.0003, 설명: 보주효과설명.영겁 },
  명운: { 타입: '강화56', 값: 0.0002, 설명: 보주효과설명.명운 },
  성운: { 타입: '강화57', 값: 0.0002, 설명: 보주효과설명.성운 },
  천운: { 타입: '강화58', 값: 0.0002, 설명: 보주효과설명.천운 },
  무위: { 타입: '강화59', 값: 0.00001, 설명: 보주효과설명.무위 },
  풍성: { 타입: '미정', 값: 0, 설명: 보주효과설명.풍성 },
  부유: { 타입: '미정', 값: 0, 설명: 보주효과설명.부유 },
  무엄: { 타입: '강화59', 값: 0.001, 설명: 보주효과설명.무엄 },
  공명: { 타입: '레벨XP', 값: 0.001, 설명: 보주효과설명.공명 },
}

// 보주 효과 합산: 각 보주 count × per-unit value
function 보주합산(b: 보주목록, 타입: string): number {
  let total = 0
  for (const k of 보주종류목록) {
    const eff = 보주효과표[k]
    if (eff.타입 === 타입) total += b[k] * eff.값
  }
  return total
}

// 크리스탈 슬롯수 (초월레벨 기반)
// 원본 맵: 30만 캐릭레벨 → 1 슬롯 기본. ExLv 더 오르면 더 많은 슬롯
function 크리스탈슬롯수(초월lv: number): number {
  if (초월lv < 1) return 0
  if (초월lv < 5) return 1
  if (초월lv < 15) return 2
  if (초월lv < 50) return 3
  if (초월lv < 200) return 4
  if (초월lv < 1000) return 5
  return 6
}

// 명칭 크리스탈 패시브 보너스 (장착된 크리스탈만 적용, Lv 기준)
function 명칭크리스탈보너스(lvMap: Partial<Record<keyof 명칭크리스탈목록, number>>, eq: (keyof 명칭크리스탈목록)[]) {
  const eqSet = new Set(eq)
  const v = (k: keyof 명칭크리스탈목록): number => eqSet.has(k) ? (lvMap[k] ?? 0) : 0
  const 개별확률 = (v('행운') + v('홍색') + v('보라')) * 0.01 + v('백색명칭') * 0.02 + v('우주') * 0.05
  const 파괴방지 = (v('보호') + v('노랑') + v('남색')) * 2 + (v('흑색') + v('백색명칭')) * 3 + v('우주') * 5
  const 초월확률 = v('무색명칭') * 2 + v('흑색') * 5 + v('우주') * 10
  // 배수형은 복리 (×): 1.5^N, 2^N, 3^N 등
  const 무색배수배율 = Math.pow(1.5, v('총명') + v('주황') + v('하늘색')) * Math.pow(2, v('흑색')) * Math.pow(3, v('우주'))
  const 판매배수배율 = Math.pow(1.05, v('절약') + v('주황') + v('보라')) * Math.pow(1.10, v('흑색')) * Math.pow(2.00, v('우주'))
  return { 개별확률, 파괴방지, 초월확률, 무색배수배율, 판매배수배율 }
}

function 초기적들(보스번호: number = 1): 적[] {
  const hp = 보스HP(보스번호)
  return [{ id: 0, pos: { x: 필드_W / 2, y: 필드_H * 0.45 }, hp, maxHp: hp, flashUntil: 0 }]
}

function 초기몹들(): 몹[] {
  // 사냥터 1/2/3 (각 별도 화면 — 중앙에 배치)
  const y = 150
  return [
    { id: 0, pos: { x: 필드_W / 2, y }, hp: 99999, maxHp: 99999, flashUntil: 0, deadUntil: 0, 티어: 1 },
    { id: 1, pos: { x: 필드_W / 2, y }, hp: 99999, maxHp: 99999, flashUntil: 0, deadUntil: 0, 티어: 2 },
    { id: 2, pos: { x: 필드_W / 2, y }, hp: 99999, maxHp: 99999, flashUntil: 0, deadUntil: 0, 티어: 3 },
  ]
}

// ============================================
// 메인 앱
// ============================================

export default function App() {
  // 화면 맞춤: 게임 컬럼을 뷰포트 너비에 비율 유지하며 fit (좌우 짤림/기기별 차이 해소)
  // 컬럼 너비 = 필드_W + container 좌우 padding(4*2). 뷰포트보다 넓으면 축소만 함(확대 X).
  // 확대를 막아야 측정 오차(뷰포트W가 실제보다 크게 읽히는 경우)에도 가로 오버플로가 안 생김.
  const { width: 뷰포트W } = useWindowDimensions()
  // 웹: 마운트 직후(폭 안정화)와 실제 resize 시 1회 재렌더 → 아래 라이브 window.innerWidth로 배율 재계산.
  // (useWindowDimensions가 로드 초기/리사이즈에 부정확할 때의 보정)
  const [, set뷰포트보정] = useState(0)
  useEffect(() => {
    if (typeof window === 'undefined') return
    const 갱신 = () => set뷰포트보정(n => n + 1)
    const id = setTimeout(갱신, 120)
    window.addEventListener('resize', 갱신)
    return () => { clearTimeout(id); window.removeEventListener('resize', 갱신) }
  }, [])
  const 컬럼W = 필드_W + 8
  // 실제 뷰포트 폭을 신뢰성 있게 측정: 라이브 window.innerWidth 우선.
  // (useWindowDimensions 상태와 모듈상수 화면W 모두 로드 타이밍에 따라 틀린 값을 줄 수 있음 →
  //  필드_W가 실제 뷰포트보다 크게 잡히면 좌우가 짤리므로, 항상 현재 폭에 맞춰 축소되도록.)
  // 뷰포트W(useWindowDimensions)는 리사이즈 시 재렌더 트리거 + 폴백용.
  const _liveW = (typeof window !== 'undefined' && window.innerWidth > 100) ? window.innerWidth : 0
  const 안전뷰포트W = _liveW || (뷰포트W > 100 ? 뷰포트W : 화면W)
  const 화면맞춤배율 = Math.min(안전뷰포트W / 컬럼W, 1)
  const 배율Ref = useRef(화면맞춤배율); 배율Ref.current = 화면맞춤배율
  // 축소가 실제로 필요할 때만(좁은 화면) scale/zoom 적용. PC(배율≈1)에선 zoom 미적용 → 원래 레이아웃 그대로(레이아웃 깨짐 방지)
  const 화면맞춤스타일: any = 화면맞춤배율 >= 0.999
    ? null
    : Platform.OS === 'web'
      ? { zoom: 화면맞춤배율 }
      : { transform: [{ scale: 화면맞춤배율 }] }

  const [현재화면, set현재화면] = useState<화면>('base')
  const [마린들, set마린들] = useState<마린[]>(() => 초기마린들())
  const [적들, set적들] = useState<적[]>(() => 초기적들(1))
  const [몹들, set몹들] = useState<몹[]>(() => 초기몹들())
  const [mineral, setMineral] = useState(100)
  const [총공격수, set총공격수] = useState(0)
  const [보스처치수, set보스처치수] = useState(0)
  const [최고DPS, set최고DPS] = useState(0)
  // 통화
  const [무색조각, set무색조각] = useState(10000)  // 시드: 초반 보석 찍기용 1만
  const [응무조, set응무조] = useState(0)
  const [크리스탈조각, set크리스탈조각] = useState(0)
  const [상급크리스탈조각, set상급크리스탈조각] = useState(0)
  const [강화최초달성, set강화최초달성] = useState<Set<number>>(new Set())
  // 보주 (원본 맵: 응무조로 구입하여 영구 강화)
  const [보주, set보주] = useState<보주목록>(() => ({ ...초기보주 }))
  // 캐릭터 레벨
  const [캐릭레벨, set캐릭레벨] = useState(1)
  const [경험치, set경험치] = useState(0)
  const [잔여포인트, set잔여포인트] = useState(0)
  const [일반스텟, set일반스텟] = useState<강화스텟>({
    돈수급량: 0, 유닛공업: 0,
    가산1강: 0, 가산2강: 0, 가산3강: 0,
    특수강화: 0, 가산1강2: 0, 가산2강2: 0, 가산3강2: 0,
    특수강화2: 0, 특수파괴방지: 0, 특수파괴방지2: 0,
    가산44강: 0, 가산45강: 0, 가산46강: 0, 가산47강: 0, 가산48강: 0,
    촉진: 0,
  })
  // 초월 스텟 (원본 맵 기반 - Ex스탯 시스템)
  // 추가초월확률: 50→51강 확률 +0.001%/pt
  // 강화51_53: 51~53강 강화확률 +2%p/pt
  // 업그51_56: 51~56강 공격력 업그레이드 +9/pt
  // 공격57_59: 57~59강 공격력 +8/pt
  // 융합56: 56강 융합 확률 +1%p/pt
  // 보스데미지: Extra Boss 데미지 +10%/pt
  const [초월스텟, set초월스텟] = useState<초월스텟타입>({ ...초기초월스텟 })
  const [스텟탭, set스텟탭] = useState<'일반' | '초월' | '보주' | '보석'>('일반')
  const [명칭크리스탈, set명칭크리스탈] = useState<명칭크리스탈목록>(() => ({ ...초기명칭크리스탈 }))
  // 크리스탈 레벨 (기본 0 = 미보유, 1+ = 레벨)
  const [명칭크리스탈Lv, set명칭크리스탈Lv] = useState<Partial<Record<keyof 명칭크리스탈목록, number>>>({})
  const [장착크리스탈, set장착크리스탈] = useState<(keyof 명칭크리스탈목록)[]>([])
  const [명칭크리스탈패널열림, set명칭크리스탈패널열림] = useState(false)
  const [명칭크리스탈탭, set명칭크리스탈탭] = useState<'노말' | '레어' | '유니크' | '갤럭시' | '퀘이사' | '오리진'>('노말')
  // 크레딧 (보스 처치 보상, 고유유닛 강화에 사용)
  const [크레딧, set크레딧] = useState(0)
  // 보석
  const [보석, set보석] = useState<보석목록>(() => ({ ...초기보석 }))
  const [보석패널열림, set보석패널열림] = useState(false)
  // 고유유닛
  const [고유유닛, set고유유닛] = useState<고유유닛스텟>(() => ({ ...초기고유유닛 }))
  const [고유유닛패널열림, set고유유닛패널열림] = useState(false)
  const [고유유닛pos, set고유유닛pos] = useState<Pos>({ x: 필드_W * 0.4, y: 200 })
  const [고유유닛dest, set고유유닛dest] = useState<Pos | null>(null)
  const [고유유닛선택, set고유유닛선택] = useState(false)
  // 초월레벨
  const [초월레벨, set초월레벨] = useState(0)
  const [초월잔여포인트, set초월잔여포인트] = useState(0)
  const [초월경험치, set초월경험치] = useState(0)  // 30만 레벨 도달 후 누적 → 초월레벨업
  // 환생 시스템
  // 트리거: 50강 마린 1마리 이상 보유
  // 보상: ExPoint = (이번 환생까지 누적 생산한 50강+ 유닛 수) × EXP_마리당
  const [환생레벨, set환생레벨] = useState(0)
  const [누적환생수, set누적환생수] = useState(0)
  // 이번 환생 동안 50강 이상에 도달한 유닛 누적 수 (환생시 리셋) — 환생 ExP 보상 산정
  const [누적50강생산, set누적50강생산] = useState(0)
  const [환생패널열림, set환생패널열림] = useState(false)
  const [재화패널열림, set재화패널열림] = useState(false)
  const [도움말패널열림, set도움말패널열림] = useState(false)
  const [정보패널열림, set정보패널열림] = useState(false)
  const [정보단계, set정보단계] = useState(1)  // 정보 패널에서 보고 있는 강화 단계
  const [상점패널열림, set상점패널열림] = useState(false)
  // 시간제 쿠폰: 만료 시각(ms). now < 만료 면 활성. 1개 구입 = +1시간, 최대 60시간 누적.
  const [경험쿠폰만료, set경험쿠폰만료] = useState(0)  // 15~44강 판매 경험치 3배
  const [재화쿠폰만료, set재화쿠폰만료] = useState(0)  // 45~50강 판매 재화 2배
  const [자동구입배수, set자동구입배수] = useState<number>(1)
  const [내부계산모드, set내부계산모드] = useState(false)  // 렌더 OFF → 렉 해소
  // 신규 재화 (원본 맵 기반)
  // 각성의 보석: 보스/뽑기에서 드랍, 추가 Ex스탯 포인트 교환에 사용
  const [각성의보석, set각성의보석] = useState(0)
  // ExPoint: 51강+ 판매 시 획득, 초월 스탯 분배 시 사용 (초월잔여포인트와 별개)
  const [ExPoint, setExPoint] = useState(0)
  // 은하의 조각: 60강 판매 25% 드랍 (우주 크리스탈 업그레이드용)
  const [은하조각, set은하조각] = useState(0)
  // 자각의 보주: 60강 판매 25% 드랍 (진각성 시스템용 - 미구현)
  const [자각보주, set자각보주] = useState(0)
  // 타격수 마일스톤 획득 인덱스 (0부터 순서대로, 다음 미획득 인덱스)
  const [타격수획득idx, set타격수획득idx] = useState(0)
  // Extra LV 보상 받은 횟수 (보스처치수 / 6 또는 11 가준)
  const [extraVI받음, setExtraVI받음] = useState(0)
  const [extraXI받음, setExtraXI받음] = useState(0)
  // 영구강화 (무색조각/응무조 사용)
  const [업그레이드, set업그레이드] = useState({ 공격력: 0, 자원: 0, 강화확률: 0, 이속: 0, 공속: 0 })
  // 패널
  const [보주패널열림, set보주패널열림] = useState(false)
  const [강화패널열림, set강화패널열림] = useState(false)
  // 통계
  const [누적강화성공, set누적강화성공] = useState(0)
  const [누적판매, set누적판매] = useState(0)
  const [최고마린lv, set최고마린lv] = useState(1)
  const [융합누적, set융합누적] = useState(0)  // 56강 융합: 흡수한 55강 마린 누적 수
  // 자동화
  const [자동강화ON, set자동강화ON] = useState(false)
  const [자동강화최대lv, set자동강화최대lv] = useState(1)
  const [자동판매ON, set자동판매ON] = useState(false)
  const [자동판매lv, set자동판매lv] = useState(50)
  const [자동구입강도, set자동구입강도] = useState(1)
  const [자동구입ON, set자동구입ON] = useState(false)
  // 자동응축 시스템 폐기됨 (무색→응무조 변환 제거)
  const [자동패널열림, set자동패널열림] = useState(false)
  const [선택ID, set선택ID] = useState<number[]>([])
  const [드래그박스, set드래그박스] = useState<{ x1: number; y1: number; x2: number; y2: number } | null>(null)
  const [생산패널열림, set생산패널열림] = useState(false)
  const [메시지, set메시지] = useState('')
  const [로드완료, set로드완료] = useState(false)

  // refs (게임 루프 stale closure 방지)
  const 마린들Ref = useRef(마린들); 마린들Ref.current = 마린들
  const 적들Ref = useRef(적들); 적들Ref.current = 적들
  const 몹들Ref = useRef(몹들); 몹들Ref.current = 몹들
  const mineralRef = useRef(mineral); mineralRef.current = mineral
  const 총공격수Ref = useRef(총공격수); 총공격수Ref.current = 총공격수
  const 누적50강생산Ref = useRef(누적50강생산); 누적50강생산Ref.current = 누적50강생산
  const 보스처치수Ref = useRef(보스처치수); 보스처치수Ref.current = 보스처치수
  const 보스킬쿨다운Ref = useRef(0)
  const 최고DPSRef = useRef(최고DPS); 최고DPSRef.current = 최고DPS
  const 보주Ref = useRef(보주); 보주Ref.current = 보주
  const 무색조각Ref = useRef(무색조각); 무색조각Ref.current = 무색조각
  const 내부계산모드Ref = useRef(내부계산모드); 내부계산모드Ref.current = 내부계산모드
  const 응무조Ref = useRef(응무조); 응무조Ref.current = 응무조
  const 크리스탈조각Ref = useRef(크리스탈조각); 크리스탈조각Ref.current = 크리스탈조각
  const 상급크리스탈조각Ref = useRef(상급크리스탈조각); 상급크리스탈조각Ref.current = 상급크리스탈조각
  const 강화최초달성Ref = useRef(강화최초달성); 강화최초달성Ref.current = 강화최초달성
  const 캐릭레벨Ref = useRef(캐릭레벨); 캐릭레벨Ref.current = 캐릭레벨
  const 경험치Ref = useRef(경험치); 경험치Ref.current = 경험치
  const 잔여포인트Ref = useRef(잔여포인트); 잔여포인트Ref.current = 잔여포인트
  const 일반스텟Ref = useRef(일반스텟); 일반스텟Ref.current = 일반스텟
  const 초월스텟Ref = useRef(초월스텟); 초월스텟Ref.current = 초월스텟
  const 명칭크리스탈Ref = useRef(명칭크리스탈); 명칭크리스탈Ref.current = 명칭크리스탈
  const 명칭크리스탈LvRef = useRef(명칭크리스탈Lv); 명칭크리스탈LvRef.current = 명칭크리스탈Lv
  const 장착크리스탈Ref = useRef(장착크리스탈); 장착크리스탈Ref.current = 장착크리스탈
  const 크레딧Ref = useRef(크레딧); 크레딧Ref.current = 크레딧
  const 보석Ref = useRef(보석); 보석Ref.current = 보석
  const 고유유닛Ref = useRef(고유유닛); 고유유닛Ref.current = 고유유닛
  const 고유유닛posRef = useRef(고유유닛pos); 고유유닛posRef.current = 고유유닛pos
  const 고유유닛destRef = useRef(고유유닛dest); 고유유닛destRef.current = 고유유닛dest
  const 고유유닛선택Ref = useRef(고유유닛선택); 고유유닛선택Ref.current = 고유유닛선택
  const 초월레벨Ref = useRef(초월레벨); 초월레벨Ref.current = 초월레벨
  const 초월잔여포인트Ref = useRef(초월잔여포인트); 초월잔여포인트Ref.current = 초월잔여포인트
  const 초월경험치Ref = useRef(초월경험치); 초월경험치Ref.current = 초월경험치
  const 융합누적Ref = useRef(융합누적); 융합누적Ref.current = 융합누적
  const 각성의보석Ref = useRef(각성의보석); 각성의보석Ref.current = 각성의보석
  const ExPointRef = useRef(ExPoint); ExPointRef.current = ExPoint
  const 경험쿠폰만료Ref = useRef(경험쿠폰만료); 경험쿠폰만료Ref.current = 경험쿠폰만료
  const 재화쿠폰만료Ref = useRef(재화쿠폰만료); 재화쿠폰만료Ref.current = 재화쿠폰만료
  const 은하조각Ref = useRef(은하조각); 은하조각Ref.current = 은하조각
  const 자각보주Ref = useRef(자각보주); 자각보주Ref.current = 자각보주
  const 타격수획득idxRef = useRef(타격수획득idx); 타격수획득idxRef.current = 타격수획득idx
  const extraVI받음Ref = useRef(extraVI받음); extraVI받음Ref.current = extraVI받음
  const extraXI받음Ref = useRef(extraXI받음); extraXI받음Ref.current = extraXI받음
  const 업그레이드Ref = useRef(업그레이드); 업그레이드Ref.current = 업그레이드
  const 자동강화ONRef = useRef(자동강화ON); 자동강화ONRef.current = 자동강화ON
  const 자동강화최대lvRef = useRef(자동강화최대lv); 자동강화최대lvRef.current = 자동강화최대lv
  const 자동판매ONRef = useRef(자동판매ON); 자동판매ONRef.current = 자동판매ON
  const 자동판매lvRef = useRef(자동판매lv); 자동판매lvRef.current = 자동판매lv
  const 자동구입ONRef = useRef(자동구입ON); 자동구입ONRef.current = 자동구입ON
  // 자동응축 Ref 제거됨
  type Dmg플로팅 = { id: number; x: number; y: number; dmg: number; crit: boolean; until: number }
  const [dmg플로팅들, setDmg플로팅들] = useState<Dmg플로팅[]>([])
  const dmgIdRef = useRef(0)
  const 자동구입강도Ref = useRef(자동구입강도); 자동구입강도Ref.current = 자동구입강도
  const 자동구입배수Ref = useRef(자동구입배수); 자동구입배수Ref.current = 자동구입배수
  const 자동구입타이머Ref = useRef(0)
  const 보석연속타이머Ref = useRef<ReturnType<typeof setInterval> | null>(null)
  const lastTapRef = useRef<{ id: number; time: number } | null>(null)
  const 선택IDRef = useRef(선택ID); 선택IDRef.current = 선택ID
  const 현재화면Ref = useRef(현재화면); 현재화면Ref.current = 현재화면
  const 생산패널열림Ref = useRef(생산패널열림); 생산패널열림Ref.current = 생산패널열림
  const 메시지타이머Ref = useRef(0)
  const dragStartRef = useRef<Pos | null>(null)
  const 터치이동중Ref = useRef(false)
  const fieldRef = useRef<any>(null)

  const 베이스마린들 = 마린들.filter(m => m.location === 'base')
  const 사냥터마린들 = 마린들.filter(m => is사냥터(m.location as 화면))
  const 사냥터1마린들 = 마린들.filter(m => m.location === 'hunting1')
  const 사냥터2마린들 = 마린들.filter(m => m.location === 'hunting2')
  const 사냥터3마린들 = 마린들.filter(m => m.location === 'hunting3')
  const 보스존마린들 = 마린들.filter(m => m.location === 'boss')

  // 촉진합 = 보석+일반+초월+단수. 자동구입 틱당 선택 가능한 최대 = 10 + 촉진합
  const 촉진합 = (보석.촉진 || 0) + (일반스텟.촉진 || 0) + (초월스텟.촉진 || 0) + Math.floor(((고유유닛.단수 || 1) - 1) / 10)
  const 최대틱수 = 10 + 촉진합
  const 화면마린들 = 현재화면 === 'hunting1' ? 사냥터1마린들
    : 현재화면 === 'hunting2' ? 사냥터2마린들
    : 현재화면 === 'hunting3' ? 사냥터3마린들
    : 현재화면 === 'boss' ? 보스존마린들 : 베이스마린들
  // 보주 효과 합산 (렌더 시점)
  const _보주공격r = 보주합산(보주, '공격')
  const _보주크리r = 보주합산(보주, '크리')
  const _보주공속r = 보주합산(보주, '공속')
  const _보주자원r = 보주합산(보주, '자원')
  const _보주배수r = 보주합산(보주, '배수')
  // 정보 패널: 현재 외부 강화 보너스 합(1~49강 공통). 게임틱 외부강화보너스와 동일 구성.
  const _명칭보너스r = 명칭크리스탈보너스(명칭크리스탈Lv, 장착크리스탈)
  const _보석b_r = 보석보너스합산(보석)
  const 현재외부강화보너스공통 = 보주합산(보주, '강화') + 업그레이드.강화확률 * 0.005 + _명칭보너스r.개별확률
  const 고유일반보너스r = (고유유닛.추가1강 * 0.0025 + 고유유닛.특수강화 * 0.005)  // 고유유닛 강화확률 보너스는 일반확률(+1)에만
  // 정보 패널: 강화 단계별 확률·판매 정보 (현재 스텟/보너스 반영, 강화시도() 로직 기준)
  const 강화단계정보 = (lv: number): { 타입: string; 확률: string[]; 판매: string; 구입: number; 공격력: number; 공속: string; dps: number; 판매획득: string; 내역: string[] } => {
    const 외부 = 현재외부강화보너스공통 + (lv <= 47 ? _보석b_r.궁극보너스 : 0)  // 궁극 보석은 47강까지만
    const 일반보 = 고유일반보너스r  // 일반확률(+1)에만
    const s = 일반스텟
    const ts: any = 초월스텟
    const 특수가산 = (s.특수강화 + s.특수강화2) * 0.001
    const pct = (p: number) => (Math.min(0.95, Math.max(0, p)) * 100).toFixed(2) + '%'
    let 타입 = '', 확률: string[] = []
    if (lv >= 60) { 타입 = 'MAX'; 확률 = ['더 이상 강화 불가'] }
    else if (lv >= 51) {
      let tb = 0
      if (lv === 51) tb = (ts.추가51강 || 0) * 0.001
      else if (lv === 52) tb = (ts.추가52강 || 0) * 0.001
      else if (lv === 53) tb = (ts.추가53강 || 0) * 0.001
      else if (lv === 54) tb = (ts.추가54강 || 0) * 0.0001 + (ts.추가54강2 || 0) * 0.0005
      else if (lv === 55) tb = (ts.추가55강 || 0) * 0.0001 + (ts.추가55강2 || 0) * 0.0002
      else if (lv === 56) tb = (ts.추가융합 || 0) * 0.001
      else if (lv === 57) tb = (ts.추가57강 || 0) * 0.001
      else if (lv === 58) tb = (ts.추가58강 || 0) * 0.001
      타입 = '초월 확률'; 확률 = [`성공 ${pct(외부 + tb)}`]
    }
    else if (lv === 50) { 타입 = '초월 시도 (50→51)'; 확률 = [`성공 ${pct(0.005 + (ts.추가초월확률 || 0) * 0.00001 + 외부)}`] }
    else if (lv === 49) { 타입 = '고정 확률'; 확률 = ['성공 0.50%  (고정 — 외부보너스 미적용)'] }
    else if (lv === 48) { 타입 = '개별 확률'; 확률 = [`성공 ${pct(s.가산48강 * 0.0005 + 외부)}  (기본 0%)`] }
    else if (lv >= 44) {
      const base = 강화기본확률(lv)
      const g = [s.가산44강, s.가산45강, s.가산46강, s.가산47강][lv - 44] * 0.001
      타입 = '개별 확률'; 확률 = [`성공 ${pct(base + g + 외부)}  (기본 ${(base * 100).toFixed(1)}%)`]
    }
    else if (lv >= 40) { 타입 = '특수 확률'; 확률 = [`성공 ${pct(특수가산 + 외부)}  (특수강화 스텟 기반)`] }
    else if (lv >= 38) {
      const base = 강화기본확률(lv)
      const g = (s.가산1강 + s.가산1강2 + s.가산2강 + s.가산2강2 + s.가산3강 + s.가산3강2) * 0.001
      타입 = '일반 확률 (+1)'; 확률 = [`+1 ${pct(base * 1.11 + g + 특수가산 + 외부 + 일반보)}`]
    }
    else {
      const base = 강화기본확률(lv)
      const p3 = base / 100 + (s.가산3강 + s.가산3강2) * 0.001 + 특수가산
      const p2 = base / 10 + (s.가산2강 + s.가산2강2) * 0.001 + 특수가산
      const p1 = base + (s.가산1강 + s.가산1강2) * 0.001 + 특수가산 + 외부 + 일반보
      타입 = '일반 확률 (+1/+2/+3)'; 확률 = [`+1 ${pct(p1)}`, `+2 ${pct(p2)}`, `+3 ${pct(p3)}`]
    }
    const sc = 판매크레딧비용(lv)
    const 판매 = lv === 51 ? '판매 불가' : (sc === 0 ? '무료' : `💰 ${숫자포맷(sc)}`)
    // 유닛 전투 수치 (현재 버프 반영)
    const 공당 = Math.round(공격력(lv, _초월r, _일반공업r) * _공격력배수r)
    const 공속초 = 공격속도(lv) * _공속배수r * 연타수(lv)
    const dps = Math.round(공당 * 공속초 * (1 + _크리r))
    // 판매 시 획득 (결정값만; 45강+는 확률 드랍 별도)
    let 판매획득 = ''
    if (lv === 51) 판매획득 = '판매 불가'
    else if (판매일반XP표[lv] != null) 판매획득 = `⭐ 경험치 ${숫자포맷(판매일반XP표[lv])}`
    else if (lv >= 45 && lv <= 50) 판매획득 = '🔷 무색조각 + 확률 드랍(크레딧·각성석 등)'
    else if (lv >= 52) 판매획득 = '🌀 초월XP·조각·박스 등 (확률 드랍)'
    else 판매획득 = '없음'
    // 강화확률 기여 내역 (어떤 항목이 이 단계 확률을 얼마나 올렸는지)
    const 내역: string[] = []
    const push = (라벨: string, v: number) => { if (v > 1e-9) 내역.push(`${라벨}  +${(v * 100).toFixed(3)}%`) }
    if ((lv < 49) || (lv >= 51 && lv <= 59)) {  // 49강(고정)·50강(초월시도)·60강 제외
      // 공통(외부) — 모든 강화확률에 적용
      push('⚔️ 보주(강화)', 보주합산(보주, '강화'))
      push('🔧 업그레이드(강화확률)', 업그레이드.강화확률 * 0.005)
      push('🏅 명칭크리스탈(개별)', _명칭보너스r.개별확률)
      if (lv <= 47) push('💎 보석(궁극)', _보석b_r.궁극보너스)
      // 단계 타입별 추가
      if (lv <= 37) { push('🦸 고유유닛(강화확률·일반전용)', 일반보); push('📊 스텟(가산1강)', (s.가산1강 + s.가산1강2) * 0.001); push('⚡ 스텟(특수강화)', 특수가산) }
      else if (lv <= 39) { push('🦸 고유유닛(강화확률·일반전용)', 일반보); push('📊 스텟(가산1~3강)', (s.가산1강 + s.가산1강2 + s.가산2강 + s.가산2강2 + s.가산3강 + s.가산3강2) * 0.001); push('⚡ 스텟(특수강화)', 특수가산) }
      else if (lv <= 43) push('⚡ 스텟(특수강화)', 특수가산)
      else if (lv <= 47) push(`📊 스텟(가산${lv}강)`, [s.가산44강, s.가산45강, s.가산46강, s.가산47강][lv - 44] * 0.001)
      else if (lv === 48) push('📊 스텟(가산48강)', s.가산48강 * 0.0005)
      else if (lv >= 51) {
        let tb = 0
        if (lv === 51) tb = (ts.추가51강 || 0) * 0.001
        else if (lv === 52) tb = (ts.추가52강 || 0) * 0.001
        else if (lv === 53) tb = (ts.추가53강 || 0) * 0.001
        else if (lv === 54) tb = (ts.추가54강 || 0) * 0.0001 + (ts.추가54강2 || 0) * 0.0005
        else if (lv === 55) tb = (ts.추가55강 || 0) * 0.0001 + (ts.추가55강2 || 0) * 0.0002
        else if (lv === 56) tb = (ts.추가융합 || 0) * 0.001
        else if (lv === 57) tb = (ts.추가57강 || 0) * 0.001
        else if (lv === 58) tb = (ts.추가58강 || 0) * 0.001
        push('🌀 초월스텟(단계별)', tb)
      }
    }
    return { 타입, 확률, 판매, 구입: lv >= 1 && lv <= 56 ? 생산비용(lv) : 0, 공격력: 공당, 공속: 공속초.toFixed(2) + '/초', dps, 판매획득, 내역 }
  }
  const _보스공격력보너스r = 1  // 보스 데미지업 제거 (사용자 요청)
  const _공격력배수r = (1 + _보주공격r + 업그레이드.공격력 * 0.03) * _보스공격력보너스r
  const _공속배수r = 1 + _보주공속r + 업그레이드.공속 * 0.02
  const _크리r = Math.min(0.95, _보주크리r)
  const 사냥터캡 = Math.min(48, 8 + 보스처치수 * 4)  // 8 시작 → 보스당 +4 → 최대 48 (1+2+3 합산)
  const 보스존캡 = 8
  const _초월r = 초월스텟
  const _일반공업r = 일반스텟.유닛공업
  const 사냥터DPS = 보스존마린들.filter(m => m.state === 'attacking').reduce((s, m) => s + 공격력(m.lv, _초월r, _일반공업r) * _공격력배수r * 공격속도(m.lv) * _공속배수r * 연타수(m.lv) * (1 + _크리r), 0)
  // 타격수 측정기: 보스 10클리어 후, 26강+ 마린 중 단위시간당 타격수 1위 유닛을 측정기수만큼 복제해 타격수(=총공격수) 자동 누적.
  // 50강↓ = 공속 기반(초당 타격수), 51강+ = 데미지×공속(초당 딜).
  const 측정기활성 = 보스처치수 >= 10
  const 측정기수 = 4 + Math.min(보석.강타 || 0, 8)
  let 측정기best = 0, _측정기유닛초당 = 0
  for (const m of 마린들) {
    if (m.lv < 26) continue
    const hits = 공격속도(m.lv) * _공속배수r * 연타수(m.lv)
    const 초당 = m.lv >= 51 ? (공격력(m.lv, _초월r, _일반공업r) * _공격력배수r * hits * (1 + _크리r)) : hits
    if (초당 > _측정기유닛초당) { _측정기유닛초당 = 초당; 측정기best = m.lv }
  }
  const 측정기타격수초당 = (측정기활성 && 측정기best > 0) ? 측정기수 * _측정기유닛초당 : 0
  const 측정기타격수초당Ref = useRef(0); 측정기타격수초당Ref.current = 측정기타격수초당
  // 보스 10클리어 시(또는 이미 클리어한 세이브 로드 시) 보스존 마린 전원 베이스로 자동 퇴장
  useEffect(() => {
    if (보스처치수 >= 10) {
      set마린들(prev => prev.some(m => m.location === 'boss')
        ? prev.map(m => m.location === 'boss' ? { ...m, location: 'base' as const, dest: null, state: 'idle' as const, 타겟적id: null } : m)
        : prev)
    }
  }, [보스처치수])
  const 현재배수 = 자원배수(Math.max(사냥터DPS, 최고DPS)) * (1 + _보주배수r)
  const 사냥터마린DPS = 사냥터마린들.reduce((s, m) => s + 공격력(m.lv, _초월r, _일반공업r) * _공격력배수r * 공격속도(m.lv) * _공속배수r * 연타수(m.lv) * (1 + _크리r), 0)
  const 시간당미네랄 = 사냥터마린DPS * 현재배수 * (1 + _보주자원r) * 3600
  const 선택한마린들 = 마린들.filter(m => 선택ID.includes(m.id)).sort((a, b) => b.lv - a.lv)

  // 메시지 자동 숨김
  function 메시지표시(msg: string) {
    set메시지(msg)
    메시지타이머Ref.current = Date.now()
  }

  // 저장 로드
  useEffect(() => {
    AsyncStorage.getItem(저장키).then(json => {
      if (json) {
        try {
          const d = JSON.parse(json)
          if (d.마린들 && Array.isArray(d.마린들)) {
            const restored = d.마린들.map((m: any) => {
              // 마이그레이션: 옛 'hunting' → 강도별 hunting1/2/3
              let loc = m.location || 'base'
              if (loc === 'hunting') loc = 사냥화면(m.lv || 1)
              return {
                ...새마린(m.lv || 1, m.pos || 베이스시작위치(0)),
                id: m.id,
                lv: m.lv || 1,
                pos: m.pos || 베이스시작위치(0),
                location: loc,
              }
            })
            다음마린ID = Math.max(...restored.map((m: 마린) => m.id), 0) + 1
            set마린들(restored)
          }
          if (typeof d.mineral === 'number') setMineral(d.mineral)
          if (typeof d.총공격수 === 'number') set총공격수(d.총공격수)
          if (typeof d.보스처치수 === 'number') {
            set보스처치수(d.보스처치수)
            set적들(d.보스처치수 >= 10 ? [] : 초기적들(d.보스처치수 + 1))
          }
          if (typeof d.최고DPS === 'number') set최고DPS(d.최고DPS)
          if (typeof d.무색조각 === 'number') set무색조각(d.무색조각)
          if (typeof d.응무조 === 'number') set응무조(d.응무조)
          if (typeof d.크리스탈조각 === 'number') set크리스탈조각(d.크리스탈조각)
          if (typeof d.상급크리스탈조각 === 'number') set상급크리스탈조각(d.상급크리스탈조각)
          if (Array.isArray(d.강화최초달성)) set강화최초달성(new Set(d.강화최초달성))
          if (d.보주 && typeof d.보주 === 'object') set보주(prev => ({ ...prev, ...d.보주 }))
          if (typeof d.누적강화성공 === 'number') set누적강화성공(d.누적강화성공)
          if (typeof d.누적판매 === 'number') set누적판매(d.누적판매)
          if (typeof d.최고마린lv === 'number') set최고마린lv(d.최고마린lv)
          if (typeof d.융합누적 === 'number') set융합누적(d.융합누적)
          if (typeof d.자동강화ON === 'boolean') set자동강화ON(d.자동강화ON)
          if (typeof d.자동강화최대lv === 'number') set자동강화최대lv(d.자동강화최대lv)
          if (typeof d.자동판매ON === 'boolean') set자동판매ON(d.자동판매ON)
          if (typeof d.자동판매lv === 'number') set자동판매lv(Math.max(15, d.자동판매lv))
          if (typeof d.자동구입강도 === 'number') set자동구입강도(d.자동구입강도)
          if (typeof d.자동구입ON === 'boolean') set자동구입ON(d.자동구입ON)
          if (typeof d.자동구입배수 === 'number') set자동구입배수(Math.max(1, d.자동구입배수))
          if (d.업그레이드 && typeof d.업그레이드 === 'object') set업그레이드(prev => ({ ...prev, ...d.업그레이드}))
          if (typeof d.캐릭레벨 === 'number') set캐릭레벨(d.캐릭레벨)
          if (typeof d.경험치 === 'number') set경험치(d.경험치)
          if (typeof d.잔여포인트 === 'number') set잔여포인트(d.잔여포인트)
          if (d.일반스텟 && typeof d.일반스텟 === 'object') set일반스텟(prev => ({ ...prev, ...d.일반스텟 }))
          if (d.초월스텟 && typeof d.초월스텟 === 'object') {
            // 새 키만 픽 (옛 스키마 호환)
            const cleaned: any = {}
            for (const k of Object.keys(초기초월스텟) as (keyof 초월스텟타입)[]) {
              if (typeof d.초월스텟[k] === 'number') cleaned[k] = d.초월스텟[k]
            }
            set초월스텟(prev => ({ ...prev, ...cleaned }))
          }
          if (d.명칭크리스탈 && typeof d.명칭크리스탈 === 'object') set명칭크리스탈(prev => ({ ...prev, ...d.명칭크리스탈 }))
          if (Array.isArray(d.장착크리스탈)) set장착크리스탈(d.장착크리스탈)
          if (d.명칭크리스탈Lv && typeof d.명칭크리스탈Lv === 'object') set명칭크리스탈Lv(d.명칭크리스탈Lv)
          if (typeof d.크레딧 === 'number') set크레딧(d.크레딧)
          if (d.보석 && typeof d.보석 === 'object') set보석(prev => ({ ...prev, ...d.보석 }))
          if (d.고유유닛 && typeof d.고유유닛 === 'object') set고유유닛(prev => ({ ...prev, ...d.고유유닛 }))
          if (typeof d.초월레벨 === 'number') set초월레벨(d.초월레벨)
          if (typeof d.초월잔여포인트 === 'number') set초월잔여포인트(d.초월잔여포인트)
          if (typeof d.초월경험치 === 'number') set초월경험치(d.초월경험치)
          if (typeof d.각성의보석 === 'number') set각성의보석(d.각성의보석)
          if (typeof d.ExPoint === 'number') setExPoint(d.ExPoint)
          if (typeof d.경험쿠폰만료 === 'number') set경험쿠폰만료(d.경험쿠폰만료)
          if (typeof d.재화쿠폰만료 === 'number') set재화쿠폰만료(d.재화쿠폰만료)
          if (typeof d.은하조각 === 'number') set은하조각(d.은하조각)
          if (typeof d.자각보주 === 'number') set자각보주(d.자각보주)
          if (typeof d.타격수획득idx === 'number') set타격수획득idx(d.타격수획득idx)
          if (typeof d.extraVI받음 === 'number') setExtraVI받음(d.extraVI받음)
          if (typeof d.extraXI받음 === 'number') setExtraXI받음(d.extraXI받음)
          if (typeof d.환생레벨 === 'number') set환생레벨(d.환생레벨)
          if (typeof d.누적환생수 === 'number') set누적환생수(d.누적환생수)
          if (typeof d.누적50강생산 === 'number') set누적50강생산(d.누적50강생산)
          // 오프라인 보상
          if (typeof d.마지막저장시간 === 'number' && d.마지막저장시간 > 0) {
            const 경과초 = Math.min(8 * 3600, (Date.now() - d.마지막저장시간) / 1000)
            if (경과초 > 60 && d.마린들) {
              const huntingMs = d.마린들.filter((m: any) => m.location === 'hunting1' || m.location === 'hunting2' || m.location === 'hunting3' || m.location === 'hunting')
              const dps = huntingMs.reduce((s: number, m: any) => s + 유닛DPS(m.lv || 1), 0)
              if (dps > 0) {
                const 보상 = Math.floor(dps * 자원배수(d.최고DPS || 0) * 경과초 * 0.5)
                if (보상 > 0) {
                  setMineral(prev => prev + 보상)
                  메시지표시(`🌙 오프라인 ${Math.floor(경과초 / 60)}분 → 💎 ${숫자포맷(보상)}`)
                }
              }
            }
          }
        } catch {}
      }
      set로드완료(true)
    })
  }, [])

  // 자동 저장
  useEffect(() => {
    if (!로드완료) return
    const minimal마린들 = 마린들.map(m => ({ id: m.id, lv: m.lv, pos: m.pos, location: m.location }))
    AsyncStorage.setItem(저장키, JSON.stringify({
      마린들: minimal마린들, mineral, 총공격수, 보스처치수, 최고DPS,
      무색조각, 응무조, 크리스탈조각, 상급크리스탈조각,
      보주,
      업그레이드,
      캐릭레벨, 경험치, 잔여포인트,
      일반스텟, 초월스텟, 명칭크리스탈, 명칭크리스탈Lv, 장착크리스탈,
      크레딧, 보석, 고유유닛, 초월레벨, 초월잔여포인트, 초월경험치,
      각성의보석, ExPoint, 경험쿠폰만료, 재화쿠폰만료, 은하조각, 자각보주,
      타격수획득idx, extraVI받음, extraXI받음,
      환생레벨, 누적환생수, 누적50강생산,
      누적강화성공, 누적판매, 최고마린lv, 융합누적,
      자동강화ON, 자동강화최대lv, 자동판매ON, 자동판매lv, 자동구입강도, 자동구입ON, 자동구입배수,
      강화최초달성: Array.from(강화최초달성),
      마지막저장시간: Date.now(),
    }))
  }, [마린들, mineral, 총공격수, 보스처치수, 최고DPS,
      무색조각, 응무조, 크리스탈조각, 상급크리스탈조각,
      보주,
      업그레이드,
      캐릭레벨, 경험치, 잔여포인트,
      일반스텟, 초월스텟, 명칭크리스탈, 명칭크리스탈Lv, 장착크리스탈,
      크레딧, 보석, 고유유닛, 초월레벨, 초월잔여포인트, 초월경험치,
      각성의보석, ExPoint, 경험쿠폰만료, 재화쿠폰만료, 은하조각, 자각보주,
      타격수획득idx, extraVI받음, extraXI받음,
      환생레벨, 누적환생수, 누적50강생산,
      누적강화성공, 누적판매, 최고마린lv, 융합누적,
      자동강화ON, 자동강화최대lv, 자동판매ON, 자동판매lv, 자동구입강도, 자동구입ON, 자동구입배수, 로드완료])

  // ============================================
  // 게임 루프
  // ============================================
  useEffect(() => {
    let lastTick = Date.now()
    const tick = () => {
      const now = Date.now()
      const dt = Math.min(0.1, (now - lastTick) / 1000)
      lastTick = now

      // 만료된 floating 제거
      setDmg플로팅들(prev => prev.filter(d => d.until > now))

      // 메시지 자동 숨김
      if (메시지타이머Ref.current && now - 메시지타이머Ref.current > 3000) {
        set메시지('')
        메시지타이머Ref.current = 0
      }

      const currentMarines = 마린들Ref.current
      const currentEnemies = 적들Ref.current
      const currentMineral = mineralRef.current
      const huntingMarines = currentMarines.filter(m => is사냥터(m.location as 화면))
      const bossMarines = currentMarines.filter(m => m.location === 'boss')

      // 보주 멀티플라이어 사전 계산 (count-based)
      const bj = 보주Ref.current
      const upg = 업그레이드Ref.current
      const 스텟 = 일반스텟Ref.current
      const 보주공격 = 보주합산(bj, '공격')
      const 보주자원 = 보주합산(bj, '자원')
      const 보주강화 = 보주합산(bj, '강화')
      const 보주이속 = 보주합산(bj, '이속')
      const 보주크리 = 보주합산(bj, '크리')
      const 보주공속 = 보주합산(bj, '공속')
      const 보주배수 = 보주합산(bj, '배수')
      const 명칭보너스 = 명칭크리스탈보너스(명칭크리스탈LvRef.current, 장착크리스탈Ref.current)
      const 보석b = 보석보너스합산(보석Ref.current)
      const 고유유닛스텟cur = 고유유닛Ref.current
      const 고유DPS = 고유유닛DPS(고유유닛스텟cur)
      const 초월lv = 초월레벨Ref.current
      const 보스공격력보너스 = 1  // 보스 데미지업 제거 (사용자 요청)
      const 공격력배수 = (1 + 보주공격 + upg.공격력 * 0.03) * 보스공격력보너스  // 유닛공업은 공격력() 내부에서 처리
      const 공속배수 = 1 + 보주공속 + upg.공속 * 0.02
      const 자원배수기여 = (1 + 보주자원 + upg.자원 * 0.05 + 스텟.돈수급량 * 0.03 + 보석b.자원배수추가) * (1 + 보주배수)
      // 허수광산 크레딧 배수 (사냥터3): 풍요/풍성=최대채광 +1배/개, 부유=최종 +20%/개, 세공=섬세도 +10%/개
      const 광산크레딧배수 = (1 + bj.풍요 + bj.풍성) * (1 + bj.부유 * 0.2) * (1 + bj.세공 * 0.1)
      const 속도 = Math.min(700, 기본이동속도 * (1 + 보주이속 + upg.이속 * 0.03))
      const 보스존캡 = 8
      const 사냥터캡 = Math.min(48, 8 + Math.min(보스처치수Ref.current, 10) * 4)  // 사냥터 1+2+3 합산 최대 48
      const 평균크리 = Math.min(0.95, 보주크리)
      const 초월s = 초월스텟Ref.current
      const 효과DPS = (lv: number) => 공격력(lv, 초월s, 스텟.유닛공업) * 공격력배수 * 공격속도(lv) * 공속배수 * 연타수(lv) * (1 + 평균크리)
      const huntingDPS = bossMarines.filter(m => m.state === 'attacking').reduce((s, m) => s + 효과DPS(m.lv), 0)
      if (huntingDPS > 최고DPSRef.current) {
        set최고DPS(huntingDPS)
      }
      // 고유유닛 단수 효과: 채광력 +1/단(+1당 자원배수 +1) — currentBatch에 추가
      const _단수 = 고유유닛Ref.current.단수
      const 채광력보너스 = _단수 - 1  // 1단=0, 2단=+1, ...
      const currentBatch = (자원배수(Math.max(huntingDPS, 최고DPSRef.current)) + 채광력보너스) * (1 + 보주배수)
      // 사냥터 곱셈 보너스 — 복리: 1.025^재물(하향) × 1.1^각성
      const _재물 = 보석Ref.current.재물 || 0
      const _각성 = 각성의보석Ref.current || 0
      const 사냥터곱셈 = Math.pow(1.025, _재물) * Math.pow(1.1, _각성)
      // Lv3 사냥터 단수 배율: 단수 1당 +10%
      const Lv3단수배율 = Math.pow(1.1, Math.max(0, _단수 - 1))  // 1단=1, 2단=1.1, 3단=1.21, ...
      const 보스게이트 = 보스DPS게이트(보스처치수Ref.current + 1)

      // 필드 경계 클램프
      function clampPos(p: Pos): Pos {
        const r = 마린_크기 / 2
        return {
          x: Math.max(r, Math.min(필드_W - r, p.x)),
          y: Math.max(r, Math.min(필드_H - r, p.y)),
        }
      }

      let 추가미네랄 = 0  // 사냥터 mob 공격으로 획득
      let 추가크레딧 = 0  // 사냥터 3 (DPS측정기) 공격으로 획득
      let 추가초월경험 = 0  // 51강 강화 시도 + 52강+ 판매
      let 융합소비수 = 0  // 이 tick에 56강 융합풀로 흡수된 55강 마린 수
      let 추가공격수 = 0
      let 잔여Mineral = currentMineral
      const 플래시적: number[] = []
      const 플래시몹: number[] = []
      const 몹데미지맵 = new Map<number, number>()
      let 베이스추가 = 0
      const 기존베이스수 = currentMarines.length - huntingMarines.length

      // 인덱스 도우미
      function nearestEnemy(p: Pos) {
        const list = currentEnemies
          .map(e => ({ e, d: 거리(p, e.pos) }))
          .filter(x => x.d <= 공격사거리)
          .sort((a, b) => a.d - b.d)
        return list[0]?.e
      }

      let 보스존추가 = 0
      const 기존보스존수 = currentMarines.filter(m => m.location === 'boss').length
      const 기존사냥1수 = currentMarines.filter(m => m.location === 'hunting1').length
      const 기존사냥2수 = currentMarines.filter(m => m.location === 'hunting2').length
      const 기존사냥3수 = currentMarines.filter(m => m.location === 'hunting3').length
      let 사냥1추가 = 0, 사냥2추가 = 0, 사냥3추가 = 0
      // 56강 융합 필요수: 절약보주 1개당 -10 마리. 최소 1마리. 55강 마린을 누적 흡수해 도달 시 56강 1마리 생성
      const 융합필요수 = Math.max(1, 20000 - bj.절약 * 10)
      const 판매수집: { lv: number }[] = []
      // 판매 크레딧 비용 (강도별)
      const 판매크레딧비용Pre = 판매크레딧비용
      let 가용크레딧Tick = 크레딧Ref.current
      let 화면전환target: 화면 | null = null

      function processBaseZones(m: 마린): 마린 {
        // 이동 중인 마린은 zone 효과 트리거하지 않음 (도착해서 idle일 때만)
        // 단, 자동 강화로 강화소로 이동하는 경우는 dest에 도착했으면 idle로 변환됨
        if (m.state !== 'idle') return m
        // 보스존 입구 (max 8 고정)
        if (점이구역안에(m.pos, ZONE_보스존입구)) {
          if (기존보스존수 + 보스존추가 < 보스존캡) {
            const 새m = {
              ...m,
              location: 'boss' as const,
              pos: 사냥터시작위치(기존보스존수 + 보스존추가),
              state: 'idle' as 유닛상태,
              dest: null,
              타겟적id: null,
            }
            보스존추가++
            화면전환target = 'boss'
            return 새m
          } else {
            if (메시지타이머Ref.current === 0 || now - 메시지타이머Ref.current > 1500) {
              메시지표시('🚫 보스존 가득참')
            }
            return { ...m, state: 'idle', dest: null }
          }
        }
        // 사냥터 입구 (cap = 사냥터캡, 강도별 사냥터 1/2/3 자동 라우팅)
        if (점이구역안에(m.pos, ZONE_사냥터입구)) {
          const 목표loc: 유닛위치 = 사냥화면(m.lv)
          // 사냥터 1+2+3 합산 cap (사냥터캡)
          const 합산수 = (기존사냥1수 + 사냥1추가) + (기존사냥2수 + 사냥2추가) + (기존사냥3수 + 사냥3추가)
          const 기존수 = 목표loc === 'hunting1' ? 기존사냥1수 + 사냥1추가
            : 목표loc === 'hunting2' ? 기존사냥2수 + 사냥2추가
            : 기존사냥3수 + 사냥3추가
          if (합산수 < 사냥터캡) {
            const 새m = {
              ...m,
              location: 목표loc,
              pos: 사냥터시작위치(기존수),
              state: 'idle' as 유닛상태,
              dest: null,
              타겟적id: null,
            }
            if (목표loc === 'hunting1') 사냥1추가++
            else if (목표loc === 'hunting2') 사냥2추가++
            else 사냥3추가++
            if (!화면전환target) 화면전환target = 목표loc
            return 새m
          } else {
            if (메시지타이머Ref.current === 0 || now - 메시지타이머Ref.current > 1500) {
              메시지표시(`🚫 사냥터${목표loc.slice(-1)} 가득참 (${사냥터캡}/${사냥터캡})`)
            }
            return { ...m, state: 'idle', dest: null }
          }
        }
        // 판매소: 마린 판매 (51강은 판매 불가 — 통과)
        if (점이구역안에(m.pos, ZONE_판매소)) {
          if (m.lv === 51) {
            if (메시지타이머Ref.current === 0 || now - 메시지타이머Ref.current > 1500) {
              메시지표시(`🚫 51강은 판매 불가`)
            }
            return { ...m, state: 'idle', dest: null }
          }
          if (m.lv === 55) {
            // 55강은 56강 융합 대기 — 판매 불가 (강화소에서 융합풀로 흡수)
            return { ...m, state: 'idle', dest: null }
          }
          // 크레딧 검증 — 부족하면 판매 취소 (마린 보존)
          const _cost = 판매크레딧비용Pre(m.lv)
          if (가용크레딧Tick < _cost) {
            if (메시지타이머Ref.current === 0 || now - 메시지타이머Ref.current > 2000) {
              메시지표시(`💰 크레딧이 부족합니다 (${숫자포맷(_cost)} 필요)`)
            }
            return { ...m, state: 'idle', dest: null }
          }
          가용크레딧Tick -= _cost
          판매수집.push({ lv: m.lv })
          return { ...m, id: -1 }  // 제거 대상
        }
        // 강화소 체크 (자동 강화 — 비용 없음)
        if (점이구역안에(m.pos, ZONE_강화) && now - m.마지막강화시간 >= 강화쿨다운) {
          {
            const idx = currentMarines.indexOf(m)
            const 시작점 = clampPos(베이스시작위치(idx))
            const 새m = {
              ...m,
              마지막강화시간: now,
              // 강화 완료 후 시작지점으로 복귀 (clamp 보장)
              pos: 시작점,
              state: 'idle' as 유닛상태,
              dest: null,
            }
            // 초월 51_53강 강화확률 보너스 (51-53강 적용)
            const 초월51_53보너스 = 0  // 신규 초월스텟에서는 강화시도() 내부에서 단계별 처리
            // 초월 56강 융합확률 보너스 (56강 적용)
            const 초월56융합 = 0  // 신규 초월스텟에서 강화시도() 내부 처리
            const 궁극적용 = m.lv <= 47 ? 보석b.궁극보너스 : 0  // 궁극 보석은 47강까지만 적용
            const 외부강화보너스 = 보주강화 + upg.강화확률 * 0.005 + 명칭보너스.개별확률 + 초월51_53보너스 + 초월56융합 + 궁극적용
            const 고유일반보너스 = (고유유닛스텟cur.추가1강 * 0.0025 + 고유유닛스텟cur.특수강화 * 0.005)  // 고유유닛 강화확률 보너스는 일반확률(+1)에만
            // 50강 → 51강 초월 시도 (base 0.5% + 누적). 51강 도달은 초월시스템과 무관 (30만렙만 unlock)
            if (m.lv === 50) {
              const 추가 = (초월스텟Ref.current.추가초월확률 + 명칭보너스.초월확률 + 초월lv) * 0.00001 + 보석b.초월확률추가
              const 초월p = 0.005 + 추가  // base 0.5%
              if (Math.random() < Math.min(0.95, 초월p)) {
                새m.lv = 51
                set누적강화성공(p => p + 1)
                set최고마린lv(p => Math.max(p, 51))
                메시지표시('✨ 초월 성공! 51강 달성!')
              } else {
                // 50강 초월 실패 → 마린 파괴
                const r = 강화실패결과(50, 스텟.특수파괴방지 + 스텟.특수파괴방지2 + 명칭보너스.파괴방지 + 보석b.파괴방지 + 고유유닛스텟cur.파괴방지)
                if (r.파괴) {
                  if (메시지타이머Ref.current === 0 || now - 메시지타이머Ref.current > 1000) {
                    메시지표시('💥 50강 초월 실패... 마린 파괴!')
                  }
                  return { ...새m, id: -1 }
                }
              }
              return 새m
            }
            // 56강 융합: 55강 마린은 강화소 도달 시 융합풀로 흡수(제거). tick 종료 후 누적 ≥ 융합필요수면 56강 생성.
            if (m.lv === 55) {
              융합소비수++
              return { ...새m, id: -1 }
            }
            const 강화가능 = (m.lv < 50) || (m.lv >= 51 && m.lv < 60)
            let 단계보주 = 0
            for (const k of 보주종류목록) {
              const eff = 보주효과표[k]
              const cnt = bj[k]
              if (cnt === 0) continue
              if (eff.타입 === `강화${m.lv}`) 단계보주 += cnt * eff.값
              if (eff.타입 === '강화54_55' && (m.lv === 54 || m.lv === 55)) 단계보주 += cnt * eff.값
            }
            const 증가 = 강화가능 ? 강화시도(m.lv, 스텟, 외부강화보너스 + 단계보주, 보석b, 초월s, 고유일반보너스) : 0
            if (m.lv === 51) 추가초월경험 += 강화51초월경험  // 51강 강화 시도 1회당 초월경험치
            if (증가 > 0) {
              새m.lv = m.lv + 증가
              set누적강화성공(p => p + 1)
              set최고마린lv(p => Math.max(p, 새m.lv))
              // 50강 첫 도달 시 누적 카운트 (환생 ExP 보상 산정용)
              if (m.lv < 50 && 새m.lv >= 50) set누적50강생산(p => p + 1)
              // 강화 최초 달성 보상 (45~50강, 사진 기반)
              const 최초보상표: Record<number, { 크레딧: number; 무색: number }> = {
                45: { 크레딧: 5e6, 무색: 32 },
                46: { 크레딧: 2e7, 무색: 322 },
                47: { 크레딧: 5e7, 무색: 3222 },
                48: { 크레딧: 322222222, 무색: 32222 },
                49: { 크레딧: 1.5e9, 무색: 322222 },
                50: { 크레딧: 1e10, 무색: 3222222 },
              }
              for (let chk = m.lv + 1; chk <= 새m.lv; chk++) {
                if (최초보상표[chk] && !강화최초달성Ref.current.has(chk)) {
                  const r = 최초보상표[chk]
                  set크레딧(p => p + r.크레딧)
                  set무색조각(p => p + r.무색)
                  set강화최초달성(prev => { const n = new Set(prev); n.add(chk); return n })
                  메시지표시(`🏆 ${chk}강 최초 달성! 💰+${숫자포맷(r.크레딧)} 🔷+${숫자포맷(r.무색)}`)
                }
              }
              if (m.lv >= 51) {
                메시지표시(`✨ ${m.lv}강 → ${새m.lv}강! (초월 강화 성공)`)
              }
            } else {
              // 실패 페널티
              const r = 강화실패결과(m.lv, 스텟.특수파괴방지 + 스텟.특수파괴방지2 + 명칭보너스.파괴방지 + 보석b.파괴방지 + 고유유닛스텟cur.파괴방지)
              // (51~55강 강화 실패 ExP 보상 제거 — 자동강화로 ExP 폭증해서 삭제)
              if (r.파괴) {
                if (m.lv < 51 && (메시지타이머Ref.current === 0 || now - 메시지타이머Ref.current > 1000)) {
                  메시지표시(`💥 ${m.lv}강 마린 파괴!`)
                }
                return { ...새m, id: -1 }  // 마킹: 제거
              }
              if (r.감소 > 0) {
                새m.lv = Math.max(1, m.lv - r.감소)
                if (메시지타이머Ref.current === 0 || now - 메시지타이머Ref.current > 1000) {
                  메시지표시(`📉 ${m.lv}강 → ${새m.lv}강`)
                }
              }
            }
            return 새m
          }
        }
        return m
      }

      const newMarines = currentMarines.map(m => {
        let n = { ...m }
        const cd = 1000 / (공격속도(n.lv) * 공속배수)

        // 사냥터 (mob): 공격→미네랄 / 보스존: 공격→무색조각
        // 마린 강도 → 사냥터 티어 일치하는 mob만 타겟
        function nearestMob(p: Pos, marineLv: number): 몹 | undefined {
          const tier = 사냥터티어(marineLv)
          const list = 몹들Ref.current
            .filter(m => m.deadUntil <= now && m.티어 === tier)
            .map(m => ({ m, d: 거리(p, m.pos) }))
            .filter(x => x.d <= 공격사거리 * 3)  // 사냥터 mob 거리 넉넉히
            .sort((a, b) => a.d - b.d)
          return list[0]?.m
        }
        // 가장 가까운 자기 티어 mob (사거리 무관 — auto-route)
        function 자기티어몹(marineLv: number): 몹 | undefined {
          const tier = 사냥터티어(marineLv)
          return 몹들Ref.current.find(m => m.티어 === tier)
        }

        // 위치별 분기
        if (is사냥터(n.location as 화면) || n.location === 'boss') {
          // 베이스 입구 체크 (서브 화면 → 베이스)
          if (점이구역안에(n.pos, ZONE_베이스입구)) {
            const 새m = {
              ...n,
              location: 'base' as const,
              pos: 베이스시작위치(기존베이스수 + 베이스추가),
              state: 'idle' as 유닛상태,
              dest: null,
              타겟적id: null,
            }
            베이스추가++
            return 새m
          }
        }

        if (n.location === 'boss') {
          // ========== 보스존 마린 (재화 X, DPS만 기여) ==========
          const 적 = currentEnemies.find(e => e.id === n.타겟적id)

          if (n.state === 'attacking' && 적) {
            const d = 거리(n.pos, 적.pos)
            if (d > 공격사거리) {
              n.pos = clampPos(이동좌표(n.pos, 적.pos, 속도 * dt))
            } else if (now - n.마지막공격시간 >= cd) {
              n.마지막공격시간 = now
              n.공격플래시Until = now + 150
              플래시적.push(적.id)
              // 보스존 데미지 floating (재화 X, gate만 본다)
              const isCrit = Math.random() < 평균크리
              // 보스 데미지 보너스 (초월스텟 보스데미지 +10%/pt)
              const 보스데미지배수 = 1  // 신규 초월스텟에 보스데미지 없음 (보스공격력보너스로 대체됨)
              const dmgShow = Math.round(공격력(n.lv, 초월s, 스텟.유닛공업) * 공격력배수 * 보스데미지배수 * 연타수(n.lv) * (isCrit ? 2 : 1))
              // (타격수는 사냥/보스 공격이 아닌 '타격수 측정기'에서만 누적 — 아래 측정기 블록)
              if (Math.random() < 0.4) {
                const fid = dmgIdRef.current++
                setDmg플로팅들(prev => [...prev.slice(-20), {
                  id: fid, x: 적.pos.x + (Math.random() - 0.5) * 40, y: 적.pos.y - 보스_크기 / 2,
                  dmg: dmgShow, crit: isCrit, until: now + 900,
                }])
              }
            }
            return n
          }
          if (n.state === 'attacking' && !적) { n.state = 'idle'; n.타겟적id = null }
          if (n.state === 'move' && n.dest) {
            n.pos = clampPos(이동좌표(n.pos, n.dest, 속도 * dt))
            if (거리(n.pos, n.dest) < 1) { n.dest = null; n.state = 'idle' }
            return n
          }
          if (n.state === 'attack-move' && n.dest) {
            const 가까운적 = nearestEnemy(n.pos)
            if (가까운적) { n.state = 'attacking'; n.타겟적id = 가까운적.id; return n }
            n.pos = clampPos(이동좌표(n.pos, n.dest, 속도 * dt))
            if (거리(n.pos, n.dest) < 1) { n.dest = null; n.state = 'idle' }
            return n
          }
          if (n.state === 'idle' || n.state === 'hold') {
            const 가까운적 = nearestEnemy(n.pos)
            if (가까운적) { n.state = 'attacking'; n.타겟적id = 가까운적.id }
            return n
          }
          return n
        } else if (is사냥터(n.location as 화면)) {
          // ========== 사냥터 마린 (미네랄 획득, 몹 처치) ==========
          const target = 몹들Ref.current.find(mb => mb.id === n.타겟적id && mb.deadUntil <= now)

          if (n.state === 'attacking' && target) {
            const d = 거리(n.pos, target.pos)
            if (d > 공격사거리) {
              n.pos = clampPos(이동좌표(n.pos, target.pos, 속도 * dt))
            } else if (now - n.마지막공격시간 >= cd) {
              n.마지막공격시간 = now
              n.공격플래시Until = now + 150
              const isCrit = Math.random() < 평균크리
              const dmg = 공격력(n.lv, 초월s, 스텟.유닛공업) * 공격력배수 * 연타수(n.lv) * (isCrit ? 2 : 1)
              // 사냥터 단가 (xlsx 신규): Lv1=1원/dmg, Lv2=10만원/dmg, Lv3=100억원/dmg × 단수배율
              const tier = target.티어
              // 단가 조정: Lv1=1, Lv2=1만, Lv3=10억 (밸런스 — 10배 감소)
              const 단가_base = tier === 1 ? 1 : tier === 2 ? 10000 : 1000000000
              const 단가 = tier === 3 ? 단가_base * Lv3단수배율 : 단가_base
              추가미네랄 += dmg * 단가 * 사냥터곱셈 * currentBatch * 자원배수기여 * 소득배수(n.lv) * 자원전역배율
              // 사냥터 3 (허수광산) → 크레딧 + 돈 동시 (풍요/풍성/부유/세공 보주 배수)
              if (tier === 3) {
                추가크레딧 += Math.max(1, Math.floor(dmg / 1000 * 광산크레딧배수))
              }
              // (타격수는 '타격수 측정기'에서만 누적)
              플래시몹.push(target.id)
              몹데미지맵.set(target.id, (몹데미지맵.get(target.id) ?? 0) + dmg)
            }
            return n
          }
          if (n.state === 'attacking' && !target) { n.state = 'idle'; n.타겟적id = null }
          if (n.state === 'move' && n.dest) {
            n.pos = clampPos(이동좌표(n.pos, n.dest, 속도 * dt))
            if (거리(n.pos, n.dest) < 1) { n.dest = null; n.state = 'idle' }
            return n
          }
          if (n.state === 'attack-move' && n.dest) {
            const 가까운몹 = nearestMob(n.pos, n.lv) ?? 자기티어몹(n.lv)
            if (가까운몹) { n.state = 'attacking'; n.타겟적id = 가까운몹.id; return n }
            n.pos = clampPos(이동좌표(n.pos, n.dest, 속도 * dt))
            if (거리(n.pos, n.dest) < 1) { n.dest = null; n.state = 'idle' }
            return n
          }
          if (n.state === 'idle' || n.state === 'hold') {
            // 자기 티어 mob 자동 타겟 (auto-route)
            const 가까운몹 = nearestMob(n.pos, n.lv) ?? 자기티어몹(n.lv)
            if (가까운몹) { n.state = 'attacking'; n.타겟적id = 가까운몹.id }
            return n
          }
          return n
        } else {
          // ========== 베이스 마린 ==========
          if (n.state === 'move' && n.dest) {
            n.pos = clampPos(이동좌표(n.pos, n.dest, 속도 * dt))
            if (거리(n.pos, n.dest) < 1) { n.dest = null; n.state = 'idle' }
            return processBaseZones(n)
          }
          if (n.state === 'attack-move' && n.dest) {
            n.pos = clampPos(이동좌표(n.pos, n.dest, 속도 * dt))
            if (거리(n.pos, n.dest) < 1) { n.dest = null; n.state = 'idle' }
            return processBaseZones(n)
          }
          if (n.state === 'attacking' || n.state === 'hold') {
            n.state = 'idle'; n.타겟적id = null
          }
          // 자동 판매 우선
          if (n.state === 'idle' && 자동판매ONRef.current && n.lv === 자동판매lvRef.current) {
            n.state = 'move'
            n.dest = { x: ZONE_판매소.x + ZONE_판매소.w / 2, y: ZONE_판매소.y + ZONE_판매소.h / 2 }
          }
          // 자동 강화
          else if (n.state === 'idle' && 자동강화ONRef.current && n.lv <= 자동강화최대lvRef.current) {
            n.state = 'move'
            n.dest = { x: ZONE_강화.x + ZONE_강화.w / 2, y: ZONE_강화.y + ZONE_강화.h / 2 }
          }
          return processBaseZones(n)
        }
      })

      // 판매소 zone에 도달한 마린 판매 처리
      let 판매무색 = 0, 판매응무조 = 0, 판매크리조각 = 0, 판매ExP = 0, 판매각성 = 0, 판매은하 = 0, 판매자각 = 0
      let 판매일반XP = 0, 판매크레딧 = 0
      const 판매크리스탈드랍: (keyof 명칭크리스탈목록)[] = []
      const 판매보상배수 = (1 + 보주합산(bj, '판매')) * 명칭보너스.판매배수배율
      const 무색배수 = (1 + 보주합산(bj, '무색')) * 명칭보너스.무색배수배율
      const 조각배수 = 1 + 보주합산(bj, '조각')
      const 장착세트 = new Set(장착크리스탈Ref.current)
      const eqCnt = (k: keyof 명칭크리스탈목록): number => 장착세트.has(k) ? (명칭크리스탈LvRef.current[k] ?? 0) : 0
      const 각성배수 = 1 + eqCnt('각성') * 1.0 + eqCnt('노랑') * 1.0 + eqCnt('우주') * 5.0
      // 크레딧은 이미 사전 검증됨 (판매소 진입 시점). 판매수집 = 판매 확정 마린
      const 총소모크레딧 = 크레딧Ref.current - 가용크레딧Tick
      const 경험쿠폰활성 = now < 경험쿠폰만료Ref.current
      const 재화쿠폰활성 = now < 재화쿠폰만료Ref.current
      for (const s of 판매수집) {
        const r = 판매보상(s.lv)
        const 경x = 경험쿠폰활성 ? 3 : 1            // 15~44강 판매 경험치 3배 (일반XP는 15~44만 발생)
        const 재x = (재화쿠폰활성 && s.lv >= 45 && s.lv <= 50) ? 2 : 1  // 45~50강 판매 재화 2배
        판매무색 += Math.round(r.무색조각 * 판매보상배수 * 무색배수 * 재x)
        판매응무조 += Math.round(r.응무조 * 판매보상배수 * 재x)
        판매크리조각 += Math.round(r.크리스탈조각 * 판매보상배수 * 조각배수 * 재x)
        판매ExP += Math.round(r.ExP * 판매보상배수 * 재x)
        판매각성 += Math.round(r.각성보석 * 각성배수 * 재x)
        판매은하 += r.은하조각
        판매자각 += r.자각보주
        판매일반XP += Math.round(r.일반XP * 판매보상배수 * 경x)
        판매크레딧 += Math.round(r.크레딧 * 판매보상배수 * 재x)
        if (s.lv >= 52) 추가초월경험 += 판매초월경험표[s.lv] ?? 0  // 52강+ 판매 → 초월경험치
        for (const 등급 of r.박스) 판매크리스탈드랍.push(박스개봉(등급))
      }
      if (총소모크레딧 > 0) set크레딧(prev => prev - 총소모크레딧)
      // id=-1 마린 제거 (판매소 도달 + 강화 실패 파괴 + 56강 융합 흡수)
      const finalMarines = newMarines.filter(m => m.id !== -1)
      // 56강 융합: 흡수한 55강 누적 → 필요수 도달 시 56강 1마리씩 생성
      let finalMarines2 = finalMarines
      if (융합소비수 > 0) {
        let 새융합누적 = 융합누적Ref.current + 융합소비수
        let 융합완성수 = 0
        while (새융합누적 >= 융합필요수) { 새융합누적 -= 융합필요수; 융합완성수++ }
        if (융합완성수 > 0) {
          let bc = finalMarines.filter(m => m.location === 'base').length
          const 추가56: 마린[] = []
          for (let i = 0; i < 융합완성수; i++) 추가56.push(새마린(56, 베이스시작위치(bc + i), 'base'))
          finalMarines2 = [...finalMarines, ...추가56]
          set누적강화성공(p => p + 융합완성수)
          set최고마린lv(p => Math.max(p, 56))
          메시지표시(`🔮 56강 융합 성공! +${융합완성수}마리 (누적 ${숫자포맷(융합필요수)})`)
        }
        set융합누적(새융합누적)
      }
      set마린들(finalMarines2)

      // 자동 구입 (총 마린 상한 미만). 배수만큼 한 tick에 일괄 구입
      // 내부계산모드(렌더 OFF) 시 상한 대폭↑, 스프라이트 렌더 시엔 렉 방지 위해 낮게
      const 최대유닛수 = 5000
      const 총마린수예상 = currentMarines.length - 판매수집.length
      if (자동구입ONRef.current && now - 자동구입타이머Ref.current >= 50 && 총마린수예상 < 최대유닛수) {
        const lv = Math.min(50, 자동구입강도Ref.current)  // 구입은 50강까지만
        const cost = 생산비용(lv)
        const 단수촉진 = Math.floor(((고유유닛Ref.current.단수 || 1) - 1) / 10)  // 고유유닛 단수 10단마다 촉진 +1
        const 틱촉진합 = (보석Ref.current.촉진 || 0) + (일반스텟Ref.current.촉진 || 0) + (초월스텟Ref.current.촉진 || 0) + 단수촉진
        const 틱최대 = 10 + 틱촉진합  // 촉진이 선택 가능한 최대치를 올림(강제 하한 X)
        const 배수 = Math.min(Math.max(1, 자동구입배수Ref.current), 틱최대)  // 슬라이더로 1~최대 자유 선택
        const 가용 = 잔여Mineral + 추가미네랄
        const 슬롯여유 = 최대유닛수 - 총마린수예상
        // 선형 구입: 1마리당 cost. 구입수 = min(배수, 슬롯여유, 살 수 있는 만큼). 비용 = cost × 구입수.
        const 구입수 = Math.min(배수, 슬롯여유, Math.floor(가용 / cost))
        if (구입수 > 0) {
          자동구입타이머Ref.current = now
          잔여Mineral -= cost * 구입수
          set마린들(prev => {
            let bc = prev.filter(m => m.location === 'base').length
            const 추가: 마린[] = []
            for (let i = 0; i < 구입수; i++) 추가.push(새마린(lv, 베이스시작위치(bc + i), 'base'))
            return [...prev, ...추가]
          })
        }
      }

      // 고유유닛 DPS 기여 (사냥터 배치 상시 적용 — 마린 없어도 단독 수입)
      if (고유DPS > 0) {
        // 고유유닛 위치는 1|2뿐 (사3 진입 X). 단수효과는 채광력보너스로 별도 적용.
        const 고유단가 = 고유유닛스텟cur.위치 === 1 ? 1 : 10000
        추가미네랄 += 고유DPS * 고유단가 * 사냥터곱셈 * currentBatch * 자원배수기여 * dt * 자원전역배율
      }
      // 고유유닛 dest 보간 이동 (smooth)
      if (고유유닛destRef.current) {
        const cur = 고유유닛posRef.current
        const dst = 고유유닛destRef.current
        const d = 거리(cur, dst)
        if (d < 2) {
          set고유유닛dest(null)
        } else {
          const 고유속도 = 속도 * 1.2
          const next = 이동좌표(cur, dst, 고유속도 * dt)
          set고유유닛pos(clampPosStatic(next))
        }
      }

      // currency 업데이트
      if (잔여Mineral !== currentMineral) setMineral(잔여Mineral + 추가미네랄)
      else if (추가미네랄 > 0) setMineral(prev => prev + 추가미네랄)
      if (추가크레딧 > 0) set크레딧(prev => prev + Math.floor(추가크레딧))
      if (판매무색 > 0) set무색조각(prev => prev + 판매무색)
      if (판매응무조 > 0) set응무조(prev => prev + 판매응무조)
      if (판매크리조각 > 0) set크리스탈조각(prev => prev + 판매크리조각)
      if (판매각성 > 0) set각성의보석(prev => prev + 판매각성)
      if (판매은하 > 0) set은하조각(prev => prev + 판매은하)
      if (판매자각 > 0) set자각보주(prev => prev + 판매자각)
      if (판매크레딧 > 0) set크레딧(prev => prev + 판매크레딧)
      if (판매일반XP > 0) XP획득(판매일반XP)
      // 크리스탈 박스 결과 (각각 1개씩 명칭크리스탈에 +1, max 캡)
      if (판매크리스탈드랍.length > 0) {
        set명칭크리스탈(prev => {
          const next = { ...prev }
          for (const k of 판매크리스탈드랍) {
            const cap = 명칭크리스탈max[k] ?? 100
            const cur = next[k] as number
            if (cur < cap) next[k] = cur + 1
          }
          return next
        })
      }

      // ExPoint 자동 초월 승급 제거 (사용자 요청). ExP는 별도 재화로만 누적
      if (판매ExP > 0) setExPoint(prev => prev + 판매ExP)
      if (추가초월경험 > 0) 초월경험획득(추가초월경험)

      // 타격수 측정기: 보스 10클리어 후 26강+ best유닛 × 측정기수 만큼 초당 타격수 누적
      if (측정기타격수초당Ref.current > 0) 추가공격수 += 측정기타격수초당Ref.current * dt

      if (추가공격수 > 0) {
        const 새공격수 = 총공격수Ref.current + 추가공격수
        set총공격수(새공격수)
        // 🎯 타격수 마일스톤 보상 체크
        let nextIdx = 타격수획득idxRef.current
        let totalExP = 0, total조각 = 0, total은하 = 0, total크레딧 = 0, total상급 = 0
        const total박스: 크리스탈박스등급[] = []
        const claimedLabels: string[] = []
        while (nextIdx < 타격마일스톤표.length && 새공격수 >= 타격마일스톤표[nextIdx].임계값) {
          const m = 타격마일스톤표[nextIdx]
          totalExP += m.보상.ExP
          total조각 += m.보상.조각
          total은하 += m.보상.은하조각
          total크레딧 += m.보상.크레딧
          total상급 += m.보상.상급조각
          for (const b of m.보상.크리스탈박스) total박스.push(b)
          claimedLabels.push(m.라벨)
          nextIdx++
        }
        if (nextIdx > 타격수획득idxRef.current) {
          set타격수획득idx(nextIdx)
          if (totalExP > 0) setExPoint(prev => prev + totalExP)
          if (total조각 > 0) set크리스탈조각(prev => prev + total조각)
          if (total은하 > 0) set은하조각(prev => prev + total은하)
          if (total크레딧 > 0) set크레딧(prev => prev + total크레딧)
          if (total상급 > 0) set상급크리스탈조각(prev => prev + total상급)
          if (total박스.length > 0) {
            set명칭크리스탈(prev => {
              const next = { ...prev }
              for (const 등급 of total박스) {
                const k = 박스개봉(등급)
                const cap = 명칭크리스탈max[k] ?? 100
                const cur = next[k] as number
                if (cur < cap) next[k] = cur + 1
              }
              return next
            })
          }
          메시지표시(`🎯 타격수 ${claimedLabels.join(',')}회 보상! ⭐+${숫자포맷(totalExP)}${total크레딧 > 0 ? ` 💰+${숫자포맷(total크레딧)}` : ''}${total상급 > 0 ? ` 🟣+${total상급}상급` : ''} 🌟${total박스.length}박스`)
        }
      }

      if (판매수집.length > 0) {
        set누적판매(p => p + 판매수집.length)
        // 판매XP는 위에서 판매일반XP로 이미 처리됨 (xlsx 기반)
        const parts: string[] = []
        if (판매일반XP > 0) parts.push(`📗+${숫자포맷(판매일반XP)}XP`)
        if (판매무색 > 0) parts.push(`🔷+${숫자포맷(판매무색)}`)
        if (판매크레딧 > 0) parts.push(`💰+${숫자포맷(판매크레딧)}`)
        if (판매응무조 > 0) parts.push(`💠+${숫자포맷(판매응무조)}`)
        if (판매크리조각 > 0) parts.push(`🔮+${숫자포맷(판매크리조각)}`)
        if (판매ExP > 0) parts.push(`⭐+${숫자포맷(판매ExP)}`)
        if (판매각성 > 0) parts.push(`💎+${판매각성}각성`)
        if (판매은하 > 0) parts.push(`🌌+${판매은하}은하`)
        if (판매자각 > 0) parts.push(`🔯+${판매자각}자각`)
        if (판매크리스탈드랍.length > 0) parts.push(`🌟${판매크리스탈드랍.length}박스`)
        메시지표시(`🛒 판매 ${판매수집.length} ${parts.join(' ')}`)
      }

      // 몹 플래시 (무적 - 죽지 않음)
      if (플래시몹.length > 0) {
        set몹들(prev => prev.map(m =>
          플래시몹.includes(m.id) ? { ...m, flashUntil: now + 150 } : m
        ))
      }

      // 보스 플래시
      if (플래시적.length > 0) {
        set적들(prev => prev.map(e => 플래시적.includes(e.id) ? { ...e, flashUntil: now + 150 } : e))
      }
      // 보스 처치: 10단계까지만. 10보스 클리어 후 보스존 종료 (ExtraBoss 제거)
      if (보스처치수Ref.current < 10 && huntingDPS >= 보스게이트 && now - 보스킬쿨다운Ref.current >= 2000) {
        보스킬쿨다운Ref.current = now
        const baseN = 보스처치수Ref.current
        set보스처치수(prev => prev + 1)
        // 보스 클리어 → 레벨 +1500 (10보스=15000렙). 레벨당 5포인트 → +7500
        set캐릭레벨(prev => prev + 1500)
        set잔여포인트(prev => prev + 7500)
        if (Platform.OS !== 'web') Vibration.vibrate([0, 100, 50, 100])
        const _보스배수 = 1
        const 조각드랍 = Math.round((baseN + 1) * 10 * (1 + 보주합산(bj, '조각')) * _보스배수)
        set크리스탈조각(prev => prev + 조각드랍)
        XP획득(Math.round((baseN + 1) * 200 * _보스배수))
        set크레딧(prev => prev + Math.round((10 + baseN * 5) * _보스배수))
        const 각성드랍 = Math.floor((baseN + 1) / 3) + (Math.random() < 0.05 ? 1 : 0)
        if (각성드랍 > 0) set각성의보석(prev => prev + Math.round(각성드랍 * _보스배수))
        setExPoint(prev => prev + Math.round((baseN + 1) * 100 * _보스배수))
        const 새보스 = baseN + 1
        const 새배수 = 1 + Math.min(새보스, 10) * 0.5
        if (새보스 >= 10) {
          set적들([])  // 보스존 종료 → 타격수 측정기 시스템으로 전환
          // 보스존 마린 전원 베이스로 자동 퇴장
          set마린들(prev => prev.map(m => m.location === 'boss' ? { ...m, location: 'base' as const, dest: null, state: 'idle' as const, 타겟적id: null } : m))
          메시지표시(`🌌 10보스 전체 클리어! 보스존 마린 퇴장 → 타격수 측정기 가동!`)
        } else {
          메시지표시(`⚔️ 보스 ${새보스}/10 클리어! 공격력 ×${새배수.toFixed(1)} · Lv+1500 · 사냥터+4 🔮+${조각드랍}`)
        }
      }
      // 자동 화면 전환 비활성 (사용자 선호: 수동 탭 전환)
    }
    const timer = setInterval(tick, 33)
    return () => clearInterval(timer)
  }, [])

  // ============================================
  // 명령 함수
  // ============================================
  function 이동명령(목적: Pos) {
    const c = clampPosStatic(목적)  // 마린이 도달 가능한 좌표로 clamp
    set마린들(prev => prev.map(m =>
      선택IDRef.current.includes(m.id) ? { ...m, state: 'move', dest: c, 타겟적id: null } : m
    ))
    // 고유유닛도 선택 상태면 같이 이동 (현재 사냥터 화면에 있을 때만)
    if (고유유닛선택Ref.current && is사냥터(현재화면Ref.current as 화면)) {
      set고유유닛dest(c)
    }
  }
  function 공격이동명령(목적: Pos) {
    const c = clampPosStatic(목적)
    set마린들(prev => prev.map(m =>
      선택IDRef.current.includes(m.id) ? { ...m, state: 'attack-move', dest: c, 타겟적id: null } : m
    ))
    if (고유유닛선택Ref.current && is사냥터(현재화면Ref.current as 화면)) {
      set고유유닛dest(c)
    }
  }
  function 공격명령(적id: number) {
    set마린들(prev => prev.map(m =>
      선택IDRef.current.includes(m.id) ? { ...m, state: 'attacking', 타겟적id: 적id, dest: null } : m
    ))
  }
  function 정지명령() {
    set마린들(prev => prev.map(m =>
      선택IDRef.current.includes(m.id) ? { ...m, state: 'idle', dest: null, 타겟적id: null } : m
    ))
  }
  function hold명령() {
    set마린들(prev => prev.map(m =>
      선택IDRef.current.includes(m.id) ? { ...m, state: 'hold', dest: null, 타겟적id: null } : m
    ))
  }

  // 사냥터 마린 전체 베이스로 복귀
  function 사냥터마린전체복귀() {
    set마린들(prev => {
      const huntingList = prev.filter(m => is사냥터(m.location as 화면))
      let baseIdx = prev.filter(m => m.location === 'base').length
      return prev.map(m => {
        if (!is사냥터(m.location as 화면)) return m
        const pos = 베이스시작위치(baseIdx++)
        return { ...m, location: 'base' as const, pos, state: 'idle' as 유닛상태, dest: null, 타겟적id: null }
      })
    })
    set현재화면('base')
    메시지표시('🏠 사냥터 마린 베이스 복귀')
  }

  // XP 획득 → 레벨업 처리 (delta 기반, 다른 set으로부터의 +2000 등을 덮어쓰지 않음)
  function XP획득(amount: number) {
    const 경험배수 = 보석보너스합산(보석Ref.current).경험배수
    const 공명배수 = Math.pow(0.9, 보주Ref.current.공명)  // 공명 보주 1개당 필요경험치 ×0.9 (복리)
    let xp = 경험치Ref.current + Math.round(amount * 경험배수)
    let simLv = 캐릭레벨Ref.current
    let lvDelta = 0
    while (simLv + lvDelta < 캐릭레벨최대 && xp >= 다음경험치(simLv + lvDelta) * 공명배수) {
      xp -= Math.round(다음경험치(simLv + lvDelta) * 공명배수)
      lvDelta++
    }
    if (lvDelta > 0) {
      set캐릭레벨(prev => prev + lvDelta)
      set잔여포인트(prev => prev + lvDelta * 5)  // 계산기 기준: 레벨 1당 스탯 5포인트
      메시지표시(`🎊 레벨업! +${lvDelta} (+${lvDelta * 5} 포인트)`)
    }
    // 만렙(30만) 도달 → 잔여 일반 XP 폐기. 초월경험치는 51강 강화/52강+ 판매로만 (초월경험획득)
    if (simLv + lvDelta >= 캐릭레벨최대) xp = 0
    set경험치(xp)
  }

  // 초월경험치 획득 (30만 도달=초월레벨≥1 이후만). 경험 보주 ×(1+경험)/개 적용 → 초월레벨업
  function 초월경험획득(amount: number) {
    if (초월레벨Ref.current < 1 || amount <= 0) return
    const 경험보주 = 1 + 보주합산(보주Ref.current, '초월경험')
    let txp = 초월경험치Ref.current + Math.round(amount * 경험보주)
    let tlv = 초월레벨Ref.current
    let tDelta = 0
    while (txp >= 다음초월경험치(tlv + tDelta)) {
      txp -= 다음초월경험치(tlv + tDelta)
      tDelta++
    }
    set초월경험치(txp)
    if (tDelta > 0) {
      set초월레벨(prev => prev + tDelta)
      set초월잔여포인트(prev => prev + tDelta * 5)  // 초월레벨당 초월스텟 5
      set잔여포인트(prev => prev + tDelta * 5)        // 초월레벨당 일반스텟 5
      메시지표시(`🌀 초월레벨업! +${tDelta} (+${tDelta * 5} 초월P / +${tDelta * 5} 일반P)`)
    }
  }

  // 스탯 포인트 분배 (일반 스텟) — max/cost 메타 기반
  // count = 올리고 싶은 횟수 (1 = +1, Infinity = ALL)
  function 스탯올리기(stat: keyof 강화스텟, count = 1) {
    const meta = 일반스텟표.find(m => m.key === stat)
    if (!meta) return
    const cur = 일반스텟Ref.current[stat]
    const 가능횟수 = Math.max(0, meta.max - cur)
    if (가능횟수 === 0) { 메시지표시(`⛔ ${meta.label} MAX (${meta.max})`); return }
    const 살횟수 = Math.min(count, 가능횟수, Math.floor(잔여포인트Ref.current / meta.cost))
    if (살횟수 <= 0) { 메시지표시(`⛔ 포인트 부족 (1회 ${meta.cost}P)`); return }
    const 총비용 = 살횟수 * meta.cost
    set잔여포인트(p => p - 총비용)
    set일반스텟(prev => ({ ...prev, [stat]: prev[stat] + 살횟수 }))
  }
  // 스탯 포인트 분배 (초월 스텟, 초월잔여포인트 사용)
  function 초월스탯올리기(stat: keyof 초월스텟타입, count = 1) {
    const meta = 초월스텟표.find(m => m.key === stat)
    if (!meta) return
    const cur = 초월스텟Ref.current[stat]
    const 가능횟수 = Math.max(0, meta.max - cur)
    if (가능횟수 === 0) { 메시지표시(`⛔ ${meta.label} MAX (${meta.max})`); return }
    const 살횟수 = Math.min(count, 가능횟수, Math.floor(초월잔여포인트Ref.current / meta.cost))
    if (살횟수 <= 0) { 메시지표시(`⛔ 초월 포인트 부족 (1회 ${meta.cost}P)`); return }
    const 총비용 = 살횟수 * meta.cost
    set초월잔여포인트(p => p - 총비용)
    set초월스텟(prev => ({ ...prev, [stat]: prev[stat] + 살횟수 }))
  }

  // ============================================
  // 🌟 환생 시스템
  // ============================================
  // 트리거 조건: 50강 마린 1마리 이상 보유
  // 보상 ExPoint = (이번 환생까지 누적 생산한 50강+ 유닛 수) × EXP_마리당50강
  // 환생레벨/누적환생수/크레딧/ExPoint 외 모두 리셋, 누적50강생산은 0으로 초기화
  const EXP_마리당50강 = 100  // 밸런스 계수: 50강 판매비용(1e8크레딧=ExP1000) 대비 ~10%. 너무 약하면 올리기
  function 환생가능여부(): { ok: boolean; 이유: string } {
    const has50 = 마린들Ref.current.some(m => m.lv >= 50)
    if (!has50) return { ok: false, 이유: '50강 마린 필요' }
    return { ok: true, 이유: '' }
  }
  function 환생보상ExPoint(): number {
    return 누적50강생산Ref.current * EXP_마리당50강
  }
  function 환생실행() {
    const chk = 환생가능여부()
    if (!chk.ok) { 메시지표시(`⛔ 환생 불가: ${chk.이유}`); return }
    const 보상 = 환생보상ExPoint()
    if (typeof window !== 'undefined' && window.confirm) {
      if (!window.confirm(`환생할까요?\n\n50강+ 누적 생산 ${숫자포맷(누적50강생산Ref.current)}마리 → 보상 ⭐ ${숫자포맷(보상)} ExPoint\n\n현재 마린은 모두 초기화됩니다.\n스텟/보석/보주/초월/크레딧 등은 유지됩니다.`)) return
    }
    // 스텟/보석/보주/초월/크레딧 등은 유지, 현재 마린만 초기화. ExP 보상 지급 + 누적50강생산 정산 리셋.
    set마린들(초기마린들())
    set선택ID([])
    set현재화면('base')
    setMineral(0)            // 미네랄 0으로
    set총공격수(0)           // 타격수(총공격수) 0으로
    set타격수획득idx(0)      // 타격 마일스톤 진행도 리셋
    setExPoint(p => p + 보상)
    set누적50강생산(0)
    set환생레벨(v => v + 1)
    set누적환생수(v => v + 1)
    set환생패널열림(false)
    메시지표시(`🌟 환생! ⭐+${숫자포맷(보상)} ExPoint (환생Lv.${환생레벨 + 1})`)
  }

  // ===== 강화 카테고리별 초기화 (해당 강화에 쓴 자원/포인트 전액 환불) =====
  function 스텟초기화() {
    if (typeof window !== 'undefined' && window.confirm && !window.confirm('일반 스텟을 초기화할까요?\n\n분배한 스텟이 모두 풀리고 쓴 포인트를 전액 환불합니다.')) return
    let 환불 = 0
    const cur = 일반스텟Ref.current
    for (const m of 일반스텟표) 환불 += (cur[m.key] || 0) * m.cost
    set일반스텟({ 돈수급량: 0, 유닛공업: 0, 가산1강: 0, 가산2강: 0, 가산3강: 0, 특수강화: 0, 가산1강2: 0, 가산2강2: 0, 가산3강2: 0, 특수강화2: 0, 특수파괴방지: 0, 특수파괴방지2: 0, 가산44강: 0, 가산45강: 0, 가산46강: 0, 가산47강: 0, 가산48강: 0, 촉진: 0 })
    set잔여포인트(p => p + 환불)
    메시지표시(`📊 스텟 초기화 — ${숫자포맷(환불)}P 환불`)
  }
  function 초월스텟초기화() {
    if (typeof window !== 'undefined' && window.confirm && !window.confirm('초월 스텟을 초기화할까요?\n\n분배한 초월 스텟이 모두 풀리고 쓴 초월 포인트를 전액 환불합니다.')) return
    let 환불 = 0
    const cur = 초월스텟Ref.current
    for (const m of 초월스텟표) 환불 += ((cur as any)[m.key] || 0) * m.cost
    set초월스텟({ ...초기초월스텟 })
    set초월잔여포인트(p => p + 환불)
    메시지표시(`🌀 초월 스텟 초기화 — ${숫자포맷(환불)}P 환불`)
  }
  function 보석초기화() {
    if (typeof window !== 'undefined' && window.confirm && !window.confirm('보석을 초기화할까요?\n\n모든 보석이 0이 되고 쓴 무색조각을 전액 환불합니다.')) return
    let 환불 = 0
    const cur = 보석Ref.current
    for (const 종류 of Object.keys(초기보석) as 보석타입[]) {
      const n = cur[종류] || 0
      for (let i = 0; i < n; i++) 환불 += 보석현재비용(종류, i)
    }
    set보석({ ...초기보석 })
    set무색조각(p => p + 환불)
    메시지표시(`💠 보석 초기화 — 🔷${숫자포맷(환불)} 환불`)
  }
  function 보주초기화() {
    if (typeof window !== 'undefined' && window.confirm && !window.confirm('보주를 초기화할까요?\n\n모든 보주가 0이 되고 쓴 응무조를 전액 환불합니다.')) return
    let 환불 = 0
    const cur = 보주Ref.current
    for (const 종류 of Object.keys(초기보주) as 보주타입[]) 환불 += (cur[종류] || 0) * 보주구입비용[종류]
    set보주({ ...초기보주 })
    set응무조(p => p + 환불)
    메시지표시(`🔮 보주 초기화 — 💠${숫자포맷(환불)} 환불`)
  }

  // 보주 구입 (응무조 사용, 수량 지정)
  function 보주구입(종류: 보주타입, 수량: number) {
    const 단가 = 보주구입비용[종류]
    const max = 보주max[종류]
    const 현재 = 보주Ref.current[종류]
    const 가능수max = Math.max(0, max - 현재)
    const 실수량 = Math.min(수량, 가능수max)
    if (실수량 < 1) { 메시지표시(`⛔ ${종류} 보주 MAX (${max})`); return }
    const 총비용 = 단가 * 실수량
    if (응무조Ref.current < 총비용) {
      메시지표시(`💠 응무조 ${숫자포맷(총비용)} 필요 (보유 ${숫자포맷(응무조Ref.current)})`)
      return
    }
    set응무조(prev => prev - 총비용)
    set보주(prev => ({ ...prev, [종류]: prev[종류] + 실수량 }))
  }
  function 보주최대구입(종류: 보주타입) {
    const 단가 = 보주구입비용[종류]
    const max = 보주max[종류]
    const 현재 = 보주Ref.current[종류]
    const 가능수max = Math.max(0, max - 현재)
    if (가능수max < 1) { 메시지표시(`⛔ ${종류} 보주 MAX (${max})`); return }
    const 가능수 = Math.min(가능수max, Math.floor(응무조Ref.current / 단가))
    if (가능수 < 1) { 메시지표시(`💠 응무조 부족`); return }
    set응무조(prev => prev - 단가 * 가능수)
    set보주(prev => ({ ...prev, [종류]: prev[종류] + 가능수 }))
    메시지표시(`⚔️ ${종류} 보주 +${가능수}`)
  }

  // 보석 구입 (무색조각 사용) — max 체크 + 동적 비용
  function 보석구입(종류: 보석타입) {
    const 현재 = 보석Ref.current[종류]
    if (현재 >= 보석max[종류]) { 메시지표시(`⛔ ${종류} MAX (${보석max[종류]})`); return }
    const 비용 = 보석현재비용(종류, 현재)
    if (무색조각Ref.current < 비용) { 메시지표시(`🔷 무색조각 ${숫자포맷(비용)} 필요`); return }
    set무색조각(prev => prev - 비용)
    set보석(prev => ({ ...prev, [종류]: prev[종류] + 1 }))
  }
  // N개 일괄 구입 (가능한 만큼)
  function 보석구입N(종류: 보석타입, 횟수: number) {
    let 보유 = 보석Ref.current[종류]
    let 잔여 = 무색조각Ref.current
    const max = 보석max[종류]
    let 산횟수 = 0, 총비용 = 0
    for (let i = 0; i < 횟수; i++) {
      if (보유 >= max) break
      const cost = 보석현재비용(종류, 보유)
      if (잔여 < cost) break
      잔여 -= cost; 총비용 += cost; 보유++; 산횟수++
    }
    if (산횟수 === 0) { 메시지표시(`🔷 무색조각 ${숫자포맷(보석현재비용(종류, 보유))} 필요`); return }
    set무색조각(prev => prev - 총비용)
    set보석(prev => ({ ...prev, [종류]: prev[종류] + 산횟수 }))
  }

  function 보석연속시작(종류: 보석타입) {
    보석구입(종류)
    보석연속타이머Ref.current = setInterval(() => 보석구입(종류), 200)
  }
  function 보석연속종료() {
    if (보석연속타이머Ref.current) {
      clearInterval(보석연속타이머Ref.current)
      보석연속타이머Ref.current = null
    }
  }

  // ExP → 크레딧 변환 (보유량 비율로 한번에 환전)
  const EXP_크레딧환율 = 100000
  function ExP를크레딧으로(비율: number) {
    const 변환ExP = Math.floor(ExPointRef.current * 비율)
    if (변환ExP < 1) { 메시지표시('⭐ 변환할 ExP가 부족합니다'); return }
    const 획득크레딧 = 변환ExP * EXP_크레딧환율
    setExPoint(p => p - 변환ExP)
    set크레딧(p => p + 획득크레딧)
    메시지표시(`⭐${숫자포맷(변환ExP)} → 💰${숫자포맷(획득크레딧)} 크레딧`)
  }

  // ExP 뽑기: 1뽑기당 EXP_뽑기비용 ExP. 기본 무색조각 50 + 확률 추가지급. 배수(횟수) 지원.
  const EXP_뽑기비용 = 1
  function ExP뽑기(횟수: number) {
    const cost = 횟수 * EXP_뽑기비용
    if (ExPointRef.current < cost) { 메시지표시(`⭐ ExP ${숫자포맷(cost)} 필요`); return }
    let 무색 = 0, 크레딧드 = 0, 각성 = 0
    for (let i = 0; i < 횟수; i++) {
      무색 += 50  // 기본 지급
      if (Math.random() < 0.0005) 크레딧드 += 600000000  // 크레딧 6억 0.05%
      if (Math.random() < 0.0015) 각성 += 1               // 각성석 1개 0.15%
      if (Math.random() < 0.01) 무색 += 20000             // 무색 2만 1%
      if (Math.random() < 0.10) 무색 += 500               // 무색 500 10%
    }
    setExPoint(p => p - cost)
    if (무색 > 0) set무색조각(p => p + 무색)
    if (크레딧드 > 0) set크레딧(p => p + 크레딧드)
    if (각성 > 0) set각성의보석(p => p + 각성)
    메시지표시(`🎰 ${숫자포맷(횟수)}뽑기! 🔷${숫자포맷(무색)}${크레딧드 > 0 ? ` 💰${숫자포맷(크레딧드)}` : ''}${각성 > 0 ? ` 💎${각성}각성석` : ''}`)
  }

  // 시간제 쿠폰 구입: 1개당 +1시간, 최대 60시간 누적
  const 쿠폰최대ms = 60 * 3600000
  function 쿠폰구입(종류: '경험' | '재화') {
    const 비용 = 종류 === '경험' ? 1 : 50
    if (ExPointRef.current < 비용) { 메시지표시(`⭐ ExP ${비용} 필요`); return }
    const now = Date.now()
    const 현재만료 = 종류 === '경험' ? 경험쿠폰만료Ref.current : 재화쿠폰만료Ref.current
    const base = Math.max(now, 현재만료)
    if (base + 3600000 - now > 쿠폰최대ms) { 메시지표시('⛔ 최대 60시간까지'); return }
    setExPoint(p => p - 비용)
    const 새만료 = base + 3600000
    if (종류 === '경험') set경험쿠폰만료(새만료); else set재화쿠폰만료(새만료)
    메시지표시(`${종류 === '경험' ? '📗 경험치 3배' : '💰 재화 2배'} 쿠폰 +1시간`)
  }

  // 고유유닛 강화 (크레딧 사용)
  const 고유유닛강화비용표: Record<keyof Omit<고유유닛스텟, '위치' | '단수'>, (lv: number) => number> = {
    공격력:   lv => (lv + 1) * 100,
    공속:     lv => (lv + 1) * 200,
    경험치:   lv => (lv + 1) * 150,
    추가1강:  lv => (lv + 1) * 80,
    특수강화: lv => (lv + 1) * 250,
    파괴방지: lv => (lv + 1) * 50,
  }
  const 고유유닛상한: Record<keyof Omit<고유유닛스텟, '위치' | '단수'>, number> = {
    공격력: 20, 공속: 6, 경험치: 5, 추가1강: 20, 특수강화: 10, 파괴방지: 200,
  }
  function 고유유닛강화(stat: keyof Omit<고유유닛스텟, '위치' | '단수'>) {
    const lv = 고유유닛Ref.current[stat] as number
    if (lv >= 고유유닛상한[stat]) { 메시지표시('MAX'); return }
    const 비용 = 고유유닛강화비용표[stat](lv)
    if (크레딧Ref.current < 비용) { 메시지표시(`💰 크레딧 ${비용} 필요`); return }
    set크레딧(prev => prev - 비용)
    set고유유닛(prev => ({ ...prev, [stat]: (prev[stat] as number) + 1 }))
  }
  // MAX까지 가능한 만큼 일괄 강화
  function 고유유닛강화MAX(stat: keyof Omit<고유유닛스텟, '위치' | '단수'>) {
    let lv = 고유유닛Ref.current[stat] as number
    const 상한 = 고유유닛상한[stat]
    let 잔여 = 크레딧Ref.current
    let 산횟수 = 0, 총비용 = 0
    while (lv < 상한) {
      const c = 고유유닛강화비용표[stat](lv)
      if (잔여 < c) break
      잔여 -= c; 총비용 += c; lv++; 산횟수++
    }
    if (산횟수 < 1) { 메시지표시(`💰 크레딧 부족`); return }
    set크레딧(prev => prev - 총비용)
    set고유유닛(prev => ({ ...prev, [stat]: (prev[stat] as number) + 산횟수 }))
    메시지표시(`${stat} +${산횟수} (💰${숫자포맷(총비용)})`)
  }
  // 고유유닛 사냥터 1 고정 (사2/사3 진입 X)
  function 고유유닛위치변경() {
    // 수동 cycle 폐기 (자동 라우팅으로 대체) — 호출시 안내만
    메시지표시('📍 고유유닛 사냥터 1 고정')
  }
  // 단수업 비용: n→n+1 = n개 각성의돌 (각성의보석 자원 사용)
  function 단수업비용(현재단수: number): number { return 현재단수 }
  function 단수업가능(u: 고유유닛스텟): boolean {
    return u.공격력 >= 20 && u.공속 >= 6 && u.경험치 >= 5 && u.추가1강 >= 20 && u.특수강화 >= 10 && u.파괴방지 >= 200
  }
  // 단수 배율: 1.1^(n-1) (1단=1, 2단=1.1, 3단=1.21, ...)
  function 단수배율(단수: number): number {
    return Math.pow(1.1, Math.max(0, 단수 - 1))
  }
  // 크리스탈 레벨업 (같은 종류 N개 소모 → 레벨 +1)
  function 크리스탈레벨업(key: keyof 명칭크리스탈목록) {
    const 등급 = 크리스탈등급맵[key]
    const max = 크리스탈등급max[등급]
    const curLv = 명칭크리스탈Lv[key] ?? 0
    // 첫 획득(보유는 있는데 lv 0)은 자동 lv 1로 표시. 이후 lv→lv+1 비용 소모
    if (curLv === 0) {
      if (명칭크리스탈Ref.current[key] < 1) { 메시지표시('보유 없음 — 박스로 획득'); return }
      set명칭크리스탈Lv(prev => ({ ...prev, [key]: 1 }))
      메시지표시(`🌟 ${key} Lv.1 활성화`)
      return
    }
    if (curLv >= max) { 메시지표시(`⛔ Lv.${max} MAX`); return }
    const cost = 크리스탈레벨업비용(등급, curLv)
    if (명칭크리스탈Ref.current[key] < cost) { 메시지표시(`재료 부족 (필요 ${숫자포맷(cost)}개)`); return }
    set명칭크리스탈(prev => ({ ...prev, [key]: (prev[key] as number) - cost }))
    set명칭크리스탈Lv(prev => ({ ...prev, [key]: curLv + 1 }))
    메시지표시(`✨ ${key} Lv.${curLv} → ${curLv + 1}`)
  }

  // 크리스탈 제작 (퀘이사: 상급조각 / 그 외: 일반 크리스탈조각)
  function 크리스탈제작(key: keyof 명칭크리스탈목록) {
    const 등급 = 크리스탈등급맵[key]
    const cost = 크리스탈제작비용[등급]
    if (!cost) { 메시지표시(`⛔ ${등급} 제작은 추후 추가`); return }
    const isQuasar = 등급 === '퀘이사'
    const 잔여 = isQuasar ? 상급크리스탈조각Ref.current : 크리스탈조각Ref.current
    if (잔여 < cost) { 메시지표시(`${isQuasar ? '🟣 상급조각' : '🔮 조각'} 부족 (필요 ${숫자포맷(cost)})`); return }
    if (isQuasar) set상급크리스탈조각(prev => prev - cost)
    else set크리스탈조각(prev => prev - cost)
    set명칭크리스탈(prev => ({ ...prev, [key]: (prev[key] as number) + 1 }))
    메시지표시(`🛠️ ${key} +1 제작`)
  }

  // 단순 화면 전환 (탭)
  function 베이스탭() { set현재화면('base'); set선택ID([]) }
  function 사냥터탭() { set현재화면('hunting1'); set선택ID([]) }

  // 판매소 zone 안 도달한 선택 마린 강제 판매 (수동)
  function 판매() {
    const sel = 마린들Ref.current.filter(m => 선택IDRef.current.includes(m.id))
    if (sel.length === 0) { 메시지표시('판매할 마린 선택'); return }
    // 선택 마린을 판매소로 이동시킴
    set마린들(prev => prev.map(m =>
      선택IDRef.current.includes(m.id) && m.location === 'base'
        ? { ...m, state: 'move' as 유닛상태, dest: { x: ZONE_판매소.x + ZONE_판매소.w / 2, y: ZONE_판매소.y + ZONE_판매소.h / 2 } }
        : m
    ))
    메시지표시(`🛒 판매소로 이동 (${sel.length}마리)`)
  }



  function 유닛구매(강도: number, 수량: number = 1) {
    if (강도 > 50) { 메시지표시('🚫 구입은 50강까지만 가능'); return }
    const 단가 = 생산비용(강도)
    const 총마린수 = 마린들Ref.current.length
    const 최대유닛수 = 5000
    if (총마린수 >= 최대유닛수) { 메시지표시(`🚫 마린 가득참 (총 ${최대유닛수} 최대)`); return }
    const 여유 = 최대유닛수 - 총마린수
    const 가능자원 = Math.floor(mineralRef.current / 단가)
    const 실수량 = Math.min(수량, 여유, 가능자원)
    if (실수량 < 1) {
      메시지표시(`⚠️ 자원/슬롯 부족 (${숫자포맷(단가)}/마리)`)
      return
    }
    const 총비용 = 단가 * 실수량
    setMineral(prev => prev - 총비용)
    set마린들(prev => {
      let bc = prev.filter(m => m.location === 'base').length
      const 추가: 마린[] = []
      for (let i = 0; i < 실수량; i++) {
        추가.push(새마린(강도, 베이스시작위치(bc + i), 'base'))
      }
      return [...prev, ...추가]
    })
    메시지표시(`✓ ${강도}강 마린 ×${실수량} 생산!`)
  }

  // 고유유닛 사냥터 1 고정 (자동 라우팅 제거)
  useEffect(() => {
    if (고유유닛.위치 !== 1) set고유유닛(prev => ({ ...prev, 위치: 1 }))
  }, [고유유닛.위치])

  // 30만 레벨 도달 시 초월레벨 자동 1렙 (초월시스템 unlock)
  useEffect(() => {
    if (캐릭레벨 >= 캐릭레벨최대 && 초월레벨 === 0) {
      set초월레벨(1)
      set초월잔여포인트(p => p + 5)  // 초월레벨당 초월스텟 5
      set잔여포인트(p => p + 5)        // 초월레벨당 일반스텟 5
      메시지표시('🌀 30만 레벨 도달! 초월레벨 1 활성화 (+5 초월P / +5 일반P)')
    }
  }, [캐릭레벨, 초월레벨])

  // 자동화 보석 → 자동구입배수 자동 동기화 (1 + 자동화보석 수, cap 10)
  useEffect(() => {
    const 목표 = Math.min(10, 1 + 보석.자동화)
    if (자동구입배수 < 목표) set자동구입배수(목표)
  }, [보석.자동화])

  // 🌟 고유유닛 자동 단수업 (모든 강화 max + 각성의돌(=각성의보석) 보유시)
  useEffect(() => {
    if (!단수업가능(고유유닛)) return
    let stones = 각성의보석
    let nextDan = 고유유닛.단수
    let consumed = 0
    while (stones >= nextDan) {
      stones -= nextDan
      consumed += nextDan
      nextDan += 1
    }
    if (nextDan > 고유유닛.단수) {
      set각성의보석(s => s - consumed)
      set고유유닛(prev => ({ ...prev, 단수: nextDan }))
      메시지표시(`🌟 고유유닛 각성! ${고유유닛.단수}단 → ${nextDan}단 (💎-${consumed})`)
    }
  }, [고유유닛, 각성의보석])

  // 키보드 (PC 전용)
  // ============================================
  useEffect(() => {
    if (Platform.OS !== 'web') return
    const handler = (e: KeyboardEvent) => {
      const k = e.key.toLowerCase()
      if (선택IDRef.current.length === 0 && (k === 'h' || k === 's')) return
      if (k === 'h') hold명령()
      if (k === 's') 정지명령()
      if (k === 'escape') set선택ID([])
    }
    document.addEventListener('keydown', handler)
    return () => document.removeEventListener('keydown', handler)
  }, [])

  // ============================================
  // 우클릭 비활성화
  // ============================================
  useEffect(() => {
    if (Platform.OS !== 'web') return
    const el = fieldRef.current
    if (!el) return
    const ctx = (e: any) => e.preventDefault()
    const wheel = (e: any) => e.preventDefault()
    const touchmove = (e: any) => e.preventDefault()
    el.addEventListener('contextmenu', ctx)
    el.addEventListener('wheel', wheel, { passive: false })
    el.addEventListener('touchmove', touchmove, { passive: false })
    return () => {
      el.removeEventListener('contextmenu', ctx)
      el.removeEventListener('wheel', wheel)
      el.removeEventListener('touchmove', touchmove)
    }
  }, [현재화면])

  // ============================================
  // 터치 핸들러
  // ============================================
  function findUnitAt(p: Pos) {
    return 마린들Ref.current.find(m =>
      m.location === 현재화면Ref.current &&
      거리(m.pos, p) < 마린_크기 / 2 + 10
    )
  }
  function findEnemyAt(p: Pos) {
    return 적들Ref.current.find(e => 거리(e.pos, p) < 적_크기 / 2 + 10)
  }

  // 화면맞춤 scale 적용 시 locationX/Y는 화면(scaled) 좌표 → 필드 논리좌표로 환산
  // 웹: CSS zoom 적용 시 locationX/Y는 zoom된(화면) 좌표 → ÷배율로 필드 논리좌표 환산.
  // 네이티브: transform scale은 locationX에 영향 없음 + 배율 항상 1이므로 보정 안 함(÷1).
  const 터치좌표 = (e: any): Pos => {
    // 웹: 필드의 실제 렌더 rect로 직접 환산 → CSS zoom/transform/스크롤 전부 무관하게 정확.
    if (Platform.OS === 'web') {
      const el = fieldRef.current
      if (el && typeof el.getBoundingClientRect === 'function') {
        const rect = el.getBoundingClientRect()
        if (rect.width > 0) {
          const scale = rect.width / 필드_W  // 실제 시각 배율
          const sx = (typeof window !== 'undefined' ? (window.scrollX || window.pageXOffset || 0) : 0)
          const sy = (typeof window !== 'undefined' ? (window.scrollY || window.pageYOffset || 0) : 0)
          const cx = (e.nativeEvent.pageX ?? e.nativeEvent.clientX ?? 0) - sx
          const cy = (e.nativeEvent.pageY ?? e.nativeEvent.clientY ?? 0) - sy
          return { x: (cx - rect.left) / scale, y: (cy - rect.top) / scale }
        }
      }
    }
    return { x: e.nativeEvent.locationX, y: e.nativeEvent.locationY }
  }
  const fieldResponderProps = {
    onStartShouldSetResponder: () => true,
    onMoveShouldSetResponder: () => true,
    onResponderTerminationRequest: () => false,
    onResponderGrant: (e: any) => {
      const p: Pos = 터치좌표(e)
      dragStartRef.current = p
      터치이동중Ref.current = false
    },
    onResponderMove: (e: any) => {
      if (!dragStartRef.current) return
      const cur: Pos = 터치좌표(e)
      const start = dragStartRef.current
      const d = Math.hypot(start.x - cur.x, start.y - cur.y)
      if (d > 8) 터치이동중Ref.current = true
      if (터치이동중Ref.current) {
        set드래그박스({
          x1: Math.min(start.x, cur.x),
          y1: Math.min(start.y, cur.y),
          x2: Math.max(start.x, cur.x),
          y2: Math.max(start.y, cur.y),
        })
      }
    },
    onResponderRelease: (e: any) => {
      if (!dragStartRef.current) return
      const start = dragStartRef.current
      const end: Pos = 터치좌표(e)
      const wasDragging = 터치이동중Ref.current
      dragStartRef.current = null
      터치이동중Ref.current = false
      set드래그박스(null)

      if (!wasDragging) {
        const 마린 = findUnitAt(end)
        const 적 = findEnemyAt(end)
        const screen = 현재화면Ref.current

        // 0. 고유유닛 탭 체크 (사냥터 화면)
        if (is사냥터(screen)) {
          if (거리(end, 고유유닛posRef.current) < 마린_크기 / 2 + 10) {
            // 클릭 시 강화창 안 열고 선택 토글만
            if (고유유닛선택Ref.current) {
              set고유유닛선택(false)
            } else {
              set고유유닛선택(true)
              set선택ID([])
            }
            return
          }
          // 고유유닛 선택 + 빈 곳 탭 = 이동 + 자동 해제 (마린 명령 회복)
          // 단, 마린 탭이면 아래 처리로 위임 (해제 후 마린 선택)
          if (고유유닛선택Ref.current && !마린 && !적) {
            set고유유닛dest(clampPosStatic(end))
            set고유유닛선택(false)
            return
          }
          // 마린/적 탭 시 고유유닛 선택 해제 후 일반 처리
          if (고유유닛선택Ref.current) {
            set고유유닛선택(false)
          }
        }

        // 1. 마린 탭 = 선택 (더블탭 = 같은 강도 전체)
        if (마린) {
          const tapNow = Date.now()
          const last = lastTapRef.current
          if (last && last.id === 마린.id && tapNow - last.time < 350) {
            // 더블탭 → 같은 강도 마린 전체 선택
            const sameLv = 마린들Ref.current.filter(m =>
              m.location === 현재화면Ref.current && m.lv === 마린.lv
            )
            set선택ID(sameLv.map(m => m.id))
            lastTapRef.current = null
          } else {
            set선택ID([마린.id])
            lastTapRef.current = { id: 마린.id, time: tapNow }
          }
          return
        }
        // 2. 선택O + 적 = 공격
        if (선택IDRef.current.length > 0 && 적 && is사냥터(screen)) {
          공격명령(적.id)
          return
        }
        // 4. 선택O + 빈곳 = 이동
        if (선택IDRef.current.length > 0) {
          이동명령(end)
          return
        }
      } else {
        const x1 = Math.min(start.x, end.x)
        const y1 = Math.min(start.y, end.y)
        const x2 = Math.max(start.x, end.x)
        const y2 = Math.max(start.y, end.y)
        const inBox = 마린들Ref.current.filter(m =>
          m.location === 현재화면Ref.current && 점이사각형안에(m.pos, x1, y1, x2, y2)
        )
        set선택ID(inBox.map(m => m.id))
        // 사냥터 화면에서 드래그 박스가 고유유닛 위치 덮으면 고유유닛도 같이 선택
        const screen = 현재화면Ref.current
        if (is사냥터(screen as 화면) && 점이사각형안에(고유유닛posRef.current, x1, y1, x2, y2)) {
          set고유유닛선택(true)
        } else {
          set고유유닛선택(false)
        }
      }
    },
  }

  function 게임초기화() {
    if (typeof window !== 'undefined' && window.confirm) {
      if (!window.confirm('정말 처음부터 시작할까요?')) return
    }
    set마린들(초기마린들())
    setMineral(100)
    set총공격수(0)
    set보스처치수(0)
    set최고DPS(0)
    set무색조각(10000); set응무조(0); set크리스탈조각(0); set상급크리스탈조각(0)
    set보주({ ...초기보주 })
    set업그레이드({ 공격력: 0, 자원: 0, 강화확률: 0, 이속: 0, 공속: 0 })
    set캐릭레벨(1); set경험치(0); set잔여포인트(0)
    set일반스텟({ 돈수급량: 0, 유닛공업: 0, 가산1강: 0, 가산2강: 0, 가산3강: 0, 특수강화: 0, 가산1강2: 0, 가산2강2: 0, 가산3강2: 0, 특수강화2: 0, 특수파괴방지: 0, 특수파괴방지2: 0, 가산44강: 0, 가산45강: 0, 가산46강: 0, 가산47강: 0, 가산48강: 0, 촉진: 0 })
    set초월스텟({ ...초기초월스텟 })
    set각성의보석(0); setExPoint(0); set은하조각(0); set자각보주(0)
    set타격수획득idx(0); setExtraVI받음(0); setExtraXI받음(0)
    set명칭크리스탈({ ...초기명칭크리스탈 })
    set명칭크리스탈Lv({})
    set장착크리스탈([])
    set크레딧(0)
    set보석({ ...초기보석 })
    set고유유닛({ ...초기고유유닛 })
    set초월레벨(0)
    set초월잔여포인트(0)
    set초월경험치(0)
    set누적강화성공(0); set누적판매(0); set최고마린lv(1); set융합누적(0)
    set환생레벨(0); set누적환생수(0); set누적50강생산(0)
    set보주패널열림(false); set강화패널열림(false); set명칭크리스탈패널열림(false)
    set보석패널열림(false); set고유유닛패널열림(false); set환생패널열림(false)
    set몹들(초기몹들())
    set자동강화ON(false); set자동강화최대lv(1)
    set자동판매ON(false); set자동판매lv(50)
    set자동구입강도(1); set자동구입ON(false)
    set적들(초기적들(1))
    set선택ID([])
    set현재화면('base')
    set생산패널열림(false)
    메시지표시('🔄 초기화됨')
  }

  // ============================================
  // 렌더
  // ============================================
  const now = Date.now()

  return (
    <SafeAreaView style={{ flex: 1, backgroundColor: '#1a1a2e' }} edges={['top']}>
    <ScrollView
      contentContainerStyle={[styles.container, 화면맞춤스타일]}
      bounces={false}
      overScrollMode="never"
      showsVerticalScrollIndicator={false}
    >
      <Text style={styles.title}>DPS 강화하기 ⚔️ RTS</Text>

      <View style={styles.statBox}>
        <View style={styles.statRow}>
          <Text style={styles.stat} numberOfLines={1}>💎 {숫자포맷(mineral)}</Text>
          <TouchableOpacity onPress={() => {
            const next = 타격마일스톤표[타격수획득idx]
            if (!next) { 메시지표시('🏆 타격수 마일스톤 전부 완료'); return }
            const r = next.보상
            const parts: string[] = []
            if (r.ExP) parts.push(`⭐${숫자포맷(r.ExP)}`)
            if (r.조각) parts.push(`🔮${r.조각}`)
            if (r.크리스탈박스.length) parts.push(`🌟${r.크리스탈박스.length}박스(${r.크리스탈박스[0]})`)
            if (r.은하조각) parts.push(`🌌${r.은하조각}`)
            if (r.크레딧) parts.push(`💰${숫자포맷(r.크레딧)}`)
            if (r.상급조각) parts.push(`🟣${r.상급조각}`)
            메시지표시(`🎯 다음: ${next.라벨}회 (${숫자포맷(총공격수)}/${숫자포맷(next.임계값)}) → ${parts.join(' ')}`)
          }}>
            <Text style={styles.statSmall}>🎯 {숫자포맷(총공격수)}</Text>
          </TouchableOpacity>
          <Text style={[styles.statSmall, { color: 잔여포인트 > 0 ? '#f5a623' : '#aaa' }]}>Lv.{캐릭레벨}{잔여포인트 > 0 ? ` (+${잔여포인트}P)` : ''}</Text>
        </View>
        <View style={[styles.statRow, { marginTop: 4 }]}>
          <TouchableOpacity onPress={() => set재화패널열림(v => {
            const next = !v
            if (next) { set도움말패널열림(false); set정보패널열림(false); set생산패널열림(false); set상점패널열림(false); set자동패널열림(false); set보주패널열림(false); set강화패널열림(false); set명칭크리스탈패널열림(false); set보석패널열림(false); set고유유닛패널열림(false); set환생패널열림(false) }
            return next
          })} style={{ paddingHorizontal: 8, paddingVertical: 2, backgroundColor: '#3a5a8a', borderRadius: 4 }}>
            <Text style={[styles.statSmall, { color: '#fff' }]}>💰 재화</Text>
          </TouchableOpacity>
          <TouchableOpacity onPress={() => set도움말패널열림(v => {
            const next = !v
            if (next) { set정보패널열림(false); set재화패널열림(false); set생산패널열림(false); set상점패널열림(false); set자동패널열림(false); set보주패널열림(false); set강화패널열림(false); set명칭크리스탈패널열림(false); set보석패널열림(false); set고유유닛패널열림(false); set환생패널열림(false) }
            return next
          })} style={{ paddingHorizontal: 8, paddingVertical: 2, backgroundColor: '#5a3a8a', borderRadius: 4 }}>
            <Text style={[styles.statSmall, { color: '#fff' }]}>📖 도움말</Text>
          </TouchableOpacity>
          <TouchableOpacity onPress={() => set정보패널열림(v => {
            const next = !v
            if (next) { set도움말패널열림(false); set재화패널열림(false); set생산패널열림(false); set상점패널열림(false); set자동패널열림(false); set보주패널열림(false); set강화패널열림(false); set명칭크리스탈패널열림(false); set보석패널열림(false); set고유유닛패널열림(false); set환생패널열림(false) }
            return next
          })} style={{ paddingHorizontal: 8, paddingVertical: 2, backgroundColor: '#3a7a5a', borderRadius: 4 }}>
            <Text style={[styles.statSmall, { color: '#fff' }]}>📊 정보</Text>
          </TouchableOpacity>
          <TouchableOpacity onPress={() => set상점패널열림(v => {
            const next = !v
            if (next) { set재화패널열림(false); set도움말패널열림(false); set정보패널열림(false); set생산패널열림(false); set자동패널열림(false); set보주패널열림(false); set강화패널열림(false); set명칭크리스탈패널열림(false); set보석패널열림(false); set고유유닛패널열림(false); set환생패널열림(false) }
            return next
          })} style={{ paddingHorizontal: 8, paddingVertical: 2, backgroundColor: '#8a5a2a', borderRadius: 4 }}>
            <Text style={[styles.statSmall, { color: '#fff' }]}>🛒 상점</Text>
          </TouchableOpacity>
        </View>
      </View>

      {재화패널열림 && (
        <View style={styles.currencyPanel}>
          <View style={styles.currencyHeader}>
            <Text style={styles.currencyTitle}>💰 재화</Text>
            <TouchableOpacity onPress={() => set재화패널열림(false)}>
              <Text style={styles.closeBtn}>✕</Text>
            </TouchableOpacity>
          </View>
          <ScrollView style={{ maxHeight: 화면H - 420 }}>
          <View style={styles.currencyGrid}>
            <Text style={[styles.currencyItem, { color: '#f5a623' }]}>📊 일반P {숫자포맷(잔여포인트)}</Text>
            <Text style={[styles.currencyItem, { color: '#a855f7' }]}>🌀 초월P {숫자포맷(초월잔여포인트)}</Text>
            <Text style={[styles.currencyItem, { color: '#f5a623' }]}>💰 크레딧 {숫자포맷(크레딧)}</Text>
            <Text style={[styles.currencyItem, { color: '#a855f7' }]}>⭐ ExP {숫자포맷(ExPoint)}</Text>
            <Text style={[styles.currencyItem, { color: '#4ec9ff' }]}>🔷 무색조각 {숫자포맷(무색조각)}</Text>
            <Text style={[styles.currencyItem, { color: '#7ed957' }]}>💠 응무조 {숫자포맷(응무조)}</Text>
            <Text style={[styles.currencyItem, { color: '#ff6ad9' }]}>💎 각성석 {숫자포맷(각성의보석)}</Text>
            <Text style={[styles.currencyItem, { color: '#9b59b6' }]}>🔮 크리스탈 {숫자포맷(크리스탈조각)}</Text>
            <Text style={[styles.currencyItem, { color: '#c44dff' }]}>🟣 상급조각 {숫자포맷(상급크리스탈조각)}</Text>
            {은하조각 > 0 && <Text style={[styles.currencyItem, { color: '#4ec9ff' }]}>🌌 은하 {숫자포맷(은하조각)}</Text>}
            {자각보주 > 0 && <Text style={[styles.currencyItem, { color: '#f5a623' }]}>🔯 자각 {숫자포맷(자각보주)}</Text>}
            {초월레벨 > 0 && <Text style={[styles.currencyItem, { color: '#c89bff' }]}>🌀 초월Lv.{초월레벨} ({숫자포맷(초월경험치)}/{숫자포맷(다음초월경험치(초월레벨))})</Text>}
            {환생레벨 > 0 && <Text style={[styles.currencyItem, { color: '#ff6ad9' }]}>🌟 환생Lv.{환생레벨}</Text>}
          </View>
          <Text style={{ color: '#888', fontSize: 10, marginTop: 6 }}>💡 ExP 교환·뽑기·쿠폰은 🛒 상점에서</Text>
          </ScrollView>
        </View>
      )}

      {상점패널열림 && (() => {
        const now = Date.now()
        const 경험남음 = Math.max(0, 경험쿠폰만료 - now)
        const 재화남음 = Math.max(0, 재화쿠폰만료 - now)
        const 시간표시 = (ms: number) => ms <= 0 ? '비활성' : `${Math.floor(ms / 3600000)}시간 ${Math.floor((ms % 3600000) / 60000)}분`
        return (
          <View style={styles.currencyPanel}>
            <View style={styles.currencyHeader}>
              <Text style={styles.currencyTitle}>🛒 상점</Text>
              <TouchableOpacity onPress={() => set상점패널열림(false)}>
                <Text style={styles.closeBtn}>✕</Text>
              </TouchableOpacity>
            </View>
            <Text style={[styles.currencyItem, { color: '#a855f7' }]}>⭐ ExP {숫자포맷(ExPoint)}</Text>
            <ScrollView style={{ maxHeight: 화면H - 360 }}>
              <Text style={styles.currencySection}>⭐ ExP → 💰 크레딧 (1 : {숫자포맷(EXP_크레딧환율)})</Text>
              <View style={styles.currencyBtnRow}>
                {([['25%', 0.25], ['50%', 0.5], ['전부', 1]] as const).map(([라벨, 비율]) => {
                  const 변환ExP = Math.floor(ExPoint * 비율)
                  return (
                    <TouchableOpacity key={라벨} style={[styles.upgBtn, 변환ExP < 1 && styles.upgBtnOff, { minWidth: 100, paddingHorizontal: 6 }]} onPress={() => ExP를크레딧으로(비율)}>
                      <Text style={[styles.upgBtnText, { fontSize: 12 }]}>{라벨}</Text>
                      <Text style={[styles.upgBtnText, { fontSize: 9, color: '#cdd' }]}>⭐{숫자포맷(변환ExP)} → 💰{숫자포맷(변환ExP * EXP_크레딧환율)}</Text>
                    </TouchableOpacity>
                  )
                })}
              </View>

              <Text style={styles.currencySection}>🎰 ExP 뽑기 (1뽑기 = ⭐{EXP_뽑기비용})</Text>
              <Text style={{ color: '#888', fontSize: 9, marginBottom: 3 }}>기본 🔷무색 50 + 확률: 💰6억(0.05%) · 💎각성석(0.15%) · 🔷2만(1%) · 🔷500(10%)</Text>
              <View style={styles.currencyBtnRow}>
                {[1, 10, 100, 1000].map(n => {
                  const cost = n * EXP_뽑기비용
                  return (
                    <TouchableOpacity key={n} style={[styles.upgBtn, ExPoint < cost && styles.upgBtnOff, { minWidth: 70, paddingHorizontal: 6 }]} onPress={() => ExP뽑기(n)}>
                      <Text style={[styles.upgBtnText, { fontSize: 12 }]}>×{n}</Text>
                      <Text style={[styles.upgBtnText, { fontSize: 9, color: '#cdd' }]}>⭐{숫자포맷(cost)}</Text>
                    </TouchableOpacity>
                  )
                })}
              </View>

              <Text style={styles.currencySection}>📗 경험치 3배 쿠폰 (15~44강 판매 XP)</Text>
              <Text style={{ color: 경험남음 > 0 ? '#7ed957' : '#888', fontSize: 11, marginBottom: 3 }}>남은 시간: {시간표시(경험남음)} · 1개 = ⭐1, +1시간 (최대 60시간)</Text>
              <TouchableOpacity style={[styles.upgBtn, (ExPoint < 1 || 경험남음 >= 쿠폰최대ms - 1000) && styles.upgBtnOff, { alignSelf: 'flex-start', paddingHorizontal: 14 }]} onPress={() => 쿠폰구입('경험')}>
                <Text style={styles.upgBtnText}>구입 (⭐1 / +1시간)</Text>
              </TouchableOpacity>

              <Text style={styles.currencySection}>💰 재화 2배 쿠폰 (45~50강 판매 재화)</Text>
              <Text style={{ color: 재화남음 > 0 ? '#7ed957' : '#888', fontSize: 11, marginBottom: 3 }}>남은 시간: {시간표시(재화남음)} · 1개 = ⭐50, +1시간 (최대 60시간)</Text>
              <TouchableOpacity style={[styles.upgBtn, (ExPoint < 50 || 재화남음 >= 쿠폰최대ms - 1000) && styles.upgBtnOff, { alignSelf: 'flex-start', paddingHorizontal: 14 }]} onPress={() => 쿠폰구입('재화')}>
                <Text style={styles.upgBtnText}>구입 (⭐50 / +1시간)</Text>
              </TouchableOpacity>
            </ScrollView>
          </View>
        )
      })()}

      {도움말패널열림 && (
        <View style={styles.currencyPanel}>
          <View style={styles.currencyHeader}>
            <Text style={styles.currencyTitle}>📖 게임 도움말</Text>
            <TouchableOpacity onPress={() => set도움말패널열림(false)}>
              <Text style={styles.closeBtn}>✕</Text>
            </TouchableOpacity>
          </View>
          <ScrollView style={{ maxHeight: 화면H - 320 }}>
            {[
              ['🎯 목표', '마린을 강화해 고강(50강+)으로 키우고 DPS·자원·크레딧을 불린다.'],
              ['⚒️ 강화', '마린 선택 → 강화소 또는 자동강화. 성공 시 +1강(저강은 +2/+3도). 실패 시 마린 파괴(40~49강은 파괴방지 적용). 강화마다 자원(미네랄) 소모. 50→51강은 초월 시스템 별도.'],
              ['🐺 사냥터/DPS', '마린을 사1/사2/사3에 두면 자동 공격 → DPS 측정 → 자원 생성. 티어 높을수록 단가↑. 사3(허수광산)은 크레딧도 함께 나온다.'],
              ['💰 재화 종류', '미네랄(자원·강화·구입) / 크레딧(고유유닛 강화·마린 판매비) / 무색조각(보석) / 응무조(보주) / 각성석·크리스탈·상급조각 / ExP(환생 보상→크레딧 교환) / 일반P·초월P(스텟 분배).'],
              ['💠 보석·🔮 보주·🔷 크리스탈', '강화확률·자원·DPS 등 각종 보너스. 무색조각·응무조 등으로 구입.'],
              ['🌀 초월', '30만 레벨 도달 후 해금. 51강+ 강화·고강 판매로 초월경험 획득, 초월스텟 분배.'],
              ['🦸 고유유닛', '크레딧으로 강화하는 특별 유닛. 모든 강화 MAX 시 각성석으로 단수업(채광력↑).'],
              ['🌟 환생', '누적 50강+ 생산 1마리당 ExP 보상. 현재 마린은 초기화되고 스텟/보석/보주/초월/크레딧은 유지. 누적 카운트는 0으로 정산.'],
              ['🔄 ExP→크레딧', '고강 판매 크레딧이 비싸 ExP를 환전해 충당. (재화 패널에서 25%/50%/전부 교환)'],
              ['🧹 강화 초기화', '스텟/보석/보주/초월스텟 카테고리별로 쓴 자원·포인트를 전액 환불하며 리셋(리스펙).'],
              ['👆 마린 선택', '마린을 탭하거나 드래그(여러 마리)해서 선택합니다.'],
            ].map(([제목, 내용]) => (
              <View key={제목} style={{ marginBottom: 8 }}>
                <Text style={[styles.currencySection, { marginTop: 0 }]}>{제목}</Text>
                <Text style={{ color: '#cfd6e4', fontSize: 11, lineHeight: 16 }}>{내용}</Text>
              </View>
            ))}
            <View style={styles.divider} />
            <TouchableOpacity
              style={[styles.resetButton, { alignSelf: 'center', backgroundColor: '#5a2a2a', paddingHorizontal: 16 }]}
              onPress={게임초기화}
            >
              <Text style={[styles.resetButtonText, { color: '#ff9a9a' }]}>🔄 전체 초기화 (모든 진행 삭제)</Text>
            </TouchableOpacity>
          </ScrollView>
        </View>
      )}

      {정보패널열림 && (
        <View style={styles.currencyPanel}>
          <View style={styles.currencyHeader}>
            <Text style={styles.currencyTitle}>📊 강화 단계 정보</Text>
            <TouchableOpacity onPress={() => set정보패널열림(false)}>
              <Text style={styles.closeBtn}>✕</Text>
            </TouchableOpacity>
          </View>
          {(() => {
            const info = 강화단계정보(정보단계)
            const 이동 = (d: number) => set정보단계(v => Math.max(1, Math.min(60, v + d)))
            return (
              <>
                <View style={{ flexDirection: 'row', alignItems: 'center', justifyContent: 'center', gap: 6, marginVertical: 6 }}>
                  <TouchableOpacity onPress={() => 이동(-10)} style={[styles.upgBtn, { minWidth: 44 }]}><Text style={styles.upgBtnText}>◀◀</Text></TouchableOpacity>
                  <TouchableOpacity onPress={() => 이동(-1)} style={[styles.upgBtn, { minWidth: 44 }]}><Text style={styles.upgBtnText}>◀</Text></TouchableOpacity>
                  <Text style={{ color: '#fff', fontSize: 22, fontWeight: 'bold', minWidth: 72, textAlign: 'center' }}>{정보단계}강</Text>
                  <TouchableOpacity onPress={() => 이동(1)} style={[styles.upgBtn, { minWidth: 44 }]}><Text style={styles.upgBtnText}>▶</Text></TouchableOpacity>
                  <TouchableOpacity onPress={() => 이동(10)} style={[styles.upgBtn, { minWidth: 44 }]}><Text style={styles.upgBtnText}>▶▶</Text></TouchableOpacity>
                </View>
                <ScrollView style={{ maxHeight: 화면H - 380 }}>
                  <Text style={styles.currencySection}>🎲 강화 확률  <Text style={{ color: '#a855f7', fontSize: 11 }}>[{info.타입}]</Text></Text>
                  {info.확률.map((line, i) => (
                    <Text key={i} style={{ color: '#cfd6e4', fontSize: 14, lineHeight: 20 }}>{line}</Text>
                  ))}
                  {info.내역.length > 0 ? (
                    <>
                      <Text style={{ color: '#7ed957', fontSize: 11, marginTop: 4 }}>📈 확률 올린 항목:</Text>
                      {info.내역.map((line, i) => (
                        <Text key={i} style={{ color: '#cfd6e4', fontSize: 11 }}>· {line}</Text>
                      ))}
                    </>
                  ) : (
                    <Text style={{ color: '#888', fontSize: 10, marginTop: 4 }}>확률 올린 추가 항목 없음 (기본 확률만)</Text>
                  )}

                  <Text style={styles.currencySection}>⚔️ 유닛 전투 (현재 버프 반영)</Text>
                  <Text style={{ color: '#cfd6e4', fontSize: 14 }}>공격력(공당): {숫자포맷(info.공격력)}</Text>
                  <Text style={{ color: '#cfd6e4', fontSize: 14 }}>공격속도: {info.공속}</Text>
                  <Text style={{ color: '#cfd6e4', fontSize: 14 }}>단위 DPS: {숫자포맷(info.dps)}</Text>

                  <Text style={styles.currencySection}>💰 판매</Text>
                  <Text style={{ color: '#cfd6e4', fontSize: 14 }}>판매 비용: {info.판매}</Text>
                  <Text style={{ color: '#cfd6e4', fontSize: 14 }}>판매 획득: {info.판매획득}</Text>

                  {info.구입 > 0 && (
                    <>
                      <Text style={styles.currencySection}>🛒 구입(생산)</Text>
                      <Text style={{ color: '#cfd6e4', fontSize: 14 }}>{정보단계}강 마린 구입: {숫자포맷(info.구입)} 자원</Text>
                    </>
                  )}

                  <Text style={{ color: '#888', fontSize: 10, marginTop: 10 }}>40강 이하 판매: 무료 · 51강: 판매 불가 · 강화는 자원 소모 없음</Text>
                </ScrollView>
              </>
            )
          })()}
        </View>
      )}

      {/* 화면 전환 탭 */}
      <View style={styles.tabBar}>
        <TouchableOpacity
          style={[styles.tab, 현재화면 === 'base' && styles.tabActive]}
          onPress={() => { set현재화면('base'); set선택ID([]) }}
        >
          <Text style={styles.tabText}>🏠 ({베이스마린들.length}) [{마린들.length}/5000]</Text>
        </TouchableOpacity>
        <TouchableOpacity
          style={[styles.tab, 현재화면 === 'hunting1' && styles.tabActive]}
          onPress={() => { set현재화면('hunting1'); set선택ID([]) }}
        >
          <Text style={styles.tabText}>🏚️ 사1 ({사냥터1마린들.length})</Text>
        </TouchableOpacity>
        <TouchableOpacity
          style={[styles.tab, 현재화면 === 'hunting2' && styles.tabActive]}
          onPress={() => { set현재화면('hunting2'); set선택ID([]) }}
        >
          <Text style={styles.tabText}>🏛️ 사2 ({사냥터2마린들.length})</Text>
        </TouchableOpacity>
        <TouchableOpacity
          style={[styles.tab, 현재화면 === 'hunting3' && styles.tabActive]}
          onPress={() => { set현재화면('hunting3'); set선택ID([]) }}
        >
          <Text style={styles.tabText}>📡 사3 ({사냥터3마린들.length})</Text>
        </TouchableOpacity>
        <TouchableOpacity
          style={[styles.tab, 현재화면 === 'boss' && styles.tabActiveRed]}
          onPress={() => { set현재화면('boss'); set선택ID([]) }}
        >
          <Text style={styles.tabText}>⚔️ 보스 ({보스존마린들.length}/{보스존캡})</Text>
        </TouchableOpacity>
      </View>

      {/* 작은 버튼 바 (자동/보주/크리스탈) */}
      <View style={styles.smallBtnBar}>
        <TouchableOpacity style={styles.smallBtn} onPress={() => {
          const v = !자동패널열림
          set자동패널열림(v); if (v) { set재화패널열림(false); set생산패널열림(false); set보주패널열림(false); set강화패널열림(false); set명칭크리스탈패널열림(false); set보석패널열림(false); set고유유닛패널열림(false) }
        }}>
          <Text style={styles.smallBtnText}>🤖 자동</Text>
        </TouchableOpacity>
        <TouchableOpacity style={styles.smallBtn} onPress={() => {
          const v = !강화패널열림
          set강화패널열림(v); if (v) { set재화패널열림(false); set도움말패널열림(false); set정보패널열림(false); set생산패널열림(false); set상점패널열림(false); set자동패널열림(false); set명칭크리스탈패널열림(false); set고유유닛패널열림(false); set환생패널열림(false) }
        }}>
          <Text style={styles.smallBtnText}>✨ 강화</Text>
        </TouchableOpacity>
        <TouchableOpacity style={styles.smallBtn} onPress={() => {
          const v = !명칭크리스탈패널열림
          set명칭크리스탈패널열림(v); if (v) { set재화패널열림(false); set도움말패널열림(false); set정보패널열림(false); set생산패널열림(false); set상점패널열림(false); set자동패널열림(false); set강화패널열림(false); set고유유닛패널열림(false); set환생패널열림(false) }
        }}>
          <Text style={styles.smallBtnText}>🌟 크리스탈</Text>
        </TouchableOpacity>
        <TouchableOpacity style={styles.smallBtn} onPress={() => {
          const v = !고유유닛패널열림
          set고유유닛패널열림(v); if (v) { set재화패널열림(false); set도움말패널열림(false); set정보패널열림(false); set생산패널열림(false); set상점패널열림(false); set자동패널열림(false); set보주패널열림(false); set강화패널열림(false); set명칭크리스탈패널열림(false); set보석패널열림(false) }
        }}>
          <Text style={styles.smallBtnText}>🦸 고유</Text>
        </TouchableOpacity>
        <TouchableOpacity style={[styles.smallBtn, { backgroundColor: '#5b2a8c' }]} onPress={() => {
          const v = !환생패널열림
          set환생패널열림(v); if (v) { set재화패널열림(false); set도움말패널열림(false); set정보패널열림(false); set생산패널열림(false); set상점패널열림(false); set자동패널열림(false); set보주패널열림(false); set강화패널열림(false); set명칭크리스탈패널열림(false); set보석패널열림(false); set고유유닛패널열림(false) }
        }}>
          <Text style={styles.smallBtnText}>🌟 환생{환생레벨 > 0 ? `(Lv.${환생레벨})` : ''}</Text>
        </TouchableOpacity>
      </View>

      {/* 필드 (화면별 배경 이미지 + 단색 fallback + responder) */}
      <View style={[styles.field, {
        backgroundColor: 현재화면 === 'base' ? '#1a2a3e'
          : is사냥터(현재화면) ? '#1a3e2a'
          : '#3e1a2a',
        // @ts-ignore - 웹: 필드 위에서 터치/휠 스크롤 막기
        ...(Platform.OS === 'web' ? { touchAction: 'none', overscrollBehavior: 'contain' } : {}),
      }]}>
      {/* 배경 이미지 (절대 첫 자식, 터치 무시) */}
      <View style={{ position: 'absolute', top: 0, left: 0, width: 필드_W, height: 필드_H }} pointerEvents="none">
        <Image
          source={현재화면 === 'base' ? BG_LOBBY : is사냥터(현재화면) ? BG_HUNTING : BG_BOSS}
          style={{ width: 필드_W, height: 필드_H, opacity: 0.55 }}
          resizeMode="cover"
        />
      </View>
      {/* 선택된 마린 표시 — 절대 오버레이, 필드 상단에 고정 */}
      {선택ID.length > 0 && (
        <ScrollView
          horizontal
          showsHorizontalScrollIndicator={false}
          style={styles.selectedBar}
          contentContainerStyle={styles.selectedBarContent}
        >
          <Text style={styles.selectedLabel}>
            선택 {선택한마린들.length} (DPS {숫자포맷(선택한마린들.reduce((s,m)=>s+유닛DPS(m.lv),0))} · 평균 +{선택한마린들.length > 0 ? Math.round(선택한마린들.reduce((s,m)=>s+m.lv,0)/선택한마린들.length) : 0}):
          </Text>
          {선택한마린들.map(m => (
            <TouchableOpacity key={m.id} style={[styles.selectedChip, { borderColor: 마린색(m.lv) }]} onPress={() => set선택ID([m.id])}>
              <Text style={styles.selectedChipEmoji}>🪖</Text>
              <Text style={[styles.selectedChipLv, { color: 마린색(m.lv) }]}>+{m.lv}</Text>
            </TouchableOpacity>
          ))}
        </ScrollView>
      )}
      <View
        ref={fieldRef}
        style={styles.fieldInner}
        {...fieldResponderProps}
      >
        {/* BASE: 4개 zone (비콘 이미지 + 라벨) */}
        {현재화면 === 'base' && (
          <>
            {[
              { z: ZONE_강화, img: BEACON_강화 },
              { z: ZONE_보스존입구, img: BEACON_보스 },
              { z: ZONE_사냥터입구, img: BEACON_사냥 },
              { z: ZONE_판매소, img: BEACON_판매 },
            ].map(({ z, img }, i) => (
              <View key={i} style={[styles.zone, {
                left: z.x, top: z.y, width: z.w, height: z.h, borderColor: z.color,
                backgroundColor: 'transparent',
                borderWidth: 0,
              }]} pointerEvents="none">
                <Image source={img} style={{ width: z.w, height: z.h - 14, resizeMode: 'contain' }} />
                <Text style={[styles.zoneLabel, { color: z.color, textShadowColor: '#000', textShadowRadius: 3, width: z.w, textAlign: 'center' }]}>{z.label}</Text>
              </View>
            ))}
          </>
        )}

        {/* HUNTING/BOSS: 베이스 비콘 */}
        {(is사냥터(현재화면) || 현재화면 === 'boss') && (
          <View style={[styles.zone, {
            left: ZONE_베이스입구.x, top: ZONE_베이스입구.y,
            width: ZONE_베이스입구.w, height: ZONE_베이스입구.h,
            borderColor: ZONE_베이스입구.color,
            backgroundColor: ZONE_베이스입구.color + '15',
          }]} pointerEvents="none">
            <Text style={[styles.zoneLabel, { color: ZONE_베이스입구.color }]}>{ZONE_베이스입구.label}</Text>
          </View>
        )}

        {/* 사냥터 몹은 내부계산모드여도 항상 표시 (마린과 동일, line 3101 참고) */}
        {is사냥터(현재화면) && 몹들.filter(mb => {
          const screenTier = 현재화면 === 'hunting1' ? 1 : 현재화면 === 'hunting2' ? 2 : 3
          return mb.티어 === screenTier
        }).map(mb => {
          const flash = mb.flashUntil > now
          const 정보 = mb.티어 === 1
            ? { 이모지: '🏚️', 색: '#7ed957', 라벨: 'LV.1 (1~25강)', 크기: 80 }
            : mb.티어 === 2
            ? { 이모지: '🏛️', 색: '#4a90e2', 라벨: 'LV.2 (26~40강)', 크기: 90 }
            : { 이모지: '📡', 색: '#f5a623', 라벨: 'LV.MAX 💰(41~60강)', 크기: 100 }
          const 사이즈 = 정보.크기
          // 티어별 DPS (마린)
          const 티어DPS = 사냥터마린들
            .filter(m => 사냥터티어(m.lv) === mb.티어)
            .reduce((s, m) => s + 공격력(m.lv, 초월스텟, 일반스텟.유닛공업) * _공격력배수r * 공격속도(m.lv) * _공속배수r * 연타수(m.lv) * (1 + _크리r), 0)
          return (
            <View key={mb.id} pointerEvents="none">
              <View style={[styles.enemy, {
                left: mb.pos.x - 사이즈 / 2,
                top: mb.pos.y - 사이즈 / 2,
                backgroundColor: flash ? '#ff6b6b' : 정보.색 + '40',
                borderColor: 정보.색,
                width: 사이즈, height: 사이즈,
                borderRadius: 사이즈 / 2,
              }]}>
                <Text style={{ fontSize: Math.floor(사이즈 * 0.6) }}>{정보.이모지}</Text>
              </View>
              <Text style={[styles.bossLabel, {
                left: mb.pos.x - 80, top: mb.pos.y - 사이즈 / 2 - 32, width: 160,
                color: 정보.색, fontSize: 10, fontWeight: 'bold',
              }]}>{정보.라벨}</Text>
              <Text style={[styles.bossLabel, {
                left: mb.pos.x - 80, top: mb.pos.y - 사이즈 / 2 - 18, width: 160,
                color: '#fff', fontSize: 11, fontWeight: 'bold',
              }]}>⚔️ {숫자포맷(티어DPS)}</Text>
            </View>
          )
        })}

        {/* 고유유닛 (선택된 사냥터에만 배치) */}
        {is사냥터(현재화면) && 현재화면 === `hunting${고유유닛.위치}` && (
          <View pointerEvents="none">
            {고유유닛선택 && (
              <View style={[styles.selectRing, {
                left: 고유유닛pos.x - 마린_크기 / 2 - 3,
                top: 고유유닛pos.y - 마린_크기 / 2 - 3,
                borderColor: '#a855f7',
              }]} />
            )}
            <View style={[styles.marine, {
              left: 고유유닛pos.x - 마린_크기 / 2,
              top: 고유유닛pos.y - 마린_크기 / 2,
              borderColor: '#a855f7',
              borderWidth: 고유유닛선택 ? 3 : 2,
              backgroundColor: '#a855f740',
            }]}>
              <Text style={styles.marineText}>🦸</Text>
            </View>
            <Text style={[styles.marineLv, {
              left: 고유유닛pos.x - 15,
              top: 고유유닛pos.y + 마린_크기 / 2 - 2,
              color: '#a855f7',
            }]}>고유</Text>
          </View>
        )}

        {/* 데미지 floating (보스존만) */}
        {!내부계산모드 && 현재화면 === 'boss' && dmg플로팅들.map(d => {
          const t = Math.max(0, (d.until - now) / 900)
          return (
            <Text key={d.id} pointerEvents="none" style={{
              position: 'absolute',
              left: d.x - 40, top: d.y - (1 - t) * 40,  // 위로 떠오름
              width: 80, textAlign: 'center',
              color: d.crit ? '#ffeb3b' : '#fff',
              fontSize: d.crit ? 18 : 13,
              fontWeight: 'bold',
              opacity: t,
              textShadowColor: '#000', textShadowRadius: 3,
            }}>
              {d.crit ? '💥' : ''}{숫자포맷(d.dmg)}
            </Text>
          )
        })}

        {/* BOSS: 10단계까지. ExtraBoss 제거 */}
        {현재화면 === 'boss' && 적들.map(e => {
          const flash = e.flashUntil > now
          const gate = 보스DPS게이트(보스처치수 + 1)
          const ok = 사냥터DPS >= gate
          return (
            <View key={e.id} pointerEvents="none">
              <View style={[styles.enemy, {
                left: e.pos.x - 보스_크기 / 2,
                top: e.pos.y - 보스_크기 / 2,
                backgroundColor: flash ? '#ff6b6b' : '#5a2a2a',
                width: 보스_크기, height: 보스_크기, borderRadius: 보스_크기 / 2,
              }]}>
                <Text style={{ fontSize: 68 }}>👹</Text>
              </View>
              <Text style={[styles.bossLabel, { left: e.pos.x - 100, top: e.pos.y - 보스_크기 / 2 - 50, width: 200, color: '#fff' }]}>
                👹 보스 {보스처치수 + 1}/10
              </Text>
              <Text style={[styles.bossLabel, {
                left: e.pos.x - 100, top: e.pos.y - 보스_크기 / 2 - 34, width: 200,
                color: ok ? '#7ed957' : '#ccc', fontSize: 10,
              }]}>
                DPS {숫자포맷(사냥터DPS)} / {숫자포맷(gate)} {ok ? '✓ 클리어 중' : '✗ DPS 부족'}
              </Text>
            </View>
          )
        })}
        {현재화면 === 'boss' && 적들.length === 0 && 측정기활성 && (
          <View pointerEvents="none" style={{ position: 'absolute', top: 0, left: 0, width: 필드_W, height: 필드_H }}>
            <Text style={{ position: 'absolute', top: 30, width: 필드_W, textAlign: 'center', color: '#7ed957', fontSize: 14, fontWeight: 'bold' }}>
              🎯 타격수 측정기 ({측정기수}기)
            </Text>
            <Text style={{ position: 'absolute', top: 52, width: 필드_W, textAlign: 'center', color: '#cfd6e4', fontSize: 11 }}>
              {측정기best > 0
                ? `측정 유닛: ${측정기best}강 · +${숫자포맷(Math.round(측정기타격수초당))} 타격수/초`
                : '⚠️ 26강+ 유닛을 생산하면 측정 시작'}
            </Text>
            {측정기best > 0 && Array.from({ length: 측정기수 }).map((_, i) => {
              const 열 = 6
              const cx = 필드_W / 2 + ((i % 열) - (열 - 1) / 2) * 46
              const cy = 필드_H * 0.42 + Math.floor(i / 열) * 50
              return (
                <View key={i} style={{ position: 'absolute', left: cx - 17, top: cy - 17, width: 34, height: 34, borderRadius: 17, backgroundColor: 마린색(측정기best), alignItems: 'center', justifyContent: 'center', borderWidth: 2, borderColor: '#fff' }}>
                  <Text style={{ fontSize: 16 }}>🎯</Text>
                </View>
              )
            })}
          </View>
        )}

        {/* 마린 (현재 화면에 있는 마린만) */}
        {/* 내부계산모드: 강화중 유닛 숨김(베이스만). 사냥터·보스존은 항상 전부 표시 */}
        {((내부계산모드 && 현재화면 === 'base') ? 화면마린들.filter(m => !(자동강화ON && m.lv <= 자동강화최대lv)) : 화면마린들).map(m => {
          const selected = 선택ID.includes(m.id)
          const flash = m.공격플래시Until > now
          return (
            <View key={m.id} pointerEvents="none">
              {selected && (
                <View style={[styles.selectRing, {
                  left: m.pos.x - 마린_크기 / 2 - 3,
                  top: m.pos.y - 마린_크기 / 2 - 3,
                }]} />
              )}
              <View style={[styles.marine, {
                left: m.pos.x - 마린_크기 / 2,
                top: m.pos.y - 마린_크기 / 2,
                borderColor: flash ? '#f5a623' : (selected ? '#7ed957' : 마린색(m.lv)),
                borderWidth: selected ? 3 : 2,
                backgroundColor: 마린색(m.lv) + '40',  // tier 색 반투명 배경
              }]}>
                <Text style={styles.marineText}>🪖</Text>
              </View>
              <Text style={[styles.marineLv, {
                left: m.pos.x - 15,
                top: m.pos.y + 마린_크기 / 2 - 2,
                color: 마린색(m.lv),
              }]}>+{m.lv}</Text>
            </View>
          )
        })}

        {드래그박스 && (
          <View style={[styles.dragBox, {
            left: 드래그박스.x1,
            top: 드래그박스.y1,
            width: 드래그박스.x2 - 드래그박스.x1,
            height: 드래그박스.y2 - 드래그박스.y1,
          }]} pointerEvents="none" />
        )}

      </View>
      </View>

      {메시지 ? <Text style={styles.message}>{메시지}</Text> : null}

      {(is사냥터(현재화면) || 현재화면 === 'boss') && (
        <View style={styles.zoneInfo}>
          <Text style={styles.zoneInfoText}>🏠 베이스 비콘으로 마린 보내면 베이스 복귀</Text>
        </View>
      )}

      {/* 영구강화 패널 */}
      {강화패널열림 && (
        <View style={styles.prodPanel}>
          <View style={styles.prodHeader}>
            <Text style={styles.prodTitle}>✨ 영구강화</Text>
            <TouchableOpacity onPress={() => set강화패널열림(false)}>
              <Text style={styles.closeBtn}>✕</Text>
            </TouchableOpacity>
          </View>
          <Text style={styles.prodSubtitle}>Lv.{캐릭레벨} · {캐릭레벨 >= 캐릭레벨최대 ? (초월레벨 > 0 ? `초월XP ${숫자포맷(초월경험치)}/${숫자포맷(다음초월경험치(초월레벨))}` : `MAX (초월 미해금)`) : `XP ${숫자포맷(경험치)}/${숫자포맷(다음경험치(캐릭레벨))}`} · 포인트 {잔여포인트} · 초월포인트 {초월잔여포인트}</Text>
          {(최고마린lv >= 55 || 융합누적 > 0) && (
            <Text style={[styles.prodSubtitle, { color: '#c89bff' }]}>🔮 56강 융합 누적: {숫자포맷(융합누적)} / {숫자포맷(Math.max(1, 20000 - 보주.절약 * 10))} (55강 마린이 강화소 도달 시 흡수)</Text>
          )}
          {/* 스텟 탭 (4종 통합: 일반/초월/보주/보석) */}
          <View style={{ flexDirection: 'row', gap: 4, marginBottom: 6, flexWrap: 'wrap' }}>
            <TouchableOpacity
              style={[styles.statTabBtn, 스텟탭 === '일반' && styles.statTabBtnOn]}
              onPress={() => set스텟탭('일반')}
            >
              <Text style={[styles.statTabText, 스텟탭 === '일반' && { color: '#000' }]}>일반</Text>
            </TouchableOpacity>
            <TouchableOpacity
              style={[styles.statTabBtn, 스텟탭 === '초월' && styles.statTabBtnOn, 캐릭레벨 < 300000 && { opacity: 0.4 }]}
              onPress={() => set스텟탭('초월')}
            >
              <Text style={[styles.statTabText, 스텟탭 === '초월' && { color: '#000' }]}>초월{캐릭레벨 < 300000 ? '🔒' : ''}</Text>
            </TouchableOpacity>
            <TouchableOpacity
              style={[styles.statTabBtn, 스텟탭 === '보주' && styles.statTabBtnOn]}
              onPress={() => set스텟탭('보주')}
            >
              <Text style={[styles.statTabText, 스텟탭 === '보주' && { color: '#000' }]}>⚔️ 보주</Text>
            </TouchableOpacity>
            <TouchableOpacity
              style={[styles.statTabBtn, 스텟탭 === '보석' && styles.statTabBtnOn]}
              onPress={() => set스텟탭('보석')}
            >
              <Text style={[styles.statTabText, 스텟탭 === '보석' && { color: '#000' }]}>💎 보석</Text>
            </TouchableOpacity>
          </View>
          <ScrollView style={{ maxHeight: 360 }}>
            {/* 일반 스텟 — 메타 기반 (max/cost) */}
            {스텟탭 === '일반' && 일반스텟표.map(meta => {
              const val = 일반스텟[meta.key]
              const maxed = val >= meta.max
              const ok = !maxed && 잔여포인트 >= meta.cost
              return (
                <View key={meta.key} style={styles.statRow2}>
                  <Text style={styles.statLabel}>{meta.label}</Text>
                  <Text style={styles.statVal}>
                    {val}/{meta.max} <Text style={{ color: '#aaa', fontSize: 9 }}>({meta.효과(val)} · {meta.cost}P)</Text>
                  </Text>
                  {[1, 10, 100].map(n => (
                    <TouchableOpacity key={n} style={[styles.statBtn, !ok && styles.statBtnOff, { marginLeft: 2 }]} onPress={() => 스탯올리기(meta.key, n)}>
                      <Text style={[styles.statBtnText, { fontSize: 10 }]}>{maxed ? '' : `+${n}`}</Text>
                    </TouchableOpacity>
                  ))}
                  <TouchableOpacity style={[styles.statBtn, { backgroundColor: ok ? '#7ed957' : '#444', marginLeft: 4 }]} onPress={() => 스탯올리기(meta.key, 999999)}>
                    <Text style={[styles.statBtnText, { color: ok ? '#000' : '#888', fontSize: 10 }]}>{maxed ? 'MAX' : 'ALL'}</Text>
                  </TouchableOpacity>
                </View>
              )
            })}
            {/* 보주 탭 (응무조로 구입) */}
            {스텟탭 === '보주' && (() => {
              return (
                <>
                  <Text style={[styles.prodSubtitle, { color: '#bbb' }]}>💠 응무조 {숫자포맷(응무조)} · 누적 효과는 영구</Text>
                  {보주종류목록.map(종류 => {
                    const 개수 = 보주[종류]
                    const max = 보주max[종류]
                    const 단가 = 보주구입비용[종류]
                    const maxed = 개수 >= max
                    return (
                      <View key={종류} style={styles.upgRow}>
                        <View style={{ flex: 1 }}>
                          <Text style={styles.upgLabel}>⚔️ {종류} 보주 <Text style={{ color: '#f5a623' }}>{개수}/{max}</Text></Text>
                          <Text style={styles.upgEffect}>{보주효과설명[종류]}</Text>
                        </View>
                        <View style={{ flexDirection: 'row', gap: 3 }}>
                          {[1, 10, 100].map(n => (
                            <TouchableOpacity
                              key={n}
                              style={[styles.upgBtn, 응무조 < 단가 * n && styles.upgBtnOff, { minWidth: 38, paddingHorizontal: 4 }]}
                              onPress={() => 보주구입(종류, n)}
                            >
                              <Text style={[styles.upgBtnText, { fontSize: 10 }]}>+{n}</Text>
                              <Text style={[styles.upgBtnText, { fontSize: 8, color: '#bbb' }]}>💠{숫자포맷(단가 * n)}</Text>
                            </TouchableOpacity>
                          ))}
                          <TouchableOpacity
                            style={[styles.upgBtn, 응무조 < 단가 && styles.upgBtnOff, { backgroundColor: 응무조 >= 단가 ? '#7ed957' : '#444', minWidth: 38, paddingHorizontal: 4 }]}
                            onPress={() => 보주최대구입(종류)}
                          >
                            <Text style={[styles.upgBtnText, { fontSize: 10, color: 응무조 >= 단가 ? '#000' : '#888' }]}>MAX</Text>
                          </TouchableOpacity>
                        </View>
                      </View>
                    )
                  })}
                </>
              )
            })()}
            {/* 보석 탭 (무색조각으로 강화, 길게 누르면 연속) */}
            {스텟탭 === '보석' && (() => {
              const 보석정보in: { 종류: 보석타입; 이모지: string; 설명: string; 효과: string }[] = [
                { 종류: '하급',    이모지: '🔸', 설명: '하급 보석',  효과: '44강 +0.1%/회' },
                { 종류: '중급',    이모지: '🔶', 설명: '중급 보석',  효과: '45강 +0.1%/회' },
                { 종류: '상급',    이모지: '💠', 설명: '상급 보석',  효과: '46강 +0.1%/회' },
                { 종류: '특급',    이모지: '🔷', 설명: '특급 보석',  효과: '47강 +0.1%/회' },
                { 종류: '고급',    이모지: '💎', 설명: '고급 보석',  효과: '48강 +0.05%/회' },
                { 종류: '재물',    이모지: '💰', 설명: '재물의 보석', 효과: '자원 ×1.025/회 (복리)' },
                { 종류: '경험보석',이모지: '📗', 설명: '경험의 보석', 효과: '경험치 ×1.1/회 (복리)' },
                { 종류: '보호보석',이모지: '🛡️', 설명: '보호의 보석', 효과: '파괴방지 +0.1%/회' },
                { 종류: '궁극',    이모지: '🌟', 설명: '궁극 보석',   효과: '1~47강 +0.01%/회' },
                { 종류: '수호',    이모지: '🔮', 설명: '수호의 보석', 효과: '파괴방지 +0.01%/회' },
                { 종류: '초월보석',이모지: '✨', 설명: '초월 보석',   효과: '44~48강 각 +0.01%/회' },
                { 종류: '인내',    이모지: '🌀', 설명: '인내의 보석', 효과: '51강확률 +0.001%/회' },
                { 종류: '강타',    이모지: '💥', 설명: '강타의 보석', 효과: '보스 동시처치 +1/회' },
                { 종류: '자동화',  이모지: '🤖', 설명: '자동화 보석', 효과: '자동속도 ×1.05/회 (캡×2)' },
                { 종류: '채광',    이모지: '⛏️', 설명: '채광의 보석', 효과: '자원 ×1.05/회 (복리)' },
                { 종류: '촉진',    이모지: '🚀', 설명: '촉진의 보석', 효과: '자동생산 배수 +1/개' },
              ]
              return (
                <>
                  <Text style={[styles.prodSubtitle, { color: '#bbb' }]}>🔷 무색조각 {숫자포맷(무색조각)} · 길게 눌러 연속 강화 (cost = base × (1+보유/10))</Text>
                  {보석정보in.map(({ 종류, 이모지, 설명, 효과 }) => {
                    const 보유 = 보석[종류]
                    const 한도 = 보석max[종류]
                    const maxed = 보유 >= 한도
                    const 비용 = maxed ? 0 : 보석현재비용(종류, 보유)
                    const ok = !maxed && 무색조각 >= 비용
                    return (
                      <View key={종류} style={styles.upgRow}>
                        <View style={{ flex: 1 }}>
                          <Text style={styles.upgLabel}>{이모지} {설명} <Text style={{ color: '#f5a623' }}>{보유}/{한도}</Text></Text>
                          <Text style={styles.upgEffect}>{효과}</Text>
                        </View>
                        <View style={{ flexDirection: 'row', gap: 3 }}>
                          {[1, 10, 100].map(n => (
                            <TouchableOpacity
                              key={n}
                              style={[styles.upgBtn, !ok && styles.upgBtnOff, { minWidth: 40, paddingHorizontal: 4 }]}
                              onPress={() => !maxed && 보석구입N(종류, n)}
                              disabled={maxed}
                            >
                              <Text style={[styles.upgBtnText, { fontSize: 10 }]}>{maxed ? 'MAX' : `+${n}`}</Text>
                              {!maxed && n === 1 && <Text style={[styles.upgBtnText, { fontSize: 8, color: '#bbb' }]}>🔷{숫자포맷(비용)}</Text>}
                            </TouchableOpacity>
                          ))}
                        </View>
                      </View>
                    )
                  })}
                </>
              )
            })()}
            {/* 초월 스텟 */}
            {스텟탭 === '초월' && (
              <>
                {초월레벨 === 0 ? (
                  <Text style={{ color: '#a855f7', textAlign: 'center', padding: 16, fontSize: 13 }}>
                    🔒 초월 스텟은 51강 달성 시 초월 포인트를 획득합니다{'\n'}현재 초월레벨: {초월레벨} (포인트: {초월잔여포인트})
                  </Text>
                ) : (
                  <>
                    <Text style={{ color: '#a855f7', fontSize: 11, marginBottom: 6 }}>🌀 초월레벨 {초월레벨} · 초월 포인트 {초월잔여포인트}</Text>
                    {초월스텟표.map(meta => {
                      const val = 초월스텟[meta.key]
                      const maxed = val >= meta.max
                      const ok = !maxed && 초월잔여포인트 >= meta.cost
                      return (
                        <View key={meta.key} style={styles.statRow2}>
                          <Text style={styles.statLabel}>{meta.label}</Text>
                          <Text style={styles.statVal}>
                            {val}/{meta.max} <Text style={{ color: '#aaa', fontSize: 9 }}>({meta.효과(val)} · {meta.cost}P)</Text>
                          </Text>
                          {[1, 10, 100].map(n => (
                            <TouchableOpacity key={n} style={[styles.statBtn, !ok && styles.statBtnOff, { marginLeft: 2 }]} onPress={() => 초월스탯올리기(meta.key, n)}>
                              <Text style={[styles.statBtnText, { fontSize: 10 }]}>{maxed ? '' : `+${n}`}</Text>
                            </TouchableOpacity>
                          ))}
                          <TouchableOpacity style={[styles.statBtn, { backgroundColor: ok ? '#7ed957' : '#444', marginLeft: 4 }]} onPress={() => 초월스탯올리기(meta.key, 999999)}>
                            <Text style={[styles.statBtnText, { color: ok ? '#000' : '#888', fontSize: 10 }]}>{maxed ? 'MAX' : 'ALL'}</Text>
                          </TouchableOpacity>
                        </View>
                      )
                    })}
                  </>
                )}
              </>
            )}
            {/* 현재 탭 카테고리 초기화 (쓴 자원/포인트 전액 환불) */}
            <TouchableOpacity
              style={[styles.resetButton, { alignSelf: 'center', marginTop: 10, backgroundColor: '#3a2a3a' }]}
              onPress={스텟탭 === '일반' ? 스텟초기화 : 스텟탭 === '초월' ? 초월스텟초기화 : 스텟탭 === '보주' ? 보주초기화 : 보석초기화}
            >
              <Text style={styles.resetButtonText}>🧹 {스텟탭 === '일반' ? '스텟' : 스텟탭 === '초월' ? '초월스텟' : 스텟탭 === '보주' ? '보주' : '보석'} 초기화 (환불)</Text>
            </TouchableOpacity>
          </ScrollView>
        </View>
      )}

      {/* 명칭 크리스탈 패널 */}
      {명칭크리스탈패널열림 && (() => {
        type 크탭 = '노말' | '레어' | '유니크' | '갤럭시' | '퀘이사' | '오리진'
        type 크정보 = { 키: keyof 명칭크리스탈목록; 이름: string; 비용: number; 색상: string; 효과: string; 구현: boolean }
        const clv = Object.fromEntries(
          (Object.keys(초기명칭크리스탈) as (keyof 명칭크리스탈목록)[]).map(k => [k, 명칭크리스탈Lv[k] ?? 0])
        ) as Record<keyof 명칭크리스탈목록, number>
        const 크리스탈목록표: Record<크탭, 크정보[]> = {
          노말: [
            { 키: '방어',  이름: '방어의 크리스탈',  비용: 50,  색상: '#4a90e2', 효과: '효과 미정 (재배정 예정)',                 구현: false },
            { 키: '행운',  이름: '행운의 크리스탈',  비용: 50,  색상: '#7ed957', 효과: `개별확률 +${(clv.행운 * 1).toFixed(0)}% (회당+1%)`,    구현: true },
            { 키: '경험',  이름: '경험의 크리스탈',  비용: 50,  색상: '#9b59b6', 효과: '초월경험치 +50% (준비중)',             구현: false },
            { 키: '무력',  이름: '무력의 크리스탈',  비용: 50,  색상: '#e94560', 효과: '51~56강 업그레이드 +2 (준비중)',       구현: false },
            { 키: '절약',  이름: '절약의 크리스탈',  비용: 50,  색상: '#f5a623', 효과: `판매보상 ×${Math.pow(1.05, clv.절약).toFixed(2)} (회당×1.05)`, 구현: true },
            { 키: '총명',  이름: '총명의 크리스탈',  비용: 50,  색상: '#1abc9c', 효과: `무색조각 ×${Math.pow(1.5, clv.총명).toFixed(2)} (회당×1.5)`,  구현: true },
            { 키: '보호',  이름: '보호의 크리스탈',  비용: 50,  색상: '#3498db', 효과: `파괴방지 +${(clv.보호 * 0.2).toFixed(1)}% (회당+0.2%)`, 구현: true },
            { 키: '각성',  이름: '각성의 크리스탈',  비용: 50,  색상: '#e67e22', 효과: '각성의 보석 +100% (준비중)',           구현: false },
          ],
          레어: [
            { 키: '홍색',   이름: '홍색의 크리스탈',  비용: 100, 색상: '#e74c3c', 효과: `개별확률 +${(clv.홍색 * 1).toFixed(0)}%`, 구현: true },
            { 키: '주황',   이름: '주황의 크리스탈',  비용: 100, 색상: '#e67e22', 효과: `판매 ×${Math.pow(1.05, clv.주황).toFixed(2)} + 무색 ×${Math.pow(1.5, clv.주황).toFixed(2)}`, 구현: true },
            { 키: '노랑',   이름: '노랑의 크리스탈',  비용: 100, 색상: '#f1c40f', 효과: `파괴방지 +${(clv.노랑 * 0.2).toFixed(1)}% + 각성보석 (준비중)`, 구현: true },
            { 키: '초록',   이름: '초록의 크리스탈',  비용: 100, 색상: '#2ecc71', 효과: '51~56강 업그레이드 +2 + 초월경험치 (준비중)',     구현: false },
            { 키: '파랑',   이름: '파랑의 크리스탈',  비용: 100, 색상: '#3498db', 효과: '51~56강 업그레이드 +2 (준비중)',  구현: false },
            { 키: '남색',   이름: '남색의 크리스탈',  비용: 100, 색상: '#2980b9', 효과: `파괴방지 +${(clv.남색 * 0.2).toFixed(1)}% + 초월경험치 (준비중)`, 구현: true },
            { 키: '보라',   이름: '보라의 크리스탈',  비용: 100, 색상: '#9b59b6', 효과: `개별확률 +${(clv.보라 * 1).toFixed(0)}% + 판매 ×${Math.pow(1.05, clv.보라).toFixed(2)}`, 구현: true },
            { 키: '하늘색', 이름: '하늘색 크리스탈',  비용: 100, 색상: '#5dade2', 효과: `무색 ×${Math.pow(1.5, clv.하늘색).toFixed(2)} + 각성보석 (준비중)`, 구현: true },
            { 키: '무색명칭', 이름: '무색의 크리스탈', 비용: 100, 색상: '#bdc3c7', 효과: `초월확률 +${(clv.무색명칭 * 0.2).toFixed(1)}% (회당+0.2%)`, 구현: true },
          ],
          유니크: [
            { 키: '흑색', 이름: '흑색의 크리스탈', 비용: 500, 색상: '#7f8c8d', 효과: `무색 ×${Math.pow(2, clv.흑색).toFixed(2)} + 판매 ×${Math.pow(1.10, clv.흑색).toFixed(2)} + 초월확률 +${(clv.흑색 * 0.5).toFixed(1)}%`, 구현: true },
            { 키: '백색명칭', 이름: '백색의 크리스탈', 비용: 500, 색상: '#ecf0f1', 효과: `개별확률 +${(clv.백색명칭 * 2).toFixed(0)}% + 파괴방지 +${(clv.백색명칭 * 0.3).toFixed(1)}% + 초월경험치 (준비중)`, 구현: true },
          ],
          갤럭시: [
            { 키: '우주', 이름: '우주의 크리스탈', 비용: 2000, 색상: '#a855f7', 효과: `개별확률 +${(clv.우주 * 5).toFixed(0)}% + 파괴방지 +${(clv.우주 * 0.5).toFixed(1)}% + 초월확률 +${(clv.우주 * 1).toFixed(0)}% + 무색 ×${Math.pow(3, clv.우주).toFixed(2)} + 판매 ×${Math.pow(2, clv.우주).toFixed(2)}`, 구현: true },
          ],
          퀘이사: [
            { 키: '길운Q',  이름: '길운의 크리스탈',  비용: 200, 색상: '#f5a623', 효과: '51~53강 강화확률 +2%p (준비중)', 구현: false },
            { 키: '무구Q',  이름: '무구의 크리스탈',  비용: 200, 색상: '#e94560', 효과: '57~59강 업그레이드 +8 (준비중)', 구현: false },
            { 키: '집중Q',  이름: '집중의 크리스탈',  비용: 200, 색상: '#4a90e2', 효과: '56강 융합확률 +1%p (준비중)',    구현: false },
            { 키: '절제Q',  이름: '절제의 크리스탈',  비용: 200, 색상: '#7ed957', 효과: '융합 필요수 -10% (준비중)',      구현: false },
            { 키: '탐욕Q',  이름: '탐욕의 크리스탈',  비용: 200, 색상: '#f1c40f', 효과: '광산 크레딧 +20% (준비중)',      구현: false },
            { 키: '증식Q',  이름: '증식의 크리스탈',  비용: 200, 색상: '#1abc9c', 효과: '유닛 생산수 +10% (준비중)',       구현: false },
            { 키: '미래Q',  이름: '미래의 크리스탈',  비용: 200, 색상: '#9b59b6', 효과: 'ExPoint +20% (준비중)',           구현: false },
            { 키: '돌파Q',  이름: '돌파의 크리스탈',  비용: 200, 색상: '#e74c3c', 효과: '엑스트라 보스 데미지 +10% (준비중)', 구현: false },
          ],
          오리진: [
            { 키: '창조O', 이름: '창조의 크리스탈', 비용: 1000, 색상: '#ffd700', 효과: '광산 크레딧/ExPoint/데미지/초월경험치 (준비중)', 구현: false },
            { 키: '파멸O', 이름: '파멸의 크리스탈', 비용: 1000, 색상: '#ff4444', 효과: '57~58강 확률/크리티컬/돈/생산량 (준비중)',       구현: false },
          ],
        }
        const 탭목록: 크탭[] = ['노말', '레어', '유니크', '갤럭시', '퀘이사', '오리진']
        const 현재목록 = 크리스탈목록표[명칭크리스탈탭]
        const 슬롯수 = 크리스탈슬롯수(초월레벨)
        const 장착수 = 장착크리스탈.length
        return (
          <View style={styles.prodPanel}>
            <View style={styles.prodHeader}>
              <Text style={styles.prodTitle}>🌟 크리스탈 (장착식)</Text>
              <TouchableOpacity onPress={() => set명칭크리스탈패널열림(false)}>
                <Text style={styles.closeBtn}>✕</Text>
              </TouchableOpacity>
            </View>
            <Text style={styles.prodSubtitle}>
              슬롯 {장착수}/{슬롯수} · 🌀 초월Lv.{초월레벨} (다음 슬롯: {슬롯수 < 6 ? [1,5,15,50,200,1000][슬롯수] + '레벨' : 'MAX'})
            </Text>
            <Text style={[styles.prodSubtitle, { fontSize: 10, color: '#888' }]}>
              크리스탈 획득: 51강+ 판매시 박스 드랍 / 타격수 보상 (탭=장착 토글)
            </Text>
            {/* 탭 */}
            <View style={{ flexDirection: 'row', flexWrap: 'wrap', marginBottom: 6, gap: 4 }}>
              {탭목록.map(t => (
                <TouchableOpacity key={t} style={[styles.statTabBtn, 명칭크리스탈탭 === t && styles.statTabBtnOn]} onPress={() => set명칭크리스탈탭(t)}>
                  <Text style={[styles.statTabText, 명칭크리스탈탭 === t && { color: '#000' }]}>{t}</Text>
                </TouchableOpacity>
              ))}
            </View>
            <ScrollView style={{ maxHeight: 300 }}>
              {현재목록.map(info => {
                const 현재수 = 명칭크리스탈[info.키] as number
                const 장착됨 = 장착크리스탈.includes(info.키)
                const 보유 = (명칭크리스탈Lv[info.키] ?? 0) >= 1  // Lv1+ (활성)이면 장착 가능
                const 슬롯가득 = !장착됨 && 장착수 >= 슬롯수
                const 등급 = 크리스탈등급맵[info.키]
                const lv = 명칭크리스탈Lv[info.키] ?? 0
                const lvMax = 크리스탈등급max[등급]
                const lv업비용 = lv === 0 ? 1 : (lv < lvMax ? 크리스탈레벨업비용(등급, lv) : 0)
                const lv업가능 = lv < lvMax && 현재수 >= lv업비용
                return (
                  <View key={info.키} style={[styles.upgRow, { borderLeftWidth: 3, borderLeftColor: info.색상, paddingLeft: 8, opacity: 보유 ? 1 : 0.4 }]}>
                    <View style={{ flex: 1 }}>
                      <Text style={[styles.upgLabel, { color: info.색상 }]}>
                        {info.이름} <Text style={{ color: '#f5a623' }}>Lv.{lv}/{lvMax}</Text> <Text style={{ color: '#888', fontSize: 9 }}>(재료 {현재수})</Text>{장착됨 ? ' 🟢' : ''}
                      </Text>
                      <Text style={[styles.upgEffect, { color: '#ccc' }]}>{info.효과}</Text>
                    </View>
                    <View style={{ flexDirection: 'row', gap: 3 }}>
                      {(() => {
                        const 제비용 = 크리스탈제작비용[등급]
                        if (!제비용) return null
                        const isQ = 등급 === '퀘이사'
                        const 제ok = (isQ ? 상급크리스탈조각 : 크리스탈조각) >= 제비용
                        return (
                          <TouchableOpacity
                            style={[styles.upgBtn, !제ok && styles.upgBtnOff, { backgroundColor: 제ok ? '#9b59b6' : '#444', minWidth: 60, paddingHorizontal: 4 }]}
                            onPress={() => 크리스탈제작(info.키)}
                          >
                            <Text style={[styles.upgBtnText, { fontSize: 9 }]}>🛠️ 제작</Text>
                            <Text style={[styles.upgBtnText, { fontSize: 8, color: '#fff' }]}>{isQ ? '🟣' : '🔮'}{숫자포맷(제비용)}</Text>
                          </TouchableOpacity>
                        )
                      })()}
                      <TouchableOpacity
                        style={[styles.upgBtn, !lv업가능 && styles.upgBtnOff, { backgroundColor: lv업가능 ? '#f5a623' : '#444', minWidth: 60, paddingHorizontal: 4 }]}
                        onPress={() => 크리스탈레벨업(info.키)}
                        disabled={lv >= lvMax}
                      >
                        <Text style={[styles.upgBtnText, { fontSize: 9 }]}>{lv >= lvMax ? 'MAX' : (lv === 0 ? '활성' : `+1`)}</Text>
                        {lv < lvMax && lv > 0 && <Text style={[styles.upgBtnText, { fontSize: 8, color: '#fff' }]}>×{숫자포맷(lv업비용)}</Text>}
                      </TouchableOpacity>
                      <TouchableOpacity
                        style={[styles.upgBtn, (!보유 || 슬롯가득) && !장착됨 && styles.upgBtnOff, { backgroundColor: 장착됨 ? '#7ed957' : 슬롯가득 ? '#444' : '#3a5a8a', minWidth: 50 }]}
                        onPress={() => {
                          if (!보유) { 메시지표시('미활성 — 먼저 활성/레벨업 필요'); return }
                          if (장착됨) {
                            set장착크리스탈(prev => prev.filter(k => k !== info.키))
                          } else if (슬롯가득) {
                            메시지표시(`슬롯 가득 (${슬롯수}/${슬롯수})`)
                          } else {
                            set장착크리스탈(prev => [...prev, info.키])
                          }
                        }}
                      >
                        <Text style={[styles.upgBtnText, { color: 장착됨 ? '#000' : '#fff', fontSize: 10 }]}>{장착됨 ? '해제' : 슬롯가득 ? '🔒' : '장착'}</Text>
                      </TouchableOpacity>
                    </View>
                  </View>
                )
              })}
            </ScrollView>
          </View>
        )
      })()}

      {/* 고유유닛 패널 */}
      {고유유닛패널열림 && (() => {
        const 고유DPS현재 = 고유유닛DPS(고유유닛)
        const upgList: { stat: keyof Omit<고유유닛스텟, '위치' | '단수'>; 이모지: string; 설명: string; 상한: number }[] = [
          { stat: '공격력',   이모지: '⚔️', 설명: '공격력 +500/강화 (기본500)',         상한: 20 },
          { stat: '공속',     이모지: '⚡', 설명: '공격속도 단계 (1→1.5→2→2.5→3→3.5→4)', 상한: 6 },
          { stat: '경험치',   이모지: '📗', 설명: '경험치 획득 +20%/강화',               상한: 5 },
          { stat: '추가1강',  이모지: '🎯', 설명: '+1강 확률 +0.25%/강화',               상한: 20 },
          { stat: '특수강화', 이모지: '🌟', 설명: '특수강화 확률 +0.5%/강화',            상한: 10 },
          { stat: '파괴방지', 이모지: '🛡️', 설명: '파괴방지 +0.1%/강화',               상한: 200 },
        ]
        return (
          <View style={styles.prodPanel}>
            <View style={styles.prodHeader}>
              <Text style={styles.prodTitle}>🦸 고유유닛 강화</Text>
              <TouchableOpacity onPress={() => set고유유닛패널열림(false)}>
                <Text style={styles.closeBtn}>✕</Text>
              </TouchableOpacity>
            </View>
            <Text style={styles.prodSubtitle}>💰 크레딧: {숫자포맷(크레딧)} · 보스 처치 시 획득</Text>
            <Text style={[styles.prodSubtitle, { color: '#7ed957' }]}>
              DPS {숫자포맷(고유DPS현재)} (공격력 {고유유닛공격력(고유유닛)} × 속도 {고유유닛공속(고유유닛).toFixed(1)})
            </Text>
            <Text style={[styles.prodSubtitle, { color: '#a855f7' }]}>
              위치: 사냥터 1 고정
            </Text>
            <Text style={[styles.prodSubtitle, { color: '#ff6ad9' }]}>
              🌟 단수 {고유유닛.단수}단 · Lv3 단가 ×{단수배율(고유유닛.단수).toFixed(2)} · 채광력 +{고유유닛.단수 - 1}
              {단수업가능(고유유닛) ? ` · 다음 각성 💎×${단수업비용(고유유닛.단수)} (보유 ${각성의보석})` : ' · (모든 강화 MAX시 각성)'}
            </Text>
            <ScrollView style={{ maxHeight: 320 }}>
              {upgList.map(({ stat, 이모지, 설명, 상한 }) => {
                const lv = 고유유닛[stat] as number
                const maxed = lv >= 상한
                const 비용 = maxed ? 0 : 고유유닛강화비용표[stat](lv)
                const ok = !maxed && 크레딧 >= 비용
                return (
                  <View key={stat} style={styles.upgRow}>
                    <View style={{ flex: 1 }}>
                      <Text style={styles.upgLabel}>{이모지} {stat} <Text style={{ color: '#f5a623' }}>Lv.{lv}/{상한}</Text></Text>
                      <Text style={styles.upgEffect}>{설명}</Text>
                    </View>
                    <View style={{ flexDirection: 'row', gap: 3 }}>
                      <TouchableOpacity
                        style={[styles.upgBtn, !ok && styles.upgBtnOff, { minWidth: 70 }]}
                        onPress={() => 고유유닛강화(stat)}
                      >
                        <Text style={styles.upgBtnText}>{maxed ? 'MAX' : `💰${숫자포맷(비용)}`}</Text>
                      </TouchableOpacity>
                      {!maxed && (
                        <TouchableOpacity
                          style={[styles.upgBtn, { backgroundColor: '#7ed957', minWidth: 50, paddingHorizontal: 4 }, !ok && styles.upgBtnOff]}
                          onPress={() => 고유유닛강화MAX(stat)}
                          disabled={!ok}
                        >
                          <Text style={[styles.upgBtnText, { color: ok ? '#000' : '#888', fontSize: 10 }]}>MAX</Text>
                        </TouchableOpacity>
                      )}
                    </View>
                  </View>
                )
              })}
            </ScrollView>
          </View>
        )
      })()}

      {/* 🌟 환생 패널 */}
      {환생패널열림 && (() => {
        const chk = 환생가능여부()
        const 보상 = 환생보상ExPoint()
        return (
          <View style={styles.prodPanel}>
            <View style={styles.prodHeader}>
              <Text style={styles.prodTitle}>🌟 환생 (Lv.{환생레벨})</Text>
              <TouchableOpacity onPress={() => set환생패널열림(false)}>
                <Text style={styles.closeBtn}>✕</Text>
              </TouchableOpacity>
            </View>
            <Text style={styles.prodSubtitle}>누적 환생: {누적환생수}회</Text>
            <Text style={[styles.prodSubtitle, { color: '#a855f7' }]}>
              조건: 50강+ 마린 1마리 이상 보유
            </Text>
            <Text style={[styles.prodSubtitle, { color: '#f5a623', fontSize: 11 }]}>
              💡 보상 ⭐ExPoint = 누적 생산한 50강+ 유닛 1마리당 {EXP_마리당50강}
              {'\n'}→ 재화창에서 ExP를 💰크레딧으로 교환해 사용
            </Text>
            <Text style={[styles.prodSubtitle, { color: '#fff' }]}>
              이번 누적 50강+ 생산: {숫자포맷(누적50강생산)}마리
            </Text>
            <Text style={[styles.prodSubtitle, { color: chk.ok ? '#7ed957' : '#ff6b6b' }]}>
              {chk.ok ? `✓ 환생 가능 — 보상 ⭐ ${숫자포맷(보상)} ExPoint` : `✗ ${chk.이유}`}
            </Text>
            <TouchableOpacity
              style={[styles.upgBtn, { backgroundColor: chk.ok ? '#7ed957' : '#555', marginVertical: 8 }]}
              onPress={환생실행}
              disabled={!chk.ok}
            >
              <Text style={[styles.upgBtnText, { fontSize: 14 }]}>
                🌟 환생하기 (보상 ⭐ {숫자포맷(보상)})
              </Text>
            </TouchableOpacity>
            <View style={styles.divider} />
            <Text style={[styles.prodSubtitle, { color: '#aaa', fontSize: 11 }]}>
              ※ 환생하면 현재 마린이 초기화됩니다. 스텟/보석/보주/초월/크레딧 등은 유지, 누적 50강+ 생산은 0으로 정산.
            </Text>
          </View>
        )
      })()}

      {/* 자동화 패널 */}
      {자동패널열림 && (
        <View style={styles.prodPanel}>
          <View style={styles.prodHeader}>
            <Text style={styles.prodTitle}>🤖 자동화</Text>
            <TouchableOpacity onPress={() => set자동패널열림(false)}>
              <Text style={styles.closeBtn}>✕</Text>
            </TouchableOpacity>
          </View>
          <Text style={styles.prodSubtitle}>판매 &gt; 강화 우선. 자동구입과 자동판매는 함께 작동</Text>
          {/* 내부 계산 모드 */}
          <View style={styles.sliderRow}>
            <Text style={styles.sliderLabel}>⚡ 내부계산</Text>
            <TouchableOpacity
              style={[styles.autoToggle, 내부계산모드 && styles.autoToggleOn]}
              onPress={() => set내부계산모드(v => !v)}
            >
              <Text style={styles.autoToggleText}>{내부계산모드 ? 'ON' : 'OFF'}</Text>
            </TouchableOpacity>
            <Text style={{ color: '#aaa', fontSize: 10 }}>렌더 OFF → 렉 해소</Text>
          </View>

          {/* 자동 강화 */}
          <View style={styles.sliderRow}>
            <Text style={styles.sliderLabel}>🔨 강화 ≤</Text>
            <TouchableOpacity style={styles.sliderArrow} onPress={() => set자동강화최대lv(v => Math.max(1, v - 5))}>
              <Text style={styles.sliderArrowText}>≪</Text>
            </TouchableOpacity>
            <TouchableOpacity style={styles.sliderArrow} onPress={() => set자동강화최대lv(v => Math.max(1, v - 1))}>
              <Text style={styles.sliderArrowText}>◀</Text>
            </TouchableOpacity>
            <Text style={styles.sliderValue}>+{자동강화최대lv}</Text>
            <TouchableOpacity style={styles.sliderArrow} onPress={() => set자동강화최대lv(v => Math.min(59, v + 1))}>
              <Text style={styles.sliderArrowText}>▶</Text>
            </TouchableOpacity>
            <TouchableOpacity style={styles.sliderArrow} onPress={() => set자동강화최대lv(v => Math.min(59, v + 5))}>
              <Text style={styles.sliderArrowText}>≫</Text>
            </TouchableOpacity>
            <TouchableOpacity
              style={[styles.autoToggle, 자동강화ON && styles.autoToggleOn]}
              onPress={() => set자동강화ON(v => !v)}
            >
              <Text style={styles.autoToggleText}>{자동강화ON ? 'ON' : 'OFF'}</Text>
            </TouchableOpacity>
          </View>

          {/* 자동 판매 */}
          <View style={styles.sliderRow}>
            <Text style={styles.sliderLabel}>🛒 판매 =</Text>
            <TouchableOpacity style={styles.sliderArrow} onPress={() => set자동판매lv(v => Math.max(15, v - 5))}>
              <Text style={styles.sliderArrowText}>≪</Text>
            </TouchableOpacity>
            <TouchableOpacity style={styles.sliderArrow} onPress={() => set자동판매lv(v => Math.max(15, v - 1))}>
              <Text style={styles.sliderArrowText}>◀</Text>
            </TouchableOpacity>
            <Text style={styles.sliderValue}>+{자동판매lv}</Text>
            <TouchableOpacity style={styles.sliderArrow} onPress={() => set자동판매lv(v => Math.min(60, v + 1))}>
              <Text style={styles.sliderArrowText}>▶</Text>
            </TouchableOpacity>
            <TouchableOpacity style={styles.sliderArrow} onPress={() => set자동판매lv(v => Math.min(60, v + 5))}>
              <Text style={styles.sliderArrowText}>≫</Text>
            </TouchableOpacity>
            <TouchableOpacity
              style={[styles.autoToggle, 자동판매ON && styles.autoToggleOnRed]}
              onPress={() => set자동판매ON(v => !v)}
            >
              <Text style={styles.autoToggleText}>{자동판매ON ? 'ON' : 'OFF'}</Text>
            </TouchableOpacity>
          </View>

          {/* 자동 구입 */}
          <View style={styles.sliderRow}>
            <Text style={styles.sliderLabel}>🏭 구입 =</Text>
            <TouchableOpacity style={styles.sliderArrow} onPress={() => set자동구입강도(v => { const i = 생산강도목록.indexOf(v as any); return 생산강도목록[Math.max(0, i - 1)] ?? v })}>
              <Text style={styles.sliderArrowText}>◀</Text>
            </TouchableOpacity>
            <Text style={styles.sliderValue}>+{자동구입강도}</Text>
            <TouchableOpacity style={styles.sliderArrow} onPress={() => set자동구입강도(v => { const i = 생산강도목록.indexOf(v as any); return 생산강도목록[Math.min(생산강도목록.length - 1, i + 1)] ?? v })}>
              <Text style={styles.sliderArrowText}>▶</Text>
            </TouchableOpacity>
            <TouchableOpacity
              style={[styles.autoToggle, 자동구입ON && styles.autoToggleOn]}
              onPress={() => set자동구입ON(v => !v)}
            >
              <Text style={styles.autoToggleText}>{자동구입ON ? 'ON' : 'OFF'}</Text>
            </TouchableOpacity>
          </View>

          {/* 자동 구입 틱당 마린수 (1 ~ 최대틱수). 촉진이 최대치를 올림 */}
          <View style={styles.sliderRow}>
            <Text style={styles.sliderLabel}>📦 틱당</Text>
            <TouchableOpacity style={styles.sliderArrow} onPress={() => set자동구입배수(v => Math.max(1, v - 1))}>
              <Text style={styles.sliderArrowText}>◀</Text>
            </TouchableOpacity>
            <Text style={styles.sliderValue}>{Math.min(자동구입배수, 최대틱수)}</Text>
            <TouchableOpacity style={styles.sliderArrow} onPress={() => set자동구입배수(v => Math.min(최대틱수, v + 1))}>
              <Text style={styles.sliderArrowText}>▶</Text>
            </TouchableOpacity>
            <Text style={{ color: '#aaa', fontSize: 10 }}>마리 / tick (최대 {최대틱수} = 기본10 + 🚀촉진 {촉진합})</Text>
          </View>
        </View>
      )}


    </ScrollView>
    </SafeAreaView>
  )
}

// ============================================
// 스타일
// ============================================

const styles = StyleSheet.create({
  container: {
    flexGrow: 1,
    backgroundColor: '#1a1a2e',
    alignItems: 'center',
    padding: 4,
    paddingTop: 2,
  },
  title: {
    fontSize: 13,
    color: '#e94560',
    fontWeight: 'bold',
    marginBottom: 1,
  },
  statBox: {
    backgroundColor: '#16213e',
    padding: 4,
    borderRadius: 6,
    width: 필드_W,
    alignItems: 'center',
    marginBottom: 2,
  },
  stat: { fontSize: 15, color: '#ffffff', marginBottom: 2 },
  statResource: { fontSize: 14, color: '#7ed957', fontWeight: 'bold' },
  statEX: { fontSize: 13, color: '#f5a623', fontWeight: 'bold', backgroundColor: '#3a2a1a', paddingHorizontal: 6, paddingVertical: 1, borderRadius: 3 },
  convertBtn: { backgroundColor: '#7ed957', paddingHorizontal: 6, paddingVertical: 3, borderRadius: 4 },
  convertBtnText: { color: '#000', fontSize: 10, fontWeight: 'bold' },
  autoHeaderRow: { flexDirection: 'row', alignItems: 'center', paddingVertical: 4, borderBottomWidth: 1, borderBottomColor: '#333' },
  autoRow: { flexDirection: 'row', alignItems: 'center', paddingVertical: 3 },
  autoColLv: { width: 40, color: '#fff', fontSize: 12, fontWeight: 'bold', textAlign: 'center' },
  autoColTog: { flex: 1, color: '#aaa', fontSize: 11, textAlign: 'center' },
  autoToggle: { flex: 1, backgroundColor: '#1f2a48', borderRadius: 4, paddingVertical: 4, marginHorizontal: 2, alignItems: 'center' },
  autoToggleOn: { backgroundColor: '#7ed957' },
  autoToggleOnRed: { backgroundColor: '#e94560' },
  autoToggleText: { color: '#fff', fontSize: 11, fontWeight: 'bold' },
  autoBuyRow: { flexDirection: 'row', alignItems: 'center', marginTop: 4, flexWrap: 'wrap', gap: 3 },
  autoBuyLv: { backgroundColor: '#1f2a48', borderRadius: 4, paddingHorizontal: 6, paddingVertical: 3 },
  autoBuyLvOn: { backgroundColor: '#f5a623' },
  autoBuyLvText: { color: '#fff', fontSize: 11, fontWeight: 'bold' },
  sliderRow: { flexDirection: 'row', alignItems: 'center', paddingVertical: 6, gap: 4 },
  sliderLabel: { color: '#aaa', fontSize: 11, flex: 1 },
  sliderArrow: { backgroundColor: '#1f2a48', borderRadius: 4, width: 30, height: 28, alignItems: 'center', justifyContent: 'center' },
  sliderArrowText: { color: '#fff', fontSize: 13, fontWeight: 'bold' },
  sliderValue: { color: '#f5a623', fontSize: 14, fontWeight: 'bold', width: 45, textAlign: 'center' },
  statRow: { flexDirection: 'row', alignItems: 'center', justifyContent: 'center', flexWrap: 'wrap', gap: 8 },
  statSmall: { fontSize: 12, color: '#aaaaaa' },
  statBatch: {
    fontSize: 14,
    color: '#f5a623',
    fontWeight: 'bold',
    backgroundColor: '#3a2a1a',
    paddingHorizontal: 8,
    paddingVertical: 2,
    borderRadius: 4,
  },
  tabBar: {
    flexDirection: 'row',
    width: 필드_W,
    marginBottom: 2,
    gap: 3,
    zIndex: 300,  // 패널보다 위 → 패널 열린 상태로도 클릭 가능
    elevation: 30,
  },
  tab: {
    flex: 1,
    paddingVertical: 3,
    paddingHorizontal: 4,
    backgroundColor: '#16213e',
    borderRadius: 6,
    alignItems: 'center',
    borderWidth: 2,
    borderColor: 'transparent',
  },
  tabActive: {
    borderColor: '#7ed957',
    backgroundColor: '#2a3a2a',
  },
  tabActiveRed: {
    borderColor: '#e94560',
    backgroundColor: '#3a2a2a',
  },
  tabText: { color: '#ffffff', fontSize: 13, fontWeight: '600' },
  selectedBar: {
    position: 'absolute',
    top: 0,
    left: 0,
    width: 필드_W,
    maxHeight: 50,
    zIndex: 50,
    backgroundColor: 'rgba(13, 20, 33, 0.85)',
    borderBottomLeftRadius: 8,
    borderBottomRightRadius: 8,
  },
  selectedBarContent: {
    alignItems: 'center',
    paddingHorizontal: 8,
    gap: 4,
  },
  selectedLabel: { color: '#aaaaaa', fontSize: 12, marginRight: 4 },
  selectedChip: {
    backgroundColor: '#16213e',
    borderColor: '#7ed957',
    borderWidth: 1,
    borderRadius: 6,
    paddingHorizontal: 6,
    paddingVertical: 3,
    alignItems: 'center',
    flexDirection: 'row',
    gap: 2,
  },
  selectedChipEmoji: { fontSize: 14 },
  selectedChipLv: { color: '#f5a623', fontSize: 11, fontWeight: 'bold' },
  field: {
    width: 필드_W,
    height: 필드_H,
    backgroundColor: '#0d1421',
    borderWidth: 2,
    borderColor: '#444',
    borderRadius: 8,
    overflow: 'hidden',
  },
  fieldInner: {
    position: 'absolute' as const,
    top: 0, left: 0,
    width: 필드_W,
    height: 필드_H,
    cursor: 'crosshair',
  } as any,
  fieldAttackMode: { borderColor: '#e94560' },
  zone: {
    position: 'absolute',
    borderWidth: 2,
    borderStyle: 'dashed',
    borderRadius: 8,
    padding: 6,
  },
  zoneLabel: { fontSize: 11, fontWeight: 'bold' },
  enemy: {
    position: 'absolute',
    width: 적_크기,
    height: 적_크기,
    borderRadius: 적_크기 / 2,
    alignItems: 'center',
    justifyContent: 'center',
    borderWidth: 2,
    borderColor: '#8a4a4a',
  },
  enemyText: { fontSize: 16 },
  bossLabel: { position: 'absolute', width: 120, textAlign: 'center', color: '#ff6b6b', fontSize: 10, fontWeight: 'bold' },
  hpBarBg: { position: 'absolute', width: 100, height: 8, backgroundColor: '#333', borderRadius: 4, overflow: 'hidden', borderWidth: 1, borderColor: '#555' },
  hpBarFill: { height: '100%', backgroundColor: '#ff6b6b' },
  hpText: { position: 'absolute', width: 120, textAlign: 'center', color: '#fff', fontSize: 9, fontWeight: 'bold' },
  marine: {
    position: 'absolute',
    width: 마린_크기,
    height: 마린_크기,
    backgroundColor: '#16213e',
    borderRadius: 마린_크기 / 2,
    alignItems: 'center',
    justifyContent: 'center',
    borderWidth: 2,
  },
  marineText: { fontSize: 14 },
  marineLv: {
    position: 'absolute',
    color: '#f5a623',
    fontSize: 9,
    fontWeight: 'bold',
    width: 30,
    textAlign: 'center',
  },
  selectRing: {
    position: 'absolute',
    width: 마린_크기 + 6,
    height: 마린_크기 + 6,
    borderRadius: (마린_크기 + 6) / 2,
    borderWidth: 2,
    borderColor: '#7ed957',
  },
  rangeRing: {
    position: 'absolute',
    borderRadius: 공격사거리,
    borderWidth: 1,
    borderColor: 'rgba(126, 217, 87, 0.25)',
    borderStyle: 'dashed',
  },
  dragBox: {
    position: 'absolute',
    borderWidth: 1,
    borderColor: '#7ed957',
    backgroundColor: 'rgba(126, 217, 87, 0.15)',
  },
  cursorHint: {
    position: 'absolute',
    top: 8,
    left: 0,
    right: 0,
    textAlign: 'center',
    color: '#e94560',
    fontWeight: 'bold',
    fontSize: 12,
  },
  actionBar: {
    flexDirection: 'row',
    width: 필드_W,
    marginTop: 4,
    backgroundColor: '#16213e',
    borderRadius: 8,
    padding: 4,
    gap: 3,
  },
  actionBtn: {
    flex: 1,
    paddingVertical: 6,
    backgroundColor: '#1f2a48',
    borderRadius: 6,
    alignItems: 'center',
    borderWidth: 2,
    borderColor: 'transparent',
  },
  actionBtnActive: {
    borderColor: '#7ed957',
    backgroundColor: '#2a3a2a',
  },
  actionBtnActiveRed: {
    borderColor: '#e94560',
    backgroundColor: '#3a2a2a',
  },
  actionBtnEmoji: { fontSize: 20 },
  actionBtnText: { color: '#ffffff', fontSize: 10, marginTop: 1, fontWeight: '600' },
  smallBtnBar: {
    flexDirection: 'row',
    width: 필드_W,
    marginBottom: 2,
    gap: 3,
    zIndex: 300,  // 패널보다 위 → 패널 열린 상태로도 클릭 가능
    elevation: 30,
  },
  smallBtn: {
    flex: 1,
    backgroundColor: '#1f2a48',
    borderRadius: 5,
    paddingVertical: 3,
    alignItems: 'center',
  },
  smallBtnText: { color: '#fff', fontSize: 10, fontWeight: '600' },
  statRow2: {
    flexDirection: 'row',
    alignItems: 'center',
    paddingVertical: 4,
    borderTopWidth: 1,
    borderTopColor: '#222',
  },
  statLabel: { color: '#aaa', fontSize: 11, flex: 1 },
  statVal: { color: '#f5a623', fontSize: 13, fontWeight: 'bold', minWidth: 40, textAlign: 'right', marginRight: 4 },
  statBtn: { backgroundColor: '#e94560', borderRadius: 4, width: 28, height: 24, alignItems: 'center', justifyContent: 'center' },
  statBtnOff: { backgroundColor: '#444' },
  statBtnText: { color: '#fff', fontSize: 14, fontWeight: 'bold' },
  divider: { height: 1, backgroundColor: '#333', marginVertical: 6 },
  upgRow: { flexDirection: 'row', alignItems: 'center', paddingVertical: 6, borderTopWidth: 1, borderTopColor: '#222' },
  upgLabel: { color: '#fff', fontSize: 13, fontWeight: '600' },
  upgEffect: { color: '#aaa', fontSize: 11, marginTop: 1 },
  upgBtn: { backgroundColor: '#e94560', borderRadius: 5, paddingHorizontal: 10, paddingVertical: 8, minWidth: 80, alignItems: 'center' },
  upgBtnOff: { backgroundColor: '#444' },
  upgBtnText: { color: '#fff', fontSize: 12, fontWeight: 'bold' },
  gemRow: { flexDirection: 'row', alignItems: 'center', paddingVertical: 6, paddingHorizontal: 6, borderTopWidth: 1, borderTopColor: '#222', gap: 8 },
  gemRowEq: { backgroundColor: '#1a3a1a' },
  gemGrade: { color: '#f5a623', fontSize: 11 },
  gemType: { color: '#fff', fontSize: 13, fontWeight: 'bold', width: 50 },
  gemEffect: { color: '#7ed957', fontSize: 12, flex: 1 },
  gemBadge: { color: '#888', fontSize: 11, fontWeight: 'bold' },
  hintBar: {
    width: 필드_W,
    marginTop: 2,
    backgroundColor: '#16213e',
    borderRadius: 6,
    paddingVertical: 2,
    paddingHorizontal: 6,
    alignItems: 'center',
  },
  hintText: { color: '#7ed957', fontSize: 10, fontWeight: '600' },
  message: {
    marginTop: 2,
    fontSize: 12,
    color: '#f5a623',
    fontWeight: 'bold',
    textAlign: 'center',
  },
  zoneInfo: {
    marginTop: 2,
    width: 필드_W,
    backgroundColor: '#16213e',
    paddingVertical: 2,
    paddingHorizontal: 5,
    borderRadius: 4,
  },
  zoneInfoText: { color: '#aaaaaa', fontSize: 9, marginVertical: 0 },
  backButton: {
    marginTop: 10,
    backgroundColor: '#16213e',
    padding: 12,
    borderRadius: 8,
    borderWidth: 2,
    borderColor: '#7ed957',
    width: 필드_W,
    alignItems: 'center',
  },
  backButtonText: { color: '#7ed957', fontSize: 14, fontWeight: 'bold' },
  currencyPanel: {
    position: 'absolute',
    top: 260,
    left: 8,
    right: 8,
    backgroundColor: '#16213e',
    padding: 10,
    borderRadius: 10,
    borderWidth: 2,
    borderColor: '#3a5a8a',
    zIndex: 250,
    maxHeight: 화면H - 360,
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 4 },
    shadowOpacity: 0.5,
    shadowRadius: 8,
    elevation: 25,
  },
  currencyHeader: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    marginBottom: 8,
    paddingBottom: 6,
    borderBottomWidth: 1,
    borderBottomColor: '#3a5a8a',
  },
  currencyTitle: { color: '#fff', fontSize: 15, fontWeight: 'bold' },
  currencyGrid: { flexDirection: 'row', flexWrap: 'wrap', rowGap: 6, columnGap: 12 },
  currencyItem: { fontSize: 12, fontWeight: '600', width: '31%' },
  currencySection: { color: '#a855f7', fontSize: 12, fontWeight: 'bold', marginTop: 10, marginBottom: 6 },
  currencyBtnRow: { flexDirection: 'row', gap: 4, flexWrap: 'wrap' },
  prodPanel: {
    position: 'absolute',
    top: 260,  // 탭/버튼바 아래로 내려서 버튼바와 안 겹치게
    left: 8,
    right: 8,
    backgroundColor: '#16213e',
    padding: 12,
    borderRadius: 10,
    borderWidth: 2,
    borderColor: '#7ed957',
    zIndex: 100,
    maxHeight: 화면H - 360,
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 4 },
    shadowOpacity: 0.5,
    shadowRadius: 8,
    elevation: 12,
  },
  prodHeader: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center', marginBottom: 2 },
  prodTitle: { color: '#7ed957', fontSize: 16, fontWeight: 'bold' },
  closeBtn: { color: '#aaa', fontSize: 20, paddingHorizontal: 8 },
  prodSubtitle: { color: '#aaaaaa', fontSize: 11, marginBottom: 8 },
  prodGrid: {
    flexDirection: 'row',
    flexWrap: 'wrap',
    gap: 4,
    justifyContent: 'space-between',
  },
  prodBtn: {
    width: '23%',
    backgroundColor: '#1f2a48',
    borderRadius: 6,
    padding: 6,
    alignItems: 'center',
    marginBottom: 4,
  },
  prodBtnDisabled: { opacity: 0.4 },
  prodBtnLv: { color: '#f5a623', fontSize: 14, fontWeight: 'bold' },
  prodBtnCost: { color: '#aaaaaa', fontSize: 10, marginTop: 1 },
  controls: {
    backgroundColor: '#16213e',
    paddingVertical: 3,
    paddingHorizontal: 8,
    borderRadius: 6,
    marginTop: 3,
    width: 필드_W,
  },
  controlsTitle: { color: '#e94560', fontSize: 10, fontWeight: 'bold', marginBottom: 0 },
  controlsText: { color: '#aaaaaa', fontSize: 9 },
  resetButton: {
    marginTop: 4,
    backgroundColor: '#333',
    paddingVertical: 3,
    paddingHorizontal: 8,
    borderRadius: 6,
    marginBottom: 6,
  },
  resetButtonText: { color: '#aaa', fontSize: 10 },
  statTabBtn: {
    flex: 1, backgroundColor: '#1f2a48', borderRadius: 6,
    paddingVertical: 5, alignItems: 'center', borderWidth: 1, borderColor: '#444',
  },
  statTabBtnOn: { backgroundColor: '#7ed957', borderColor: '#7ed957' },
  statTabText: { color: '#aaa', fontSize: 12, fontWeight: 'bold' },
})
